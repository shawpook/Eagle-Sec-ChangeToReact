import { EventHandler, EventUnsubscribe, MediaEventMap, AllEventMap } from './types/events';

/**
 * 類型安全的事件總線實現
 * @template EventMap - 事件類型映射
 */
export declare class EventBus<EventMap extends object = AllEventMap> {
    private handlers;
    /**
     * 訂閱事件
     * @param event - 事件名稱
     * @param handler - 事件處理器
     * @returns 取消訂閱函數
     */
    on<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): EventUnsubscribe;
    /**
     * 取消訂閱事件
     * @param event - 事件名稱
     * @param handler - 事件處理器
     */
    off<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): void;
    /**
     * 訂閱一次性事件
     * @param event - 事件名稱
     * @param handler - 事件處理器
     * @returns 取消訂閱函數
     */
    once<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): EventUnsubscribe;
    /**
     * 觸發事件
     * @param event - 事件名稱
     * @param data - 事件數據
     */
    emit<K extends keyof EventMap>(event: K, data: EventMap[K]): void;
    /**
     * 檢查是否有指定事件的監聽器
     * @param event - 事件名稱
     */
    hasListeners<K extends keyof EventMap>(event: K): boolean;
    /**
     * 移除指定事件的所有監聽器
     * @param event - 事件名稱
     */
    removeListeners<K extends keyof EventMap>(event: K): void;
    /**
     * 移除所有事件監聯器
     */
    removeAllListeners(): void;
    /**
     * 獲取指定事件的監聽器數量
     * @param event - 事件名稱
     */
    listenerCount<K extends keyof EventMap>(event: K): number;
    /**
     * 獲取所有已註冊的事件名稱
     */
    eventNames(): Array<keyof EventMap>;
}
/**
 * 媒體事件總線
 * 專用於媒體相關事件
 */
export declare class MediaEventBus extends EventBus<MediaEventMap> {
    /**
     * 觸發媒體事件並同時派發 DOM 事件
     * @param target - DOM 事件目標元素
     * @param event - 事件名稱
     * @param data - 事件數據
     */
    emitWithDOM<K extends keyof MediaEventMap>(target: EventTarget, event: K, data: MediaEventMap[K]): void;
}
/**
 * 創建帶有事件代理的事件總線
 * 允許將事件轉發到另一個 EventTarget
 */
export declare function createProxyEventBus<EventMap extends object>(target: EventTarget): EventBus<EventMap> & {
    proxyTo(target: EventTarget): void;
};
/**
 * 事件節流器
 * 限制事件觸發頻率
 */
export declare function throttleEvent<T>(handler: EventHandler<T>, interval: number): EventHandler<T>;
/**
 * 事件防抖器
 * 只在事件停止觸發後執行
 */
export declare function debounceEvent<T>(handler: EventHandler<T>, delay: number): EventHandler<T>;
//# sourceMappingURL=EventBus.d.ts.map