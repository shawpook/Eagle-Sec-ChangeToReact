import { C as ControlPlugin } from "../../../ControlPlugin-c47a1127.js";
import { f as fullscreenIcon, d as fullscreenExitIcon } from "../../../index-5760cd8c.js";
const styles = "/**\r\n * Fullscreen Plugin Styles\r\n */\r\n\r\n.vp-fullscreen-btn {\r\n    width: var(--vp-button-size, 24px);\r\n    height: var(--vp-button-size, 24px);\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    background: transparent;\r\n    border: none;\r\n    border-radius: 4px;\r\n    cursor: pointer;\r\n    transition: background var(--vp-transition-duration, 0.15s);\r\n    padding: 0;\r\n    flex-shrink: 0;\r\n}\r\n\r\n.vp-fullscreen-btn:hover {\r\n    background: var(--vp-button-hover-bg, rgba(44, 47, 50, 0.08));\r\n}\r\n\r\n.vp-fullscreen-btn svg {\r\n    width: var(--vp-icon-size, 24px);\r\n    height: var(--vp-icon-size, 24px);\r\n}\r\n\r\n/* 全螢幕狀態切換圖標 */\r\n.vp-fullscreen-btn .vp-icon-exit-fullscreen {\r\n    display: none;\r\n}\r\n\r\n:host(.fullscreen) .vp-fullscreen-btn .vp-icon-fullscreen {\r\n    display: none;\r\n}\r\n\r\n:host(.fullscreen) .vp-fullscreen-btn .vp-icon-exit-fullscreen {\r\n    display: block;\r\n}\r\n";
const FullscreenPlugin = {
  name: "fullscreen",
  version: "1.0.0",
  styles,
  install(player, options = {}) {
    return new FullscreenManager(player, {
      position: options.position ?? "right",
      order: options.order ?? 20
    });
  }
};
class FullscreenManager extends ControlPlugin {
  constructor() {
    super(...arguments);
    this.name = "fullscreen";
  }
  createEl() {
    const btn = document.createElement("button");
    btn.className = "vp-btn vp-fullscreen-btn";
    btn.title = "全螢幕 (F)";
    btn.innerHTML = `
            <span class="vp-icon-fullscreen">${fullscreenIcon}</span>
            <span class="vp-icon-exit-fullscreen">${fullscreenExitIcon}</span>
        `;
    this.on(btn, "click", (e) => {
      e.stopPropagation();
      this.toggleFullscreen();
    });
    this.on(document, "fullscreenchange", () => this.updateState());
    return btn;
  }
  toggleFullscreen() {
    if (!document.fullscreenElement) {
      this.player.requestFullscreen();
    } else {
      document.exitFullscreen();
    }
  }
  updateState() {
    const isFullscreen = !!document.fullscreenElement;
    this.player.classList.toggle("fullscreen", isFullscreen);
  }
}
export {
  FullscreenManager,
  FullscreenPlugin,
  FullscreenPlugin as default
};
//# sourceMappingURL=index.js.map
