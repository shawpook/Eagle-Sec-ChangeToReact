import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface ProgressPluginOptions extends ControlPluginOptions {
    /** 是否顯示 tooltip */
    showTooltip?: boolean;
    /** 是否啟用縮圖預覽 */
    showThumbnail?: boolean;
    /** 縮圖寬度 */
    thumbnailWidth?: number;
    /** 縮圖高度 */
    thumbnailHeight?: number;
}
export declare const ProgressPlugin: MpvPlugin<ProgressPluginOptions>;
export declare class ProgressManager extends ControlPlugin<Required<ProgressPluginOptions & {
    position: 'progress';
    order: number;
}>> {
    readonly name = "progress";
    private progressBar;
    private progressBuffered;
    private progressPlayhead;
    private progressTooltip;
    private tooltipTime;
    private thumbnailCanvas;
    private thumbnailWrapper;
    private thumbnailRenderer;
    private isHovering;
    private lastThumbnailRequestTime;
    private static readonly THUMBNAIL_THROTTLE_MS;
    private isDragging;
    private wasPlayingBeforeDrag;
    protected createEl(): HTMLElement;
    /**
     * 覆寫插入方法 - 直接附加到 progress-section
     */
    protected insertElement(): void;
    private initializeThumbnail;
    private subscribeToThumbnailEvents;
    private bindEvents;
    private updateSeekFromMouse;
    private updateTooltip;
    /**
     * 請求縮圖（帶節流）
     * 就是簡單地 seek 到指定時間，rAF 循環會自動渲染
     */
    private requestThumbnail;
    /**
     * 收到縮圖幀，直接渲染（模仿主播放器 Seek 的行為）
     */
    private onThumbnailReceived;
    private clearThumbnail;
    private hideTooltip;
    private updateProgress;
    private updatePlayhead;
    /** 獲取進度條元素（供其他插件使用，如 ABLoop） */
    getProgressBar(): HTMLElement | null;
    /** 獲取 tooltip 容器（供縮圖插件使用） */
    getTooltip(): HTMLElement | null;
    destroy(): void;
}
//# sourceMappingURL=ProgressPlugin.d.ts.map