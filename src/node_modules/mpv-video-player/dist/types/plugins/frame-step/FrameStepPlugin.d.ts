import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface FrameStepPluginOptions extends ControlPluginOptions {
    position?: 'left' | 'right';
    order?: number;
    /** 長按初始延遲（毫秒） */
    initialDelay?: number;
    /** 長按初始重複間隔（毫秒） */
    repeatInterval?: number;
    /** 長按最快重複間隔（毫秒），加速後不會低於此值 */
    minInterval?: number;
    /** 加速因子，每次重複後間隔乘以此值（0.85 = 每次快 15%） */
    accelerationFactor?: number;
}
export declare const FrameStepPlugin: MpvPlugin<FrameStepPluginOptions>;
export declare class FrameStepManager extends ControlPlugin<Required<FrameStepPluginOptions>> {
    readonly name = "frameStep";
    private prevBtn;
    private nextBtn;
    protected createEl(): HTMLElement;
    /**
     * 覆寫插入方法，將兩個按鈕分別插入
     */
    protected insertElement(): void;
    /**
     * 設置長按加速重複按鈕
     *
     * 加速機制：
     * - 初始延遲後開始重複
     * - 每次重複後，間隔乘以 accelerationFactor（變快）
     * - 最快不超過 minInterval
     *
     * 例如：初始 100ms，加速因子 0.85，最小 16ms
     * → 100ms → 85ms → 72ms → 61ms → 52ms → 44ms → 37ms → 32ms → 27ms → 23ms → 20ms → 17ms → 16ms
     */
    private setupRepeatButton;
    enable(): void;
    disable(): void;
    destroy(): void;
}
//# sourceMappingURL=FrameStepPlugin.d.ts.map