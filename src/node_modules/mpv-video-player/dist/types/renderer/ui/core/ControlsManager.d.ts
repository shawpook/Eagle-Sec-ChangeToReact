/**
 * ControlsManager - 控制列 UI 管理
 *
 * 負責：
 * - 控制列顯示/隱藏邏輯
 * - 控制列模式管理 (always/auto/none)
 * - 控制列相關事件綁定
 * - CSS 狀態類別管理
 */
export type ControlsMode = 'always' | 'auto' | 'none';
/**
 * ControlsManager 配置
 */
export interface ControlsManagerConfig {
    /** Host 元素（MpvVideoElement 自身） */
    host: HTMLElement;
    /** Shadow Root */
    shadowRoot: ShadowRoot;
    /** 視頻包裝元素 */
    videoWrapper: HTMLDivElement;
    /** 控制列元素 */
    controlsEl: HTMLDivElement;
    /** 切換播放/暫停 */
    toggle: () => void;
    /** 切換全螢幕 */
    toggleFullscreen: () => void;
    /** 獲取音量 */
    getVolume: () => number;
}
/**
 * 控制列管理器
 */
export declare class ControlsManager {
    private config;
    private _mode;
    private _userMode;
    private _initialized;
    private _visible;
    private hideTimeout;
    private cleanups;
    private static readonly HIDE_DELAY;
    constructor(config: ControlsManagerConfig);
    /**
     * 初始化控制列（只應在 connectedCallback 中調用一次）
     */
    initialize(): void;
    /**
     * 獲取控制列模式
     */
    get mode(): ControlsMode;
    /**
     * 設置控制列模式
     */
    set mode(value: ControlsMode);
    /**
     * 獲取用戶設定的控制列模式（不受全螢幕覆蓋影響）
     */
    get userMode(): ControlsMode;
    /**
     * 控制列是否可見
     */
    get visible(): boolean;
    /**
     * 是否已初始化
     */
    get initialized(): boolean;
    /**
     * 顯示控制列
     */
    show(): void;
    /**
     * 隱藏控制列
     */
    hide(): void;
    /**
     * 取消隱藏計時器
     */
    cancelHide(): void;
    /**
     * 排程隱藏控制列
     */
    scheduleHide(): void;
    /**
     * 應用控制列模式
     */
    applyMode(): void;
    /**
     * 銷毀並清理資源
     */
    destroy(): void;
    /**
     * 添加事件監聽器並追蹤以便清理
     */
    private on;
    /**
     * 綁定控制列核心事件
     */
    private bindCoreEvents;
    /**
     * 綁定狀態類別事件（供 CSS 使用）
     */
    private bindStateClassEvents;
}
//# sourceMappingURL=ControlsManager.d.ts.map