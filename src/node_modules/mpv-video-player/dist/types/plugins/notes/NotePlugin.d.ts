import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * 筆記資料結構
 */
export interface Note {
    /** 筆記內容 */
    content: string;
    /** 影片時間點（秒） */
    videoTime: number;
    /** 建立時間 */
    createdAt: Date;
    /** 標記顏色（可選） */
    color?: string;
}
/**
 * 筆記插件配置
 */
export interface NotePluginOptions {
    /** 按鈕在控制列的順序，默認 75 */
    order?: number;
    /** 筆記最大字數限制，默認 500 */
    maxLength?: number;
}
/**
 * 筆記插件定義
 */
export declare const NotePlugin: MpvPlugin<NotePluginOptions>;
/**
 * 筆記管理器
 */
export declare class NoteManager implements PluginInstance {
    readonly name = "notes";
    private player;
    private options;
    private notes;
    private toggleBtn;
    private dialogEl;
    private markersContainer;
    private _enabled;
    private _dialogVisible;
    private eventCleanups;
    constructor(player: MpvVideoElement, options: Required<NotePluginOptions>);
    private initialize;
    /**
     * 創建工具列按鈕
     */
    private createToggleButton;
    /**
     * 創建筆記輸入對話框
     */
    private createDialog;
    /**
     * 創建進度條標記容器
     */
    private createMarkersContainer;
    /**
     * 顯示對話框
     */
    showDialog(): void;
    /**
     * 隱藏對話框
     */
    hideDialog(): void;
    /**
     * 儲存筆記
     */
    private saveNote;
    /**
     * 渲染進度條標記
     */
    private renderMarkers;
    private formatTime;
    private insertByOrder;
    private addEvent;
    /** 獲取所有筆記 */
    getNotes(): Note[];
    /** 新增筆記（程式化方式） */
    addNote(content: string, videoTime?: number): Note;
    /** 刪除筆記 */
    removeNote(index: number): boolean;
    /** 清空所有筆記 */
    clearNotes(): void;
    /** 匯出筆記（JSON 格式） */
    exportNotes(): string;
    /** 匯入筆記（JSON 格式） */
    importNotes(json: string): boolean;
    enable(): void;
    disable(): void;
    get enabled(): boolean;
    destroy(): void;
}
//# sourceMappingURL=NotePlugin.d.ts.map