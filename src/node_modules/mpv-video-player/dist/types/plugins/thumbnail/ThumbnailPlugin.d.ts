import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * 縮圖插件配置
 */
export interface ThumbnailPluginOptions {
    /** 縮圖寬度，默認 160 */
    width?: number;
    /** 縮圖高度，默認 90 */
    height?: number;
}
/**
 * 縮圖插件定義
 */
export declare const ThumbnailPlugin: MpvPlugin<ThumbnailPluginOptions>;
/**
 * 縮圖管理器
 */
export declare class ThumbnailManager implements PluginInstance {
    readonly name = "thumbnail";
    private player;
    private options;
    private tooltipEl;
    private thumbnailCanvas;
    private thumbnailWrapper;
    private renderer;
    private _enabled;
    private isHovering;
    private eventUnsubscribe;
    private boundOnProgressHover;
    private boundOnProgressLeave;
    /** 緩存進度條容器元素，避免 destroy 時重複查詢 */
    private progressContainer;
    constructor(player: MpvVideoElement, options: Required<ThumbnailPluginOptions>);
    private initialize;
    private bindEvents;
    private subscribeToThumbnailEvents;
    private onProgressHover;
    private onProgressLeave;
    private requestThumbnail;
    private fetchThumbnail;
    private onThumbnailReceived;
    private clearThumbnail;
    enable(): void;
    disable(): void;
    get enabled(): boolean;
    destroy(): void;
}
//# sourceMappingURL=ThumbnailPlugin.d.ts.map