import { Component, ComponentOptions } from './Component';
import { MpvVideoElement } from '../renderer/ui/MpvVideoElement';

/**
 * 按鈕配置選項
 */
export interface ButtonOptions extends ComponentOptions {
    /** 按鈕文字（無圖標時顯示） */
    text?: string;
    /** 圖標 HTML（SVG 或 img） */
    icon?: string;
    /** 替代圖標（用於狀態切換） */
    iconAlt?: string;
    /** ARIA 標籤 */
    ariaLabel?: string;
    /** 是否禁用 */
    disabled?: boolean;
    /** 點擊回調（簡化使用） */
    onClick?: (event: MouseEvent) => void;
}
/**
 * 按鈕組件基類
 */
export declare abstract class Button extends Component {
    static componentName: string;
    protected options: ButtonOptions;
    /** 當前顯示的是替代圖標 */
    private isAltIcon;
    constructor(player: MpvVideoElement, options?: ButtonOptions);
    /**
     * 創建按鈕元素
     */
    protected createEl(): HTMLElement;
    /**
     * 內部點擊處理
     */
    private onClickInternal;
    /**
     * 子類可重寫：處理點擊事件
     */
    protected handleClick(_event: MouseEvent): void;
    /**
     * 設置按鈕圖標
     */
    setIcon(iconHtml: string): void;
    /**
     * 切換圖標
     */
    toggleIcon(showAlt: boolean): void;
    /**
     * 切換到替代圖標
     */
    showAltIcon(): void;
    /**
     * 切換到主圖標
     */
    showMainIcon(): void;
    /**
     * 設置按鈕文字
     */
    setText(text: string): void;
    /**
     * 設置按鈕標題（tooltip）
     */
    setTitle(title: string): void;
    /**
     * 設置活動狀態
     */
    setActive(active: boolean): void;
    /**
     * 是否處於活動狀態
     */
    isActive(): boolean;
}
/**
 * 切換按鈕
 * 有兩個狀態，點擊切換
 */
export declare abstract class ToggleButton extends Button {
    /** 當前是否處於「開」狀態 */
    protected toggled: boolean;
    /**
     * 子類實現：獲取當前狀態
     */
    protected abstract isToggled(): boolean;
    /**
     * 子類實現：執行切換操作
     */
    protected abstract toggle(): void;
    protected handleClick(_event: MouseEvent): void;
    /**
     * 更新視覺狀態
     */
    protected updateVisualState(): void;
}
//# sourceMappingURL=Button.d.ts.map