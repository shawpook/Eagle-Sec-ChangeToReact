# 项目结构

> 生成时间：2026-08-09
> 项目名称：Eagle 1:1 复刻工作区
> 工作区：`H:\dev\Eagle-Sec-development`

## 项目概览

这是 Eagle 桌面软件的 1:1 复刻工程。项目复用 `src/` 中原版 Eagle 4.0.0 的前端资源（AngularJS 主界面、Viewer、多语言和 `my_modules`），通过 Vite 提供浏览器预览，用 Express 实现兼容 API、缩略图服务和浏览器扩展采集服务，并用 Electron 组装桌面端。

主要入口：

- `src/app/index.html`：原版主界面。
- `frontend/public/pages.html`：可预览页面入口页。
- `frontend/public/workbench.html`：资源库管理工作台。
- `frontend/public/roadmap.html`：批量管理、重复合并、Eaglepack、插件中心等执行面板。
- `frontend/document-viewer/index.html`：React 文档查看器。
- `backend/src/server.js`：本地 API、缩略图服务、扩展采集服务。
- `electron/main.cjs`：Electron 主进程。

## 顶层结构

```text
Eagle-Sec-development/
├─ .claude/                          # 本机/代理配置
├─ .edge-cdp-profile/                # 浏览器捕获测试使用的 Edge CDP profile
├─ .git/
├─ backend/
│  └─ src/                           # Express 后端
├─ dist/
│  └─ frontend/                      # npm run build 产物
├─ docs/                             # 构建审查、资源复用路线、拖放修复文档
├─ electron/
│  ├─ main.cjs                       # Electron 主进程
│  ├─ preload.cjs                    # contextBridge + IPC 暴露层
│  ├─ pdf-viewer/                    # PDF.js 静态资源
│  ├─ pdf-thumbnail-worker.cjs/html  # PDF 缩略图 worker
│  ├─ video-thumbnail-worker.cjs/html # 视频缩略图 worker
├─ frontend/
│  ├─ vite.preview.config.mjs        # Vite 预览/构建配置
│  ├─ document-viewer/               # React 文档查看器源码
│  └─ public/                        # 静态页面、shims、mock 数据/资源
├─ node_modules/
├─ screenshots/                      # 截图回归产物
├─ scripts/
│  ├─ clear-ports.mjs                # 清理本地开发端口
│  └─ health-check.mjs               # 检查 Vite/API/缩略图/扩展服务
├─ src/                              # 原版 Eagle 前端资源与依赖
│  ├─ app/
│  ├─ build/
│  ├─ i18n/
│  ├─ my_modules/
│  ├─ node_modules/
│  ├─ test/
│  ├─ CLAUDE.md
│  ├─ config.js
│  ├─ package.json
│  ├─ run.js / run.jsc / main.jsc / eagle.babel
│  └─ 其他原版构建/运行文件
├─ test-run/                         # 测试运行 user-data 与临时 .library
├─ tests/                            # 自动化测试与 fixtures
├─ tests-tmp/                        # 测试临时日志/截图
├─ 图片参考定位组件位置/              # UI 定位参考截图
├─ README.md
├─ TASK.md
├─ package.json
└─ 启动开发环境.bat
```

## backend

`backend/src/server.js` 是唯一的后端入口，启动时会同时创建三个 Express 服务：

| 服务 | 默认端口 | 说明 |
| --- | --- | --- |
| API | `41695`，兼容 `41595` | Eagle 兼容 REST API |
| Thumbnail | `41692`，兼容 `41592` | 本地缩略图/文件服务 |
| Extension | `41693`，兼容 `41593` | 浏览器扩展采集服务 |

后端按职责拆分为以下模块：

| 文件 | 职责 |
| --- | --- |
| `server.js` | Express 入口、路由、v1/v2 API、三个服务监听 |
| `library-service.js` | 资源库创建、打开、切换、当前库管理 |
| `library-store.js` | `.library` 元数据、items、folders、tags 的读写 |
| `library-strict.js` | 资源库数据结构校验 |
| `library-transaction-coordinator.js` | 资源库事务、故障回滚、崩溃恢复 |
| `library-consistency-service.js` | 一致性扫描与报告 |
| `library-backup-service.js` | 恢复点、备份、恢复与校验 |
| `library-stats.js` | 资源库统计与 repair |
| `library-migration.js` | 真实 `.library` 扫描与迁移 |
| `importer.js` | 文件、路径、URL、Base64、书签、文件夹导入 |
| `export-service.js` | 图片/文件夹导出 |
| `eaglepack.js` | `.eaglepack` 打包与导入 |
| `csv-export.js` | CSV 导出 |
| `duplicates.js` | 重复条目扫描与合并 |
| `item-workflow-service.js` | 批量更新、重命名、移动等条目工作流 |
| `thumbnailer.js` | 缩略图路径与基础处理 |
| `thumbnail-task-service.js` | 缩略图任务、状态、取消 |
| `custom-thumbnail.js` | 自定义缩略图 |
| `color-analyzer.js` | 调色板分析队列 |
| `media-info.js` | 媒体类型识别 |
| `file-format-policy.js` | 支持格式/缩略图格式策略 |
| `text-thumbnail-renderer.js` | 文本类缩略图渲染 |
| `text-detail-service.js` | 文本详情读取 |
| `text-save-service.js` | 文本保存与撤销 |
| `text-document-support.js` | 文本文档支持 |
| `office-thumbnail-renderer.js` | Office 缩略图渲染 |
| `legacy-office-thumbnail-renderer.js` | 旧版 Office 格式兼容 |
| `office-document-viewer.js` | Office 文档查看与保存 |
| `office-document-support.js` | Office 格式常量与支持逻辑 |
| `document-preview-service.js` | 文档预览源/文件解析 |
| `search-service.js` | 搜索与筛选 |
| `search-index-service.js` | 持久化搜索索引 |
| `smart-folders.js` | 智能文件夹条目计算 |
| `smart-folder-rules.js` | 智能文件夹规则校验 |
| `capture-service.js` | 浏览器捕获协议与采集保存 |
| `controlled-downloader.js` | URL 受控下载、状态、取消、释放 |
| `plugin-package.js` | 插件打包、安装、卸载 |
| `plugin-runtime.js` | 插件生命周期运行支持 |
| `security.js` | localhost/token 鉴权与统一错误处理 |

## frontend

### Vite 配置

`frontend/vite.preview.config.mjs` 使用工作区根目录作为 Vite root：

- `/` 重定向到 `/src/app/index.html`。
- 原版 `/src/app/*.html` 页面由 middleware 注入 `mock-data.js` 和 `shims.js`。
- `registration.html`、`manage-device.html` 使用 `frontend/public/replaced/` 中的安全替代页。
- `/file` 代理到缩略图服务，默认 `41692`。
- 构建输出到 `dist/frontend`。

### 静态页面与公共资源

```text
frontend/public/
├─ pages.html                       # 预览页面入口
├─ workbench.html                   # 资源库管理工作台
├─ roadmap.html                     # 路线图执行面板
├─ shims.js                         # 浏览器/Electron 运行时 shim
├─ mock-data.js                     # 主界面 mock 条目数据
├─ browser-extension/               # Manifest V3 浏览器扩展
├─ media-viewer/
│  ├─ video.html
│  └─ audio.html
├─ mock-assets/                     # 示例 PDF、GIF、WebP、WAV、HDR、GLB、OBJ
├─ mock-library/
│  └─ Eagle Reverse Demo.library/   # 演示 .library
├─ replaced/
│  ├─ registration.html             # 安全替代注册页
│  └─ manage-device.html            # 安全替代设备管理页
└─ src/app/text-editor/             # 复制的 Text Editor 预览页面
```

### Document Viewer

`frontend/document-viewer/` 是一个 React 19 文档查看器：

```text
frontend/document-viewer/
├─ index.html
└─ src/
   ├─ DocumentViewerApp.tsx
   ├─ main.tsx
   ├─ components/       # AssetPreviewStage、OfficeDocumentSurface、TextDocumentSurface 等
   ├─ hooks/            # use-assets、use-preview
   ├─ lib/              # api、i18n、settings、action-history、system-theme、utils
   ├─ shared/           # asset-formats、types
   ├─ stores/           # library-store、ui-store
   └─ styles/           # Tailwind CSS + eagle-document-theme.css
```

#### 本轮 UI 调整

- `frontend/document-viewer/src/styles/eagle-document-theme.css`：统一 Eagle 暗/亮主题、圆形按钮、无荧光背景。
- `TextDocumentSurface.tsx`：Markdown 工具栏改用 lucide 图标，移除右侧 Edit/Live/Preview/Fullscreen 按钮，并统一编辑区字体、字号、行高和内边距，修复光标与文字错位。
- 代码块默认显示行号：通过 `pluginsFilter` 给 `rehype-prism-plus` 注入 `showLineNumbers: true`；指定行高亮沿用 ```` ```js {1,3-4} ```` 语法；复制按钮按 Eagle 风格做成圆形小按钮。
- 字体切换是自定义功能，不是 `@uiw/react-md-editor` 自带；保存到 viewer 的 localStorage key `eagle.document-viewer.settings`，相关代码在 `lib/settings.ts`、`lib/api.ts`、`stores/ui-store.ts`。

## electron

`electron/main.cjs` 是 Electron 主进程，负责：

- 创建主窗口、预览窗口、Viewer 窗口和插件窗口。
- 通过 `ipcMain` 暴露资源库、文件系统、剪贴板、导入导出、缩略图、重复合并、插件等能力。
- 提供 `fs:list`、`fs:read`、`library:resolve` 等文件 IPC，并带路径白名单/穿越防护。
- 内置多个 smoke 模式：`--smoke`、`--smoke-plugin`、`--smoke-desktop`、`--smoke-library`、`--smoke-document-viewer` 等。

`electron/preload.cjs` 通过 `contextBridge` 把主进程 IPC 封装为前端可用的 `window.eagleDesktop` API。

`electron/pdf-thumbnail-worker.*` 和 `electron/video-thumbnail-worker.*` 用于生成 PDF/视频缩略图；`electron/pdf-viewer/` 存放 PDF.js 运行资源。

偏好设置窗口链路：

- `electron/main.cjs` 注册 `open.preferences` IPC，创建/复用无边框 `preferences.html` 窗口，并支持 `panel` / `keyword` 参数。
- `frontend/public/shims.js` 收到 `open.preferences` 时优先转发给原生 Electron IPC；纯浏览器环境回退为 `window.open('/src/app/preferences.html?...')`。
- 主界面左上角应用菜单由 `src/app/app.bundle.js` 的 AngularJS 菜单触发；已修复自定义菜单点击时 `event` 缺失导致“偏好设置”无响应的问题。

## src（原版 Eagle 前端）

`src/` 是原版 Eagle 4.0.0 前端反编译/复用资源：

```text
src/
├─ app/
│  ├─ index.html                     # 原版主界面
│  ├─ app.bundle.js                  # 原版 AngularJS bundle
│  ├─ background.html / main.js
│  ├─ preferences.html               # 偏好设置
│  ├─ preview-window.html            # 预览窗口
│  ├─ progress.html                  # 进度窗口
│  ├─ registration.html              # 原版注册页（预览时被替换）
│  ├─ manage-device.html             # 原版设备管理页（预览时被替换）
│  ├─ thumbnail.html
│  ├─ collect-window/                # 采集窗口 AngularJS 前端
│  ├─ exif-viewer/                   # EXIF Viewer
│  ├─ font-viewer/                   # Font Viewer
│  ├─ gif-viewer/                    # GIF Viewer
│  ├─ model-viewer/                  # Model Viewer
│  ├─ native-viewer/                 # Native Viewer
│  ├─ pdf-viewer/                    # PDF Viewer
│  ├─ raw-viewer/                    # RAW Viewer
│  ├─ text-editor/                   # Text Editor
│  ├─ texture-viewer/                # Texture Viewer
│  ├─ assets/                        # 原版图片/图标资源
│  ├─ css/ style/                    # 原版样式
│  ├─ fonts/                         # 内置字体
│  ├─ images/ sounds/                # 原版界面资源
│  └─ js/                            # controllers/services/directives/modules/plugin/workers
├─ build/config.gypi                 # 原版构建残留配置
├─ i18n/                             # de_DE/en/es_ES/ja_JP/ko_KR/ru_RU/zh_CN/zh_TW
├─ my_modules/                       # 原版内置 Node/前端模块
├─ test/api-v2/test-snippets.js
├─ config.js                         # 支持格式、端口等原版配置
└─ package.json                      # Eagle 4.0.0 原版元数据/构建脚本
```

`src/app/preferences.html` 是原版偏好设置窗口；主界面左上角应用菜单和快捷键 `Ctrl+,` 都通过 `open.preferences` IPC 打开该页面。

`src/my_modules/` 主要包含：

```text
access, appdata-path, applescript, audiobuffer-slice, bplist-parse,
cartesian-product, check-disk-space, chinese_convert, curl-request,
electron-download, electron-settings, electron-window-state, exif,
extract-file-from-zip, extract-icon, file-icon, file-type,
get-associated-application, get-drive-type, heif, image-cropper,
image-palette, image-size, iptc, is-directory, is-hidden-file,
is-network-drive, jpgjs, json-rest-light, junk, model-thumbnail, ms,
n-readlines, native-mouse, opentype, osascript, pako, pinyinlite, pngjs,
quantize, raw-parser, readimage, sanitize-filename, set-icon, sudo-prompt,
tiny-pinyin, tmp, ua-parser, unicode-normalize, UPNG, url, utif, vtt2srt,
win-clipboard, zip-folder
```

## tests

`tests/` 包含 API smoke、资源库闭环、Electron、浏览器扩展、截图回归和 UI 交互测试：

```text
tests/
├─ fixtures/
│  ├─ library-example/Demo.library/         # 最小演示 .library
│  ├─ plugins/example-service-plugin/       # 示例 service plugin
│  └─ browser-extension-mv2/                # MV2 扩展 fixture
├─ api-smoke.mjs
├─ validate-library.mjs
├─ plugin-smoke.mjs
├─ library-create-service.mjs / library-create-api.mjs
├─ image-import-closed-loop.mjs / folder-import-closed-loop.mjs
├─ collect-save-closed-loop.mjs / image-export-closed-loop.mjs
├─ color-palette-closed-loop.mjs / custom-thumbnail-closed-loop.mjs
├─ controlled-download-closed-loop.mjs
├─ thumbnail-task-closed-loop.mjs
├─ text-thumbnail-closed-loop.mjs / text-detail-closed-loop.mjs
├─ text-save-closed-loop.mjs / text-search-closed-loop.mjs
├─ office-thumbnail-closed-loop.mjs / legacy-office-thumbnail-closed-loop.mjs
├─ video-thumbnail-closed-loop.mjs / video-detail-mode-closed-loop.mjs
├─ item-workflow-closed-loop.mjs / item-api-compatibility.mjs
├─ importer-search.mjs / csv-comments-thumbnail.mjs
├─ eaglepack-duplicates.mjs / search-index-v2.mjs
├─ smart-extension-plugin.mjs / browser-extension.mjs
├─ workbench-upload.mjs / library-stats.mjs / search-media.mjs
├─ plugin-package.mjs / security.mjs
├─ import-export-migration.mjs / library-migration.mjs
├─ library-transaction-faults.mjs / library-crash-recovery.mjs
├─ library-concurrency.mjs / library-consistency-scan.mjs
├─ library-backup-restore.mjs / library-repair-plan.mjs
├─ preview-delivery-closed-loop.mjs / export-progress-closed-loop.mjs
├─ electron-smoke.cjs / electron-library-bridge.mjs
├─ browser-capture-*.mjs / browser-capture-electron-extension-e2e.mjs
├─ workbench-interactions.mjs / roadmap-panels.mjs
├─ full-regression-isolated.mjs / screenshot-regression.mjs
└─ 其他闭环/回归测试
```

## docs 与 scripts

```text
docs/
├─ REUSE_ROADMAP.md                    # 原版资源复用路线
├─ BUILD_AUDIT_REPORT_2026-08-04.md    # 深度构建审查报告
└─ import-drag-review.md               # 拖放导入审查与修复记录

scripts/
├─ clear-ports.mjs
└─ health-check.mjs
```

## 运行时与产物目录

| 目录 | 用途 |
| --- | --- |
| `dist/frontend/` | Vite 构建产物 |
| `screenshots/` | 截图回归产物 |
| `test-run/` | 测试运行 user-data、临时资源库、上传文件、插件目录 |
| `tests-tmp/` | 测试临时日志与截图 |
| `.edge-cdp-profile/` | 浏览器捕获测试 CDP profile |
| 根目录 `*.log` | 本地开发服务日志 |

## 常用命令

| 命令 | 说明 |
| --- | --- |
| `npm run dev` | 启动 Vite 预览，默认 `http://localhost:5176` |
| `npm run backend` | 启动后端 API/缩略图/扩展服务 |
| `npm run electron` | 启动 Electron 桌面版 |
| `npm run dev:all` | 同时启动 Vite 和 backend |
| `npm run dev:electron` | 同时启动 Vite、backend 和 Electron |
| `npm run build` | 构建到 `dist/frontend` |
| `npm run preview` | 预览构建产物 |
| `npm run health` | 检查本地服务健康状态 |
| `npm test` | 运行 API/资源库/插件/闭环回归测试 |
| `npm run test:full` | 运行完整测试链 |
| `npm run test:electron` | Electron smoke |
| `npm run test:electron-main` | Electron 主进程 smoke |
| `npm run test:electron-plugin` | Electron 插件窗口 smoke |
| `npm run test:electron-desktop` | Electron 桌面能力 smoke |
| `npm run test:screenshots` | 截图回归 |
| `npm run test:workbench` | Workbench 交互测试 |

## 端口

| 服务 | 默认端口 | 兼容原版端口 |
| --- | --- | --- |
| Vite | `5176` | - |
| API | `41695` | `41595` |
| Thumbnail | `41692` | `41592` |
| Extension | `41693` | `41593` |

## 主要文档

- [README.md](README.md)：项目状态、启动方式和脚本。
- [TASK.md](TASK.md)：分阶段任务、完成记录和验收记录。
- [docs/REUSE_ROADMAP.md](docs/REUSE_ROADMAP.md)：原版资源复用路线。
- [docs/BUILD_AUDIT_REPORT_2026-08-04.md](docs/BUILD_AUDIT_REPORT_2026-08-04.md)：深度构建审查。
- [docs/import-drag-review.md](docs/import-drag-review.md)：拖放导入修复记录。

## 扫描统计

以下统计不包含根目录 `node_modules`、`dist`、`test-run`、`screenshots`、`tests-tmp` 等生成/忽略目录，也不包含所有 `node_modules`（含 `src/node_modules`）：

| 目录 | 文件数 | 子目录数 | 大小 |
| --- | ---: | ---: | ---: |
| `backend` | 39 | 1 | 0.46 MB |
| `frontend` | 104 | 42 | 4.13 MB |
| `electron` | 9 | 2 | 3.18 MB |
| `docs` | 3 | 0 | 0.04 MB |
| `scripts` | 2 | 0 | 约 0 MB |
| `tests` | 69 | 7 | 0.40 MB |
| `src` | 2685 | 238 | 54.01 MB |
