import { NativeInstanceManager } from './NativeInstanceManager';
import { NativeRenderManager } from './NativeRenderManager';
import { VideoEventManager } from '../VideoEventManager';
import { AdaptiveFpsController } from '../AdaptiveFpsController';
import { MediaState } from '../../../core/StateStore';

/**
 * HDR 資訊
 */
export interface HdrInfo {
    isHdr: boolean;
    primaries: string;
    transfer: string;
    maxLuminance: number;
    hdrModeEnabled: boolean;
}
/**
 * 完整元數據
 */
export interface FullMetadata {
    duration: number;
    width: number;
    height: number;
    rotation: number;
    format: string;
    videoCodec: string;
    audioCodec: string;
    bitrate: number;
    fps: number;
    isHdr: boolean;
    hdrPrimaries: string;
    hdrTransfer: string;
    hdrMaxLuminance: number;
    sampleRate: number;
    channels: number;
}
/**
 * NativeMediaFacade 配置
 */
export interface NativeMediaFacadeConfig {
    /** 實例 ID */
    instanceId: string;
    instanceManager: NativeInstanceManager;
    renderManager: NativeRenderManager;
    eventManager: VideoEventManager;
    fpsController: AdaptiveFpsController;
    mediaState: MediaState;
    /** 檢查元素是否連接到 DOM */
    isConnected: () => boolean;
    /** 獲取 loop 屬性 */
    getLoop: () => boolean;
    /** 獲取 autoplay 屬性 */
    getAutoplay: () => boolean;
    /** 獲取 defaultMuted 屬性 */
    getDefaultMuted: () => boolean;
    /** 獲取 muted 狀態 */
    getMuted: () => boolean;
    /** 設置 muted 狀態 */
    setMuted: (value: boolean) => void;
    /** Canvas 元素 */
    canvas: HTMLCanvasElement;
    /** WebGL 渲染器 */
    renderer: {
        resetState: () => void;
    };
}
/**
 * 視頻元數據
 */
export interface VideoMetadata {
    fps: number;
    width: number;
    height: number;
    isHdr: boolean;
    hdrPrimaries: string;
    hdrTransfer: string;
    hdrMaxLuminance: number;
    videoCodec?: string;
    audioCodec?: string;
    format?: string;
    bitrate?: number;
    rotation?: number;
    sampleRate?: number;
    channels?: number;
}
/**
 * NativeMediaFacade - 直連模式封裝
 */
export declare class NativeMediaFacade {
    private config;
    private _currentTime;
    private _duration;
    private _paused;
    private _volume;
    private _playbackRate;
    private _defaultPlaybackRate;
    private _readyState;
    private _networkState;
    private _seeking;
    private _currentSrc;
    private _videoWidth;
    private _videoHeight;
    private _sourceWidth;
    private _sourceHeight;
    private _fps;
    private _videoCodec;
    private _audioCodec;
    private _containerFormat;
    private _bitrate;
    private _rotation;
    private _sampleRate;
    private _channels;
    private _isHdr;
    private _hdrPrimaries;
    private _hdrTransfer;
    private _hdrMaxLuminance;
    private _lastSeekTime;
    private _scrubbing;
    private lastVolume;
    private syncInterval;
    private skipNextStateSync;
    private currentLoadId;
    constructor(config: NativeMediaFacadeConfig);
    get currentTime(): number;
    get duration(): number;
    get paused(): boolean;
    get volume(): number;
    get playbackRate(): number;
    get defaultPlaybackRate(): number;
    get readyState(): number;
    get networkState(): number;
    get seeking(): boolean;
    get currentSrc(): string;
    get videoWidth(): number;
    get videoHeight(): number;
    get sourceWidth(): number;
    get sourceHeight(): number;
    get fps(): number;
    get videoCodec(): string;
    get audioCodec(): string;
    get containerFormat(): string;
    get bitrate(): number;
    get rotation(): number;
    get sampleRate(): number;
    get channels(): number;
    get isHdr(): boolean;
    get hdrPrimaries(): string;
    get hdrTransfer(): string;
    get hdrMaxLuminance(): number;
    get scrubbing(): boolean;
    get ended(): boolean;
    set currentTime(value: number);
    set volume(value: number);
    set playbackRate(value: number);
    set defaultPlaybackRate(value: number);
    play(): Promise<void>;
    pause(): void;
    toggle(): void;
    /**
     * 前進 n 幀
     */
    nextFrame(n?: number): void;
    /**
     * 後退 n 幀
     */
    prevFrame(n?: number): void;
    /**
     * 設置 scrubbing 模式
     */
    setScrubbing(enabled: boolean): void;
    /**
     * 載入視頻
     */
    load(src: string, lifecycleSignal?: AbortSignal): Promise<boolean>;
    /**
     * 中止載入
     */
    abort(): void;
    private seekTo;
    private resetState;
    private applyMetadata;
    startStateSync(): void;
    stopStateSync(): void;
    private syncStateToSignals;
    setMuted(value: boolean): void;
    getLastVolume(): number;
    setLastVolume(value: number): void;
    updateVideoSize(width: number, height: number): void;
    incrementLoadId(): void;
    getCurrentLoadId(): number;
    getPlaybackState(): {
        paused: boolean;
        seeking: boolean;
        scrubbing: boolean;
    };
    /**
     * 獲取完整視頻元數據
     */
    getMetadata(): Promise<FullMetadata | null>;
    /**
     * 獲取 HDR 資訊
     */
    getHdrInfo(): Promise<HdrInfo | null>;
    /**
     * 設置 HDR 模式
     */
    setHdrMode(enabled: boolean): Promise<boolean>;
}
//# sourceMappingURL=NativeMediaFacade.d.ts.map