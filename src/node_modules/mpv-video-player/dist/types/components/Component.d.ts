import { MpvVideoElement } from '../renderer/ui/MpvVideoElement';
import { MediaState, MediaStateKey } from '../core/types/state';
import { MediaEventMap, MediaEventType } from '../core/types/events';

/**
 * 組件配置選項
 */
export interface ComponentOptions {
    /** CSS 類名 */
    className?: string;
    /** 標題（tooltip） */
    title?: string;
    /** 是否可見，默認 true */
    visible?: boolean;
    /** 額外的 CSS 樣式 */
    style?: Partial<CSSStyleDeclaration>;
    /** 額外配置 */
    [key: string]: unknown;
}
/**
 * 所有 UI 組件的抽象基類
 *
 * 提供：
 * - DOM 元素創建和管理
 * - 播放器狀態訂閱
 * - 事件監聽和自動清理
 * - 生命週期管理
 */
export declare abstract class Component {
    /** 組件名稱（用於註冊和識別） */
    static componentName: string;
    /** 播放器實例引用 */
    protected player: MpvVideoElement;
    /** 根 DOM 元素 */
    protected el: HTMLElement;
    /** 組件配置 */
    protected options: ComponentOptions;
    /** 清理函數列表（事件監聽、狀態訂閱等） */
    private cleanups;
    /** 是否已銷毀 */
    private destroyed;
    constructor(player: MpvVideoElement, options?: ComponentOptions);
    /**
     * 子類必須實現：創建根 DOM 元素
     */
    protected abstract createEl(): HTMLElement;
    /**
     * 獲取根元素
     */
    getEl(): HTMLElement;
    /**
     * 獲取組件名稱
     */
    getName(): string;
    /**
     * 訂閱播放器狀態變化
     * 自動在組件銷毀時取消訂閱
     *
     * @param key - 狀態鍵
     * @param callback - 狀態變化回調
     * @param immediate - 是否立即執行一次
     */
    protected subscribeState<K extends MediaStateKey>(key: K, callback: (value: MediaState[K] extends {
        get(): infer V;
    } ? V : never) => void, _immediate?: boolean): void;
    /**
     * 監聽播放器事件
     * 自動在組件銷毀時取消監聽
     *
     * @param event - 事件名稱
     * @param handler - 事件處理器
     */
    protected onPlayerEvent<K extends MediaEventType>(event: K, handler: (data: MediaEventMap[K]) => void): void;
    /**
     * 監聽 DOM 事件
     * 自動在組件銷毀時取消監聽
     *
     * @param target - 事件目標
     * @param event - 事件名稱
     * @param handler - 事件處理器
     * @param options - 事件選項
     */
    protected listen<K extends keyof HTMLElementEventMap>(target: HTMLElement | Window | Document, event: K, handler: (e: HTMLElementEventMap[K]) => void, options?: AddEventListenerOptions): void;
    /**
     * 監聯自身元素的 DOM 事件
     */
    protected on<K extends keyof HTMLElementEventMap>(event: K, handler: (e: HTMLElementEventMap[K]) => void, options?: AddEventListenerOptions): void;
    /**
     * 顯示組件
     */
    show(): void;
    /**
     * 隱藏組件
     */
    hide(): void;
    /**
     * 切換顯示/隱藏
     */
    toggleVisibility(): void;
    /**
     * 是否可見
     */
    isVisible(): boolean;
    /**
     * 啟用組件
     */
    enable(): void;
    /**
     * 禁用組件
     */
    disable(): void;
    /**
     * 是否已禁用
     */
    isDisabled(): boolean;
    /**
     * 添加 CSS 類
     */
    addClass(...classNames: string[]): void;
    /**
     * 移除 CSS 類
     */
    removeClass(...classNames: string[]): void;
    /**
     * 切換 CSS 類
     */
    toggleClass(className: string, force?: boolean): boolean;
    /**
     * 檢查是否包含 CSS 類
     */
    hasClass(className: string): boolean;
    /**
     * 設置 setTimeout，自動在銷毀時清理
     */
    protected setTimeout(callback: () => void, delay: number): number;
    /**
     * 設置 setInterval，自動在銷毀時清理
     */
    protected setInterval(callback: () => void, interval: number): number;
    /**
     * 設置 requestAnimationFrame，自動在銷毀時清理
     */
    protected requestAnimationFrame(callback: FrameRequestCallback): number;
    /**
     * 銷毀組件
     * 清理所有資源、事件監聽和 DOM 元素
     */
    destroy(): void;
    /**
     * 是否已銷毀
     */
    isDestroyed(): boolean;
}
//# sourceMappingURL=Component.d.ts.map