import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * HDR 資訊
 */
export interface HdrInfo {
    /** 色彩原色（如 bt.2020） */
    primaries: string;
    /** 傳輸特性/gamma（如 pq, hlg） */
    gamma: string;
    /** 信號峰值亮度 */
    sigPeak: number;
    /** 是否為 HDR 內容 */
    isHdr: boolean;
}
/**
 * HDR 插件配置
 */
export interface HdrPluginOptions {
    /** 是否自動偵測並啟用 HDR */
    autoEnable?: boolean;
    /** HDR 模式變更回調 */
    onHdrChange?: (info: HdrInfo) => void;
}
/**
 * HDR 插件定義
 */
export declare const HdrPlugin: MpvPlugin<HdrPluginOptions>;
/**
 * HDR 管理器
 */
export declare class HdrManager implements PluginInstance {
    readonly name = "hdr";
    private player;
    private options;
    private _hdrInfo;
    private _hdrModeEnabled;
    private _enabled;
    private loadedmetadataHandler;
    constructor(player: MpvVideoElement, options: Required<Omit<HdrPluginOptions, 'onHdrChange'>> & Pick<HdrPluginOptions, 'onHdrChange'>);
    private initialize;
    private detectHdr;
    private checkIsHdrContent;
    /** 獲取當前 HDR 資訊 */
    get hdrInfo(): HdrInfo;
    /** 是否為 HDR 內容 */
    get isHdrContent(): boolean;
    /** HDR 模式是否已啟用 */
    get hdrModeEnabled(): boolean;
    /** 啟用 HDR 模式 */
    enableHdrMode(): Promise<void>;
    /** 停用 HDR 模式 */
    disableHdrMode(): Promise<void>;
    /** 切換 HDR 模式 */
    toggleHdrMode(): Promise<void>;
    /** 重新偵測 HDR */
    refresh(): Promise<void>;
    enable(): void;
    disable(): void;
    get enabled(): boolean;
    destroy(): void;
}
//# sourceMappingURL=HdrPlugin.d.ts.map