"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const styles = `/**\r
 * Note Plugin Styles\r
 */\r
\r
/* ===== 工具列按鈕 ===== */\r
\r
.vp-notes-btn {\r
    display: flex;\r
    align-items: center;\r
    justify-content: center;\r
    width: 32px;\r
    height: 32px;\r
    padding: 0;\r
    color: var(--vp-text-color, #2c2f32);\r
    transition: opacity var(--vp-transition-duration, 0.15s);\r
}\r
\r
.vp-notes-btn:hover {\r
    opacity: 0.8;\r
}\r
\r
.vp-notes-btn.disabled {\r
    opacity: 0.3;\r
    pointer-events: none;\r
}\r
\r
.vp-notes-btn svg {\r
    pointer-events: none;\r
}\r
\r
/* ===== 對話框 ===== */\r
\r
.mpv-notes-dialog {\r
    position: absolute;\r
    top: 0;\r
    left: 0;\r
    right: 0;\r
    bottom: 0;\r
    display: flex;\r
    align-items: center;\r
    justify-content: center;\r
    background: rgba(0, 0, 0, 0.6);\r
    z-index: 10000;\r
    backdrop-filter: blur(4px);\r
    animation: mpv-notes-fade-in 0.15s ease-out;\r
}\r
\r
.mpv-notes-dialog.hidden {\r
    display: none;\r
}\r
\r
@keyframes mpv-notes-fade-in {\r
    from {\r
        opacity: 0;\r
    }\r
\r
    to {\r
        opacity: 1;\r
    }\r
}\r
\r
.mpv-notes-dialog-content {\r
    width: 90%;\r
    max-width: 400px;\r
    background: rgba(255, 255, 255, 0.95);\r
    border-radius: 12px;\r
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);\r
    overflow: hidden;\r
    animation: mpv-notes-slide-up 0.2s ease-out;\r
}\r
\r
@keyframes mpv-notes-slide-up {\r
    from {\r
        opacity: 0;\r
        transform: translateY(20px);\r
    }\r
\r
    to {\r
        opacity: 1;\r
        transform: translateY(0);\r
    }\r
}\r
\r
.mpv-notes-dialog-header {\r
    display: flex;\r
    align-items: center;\r
    justify-content: space-between;\r
    padding: 16px 20px;\r
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);\r
}\r
\r
.mpv-notes-dialog-title {\r
    font-size: 16px;\r
    font-weight: 600;\r
    color: #1a1a1a;\r
}\r
\r
.mpv-notes-dialog-time {\r
    font-size: 13px;\r
    color: #666;\r
    font-family: 'Monaco', 'Menlo', monospace;\r
    background: rgba(0, 0, 0, 0.06);\r
    padding: 4px 8px;\r
    border-radius: 4px;\r
}\r
\r
.mpv-notes-textarea {\r
    width: 100%;\r
    min-height: 120px;\r
    padding: 16px 20px;\r
    border: none;\r
    outline: none;\r
    font-size: 14px;\r
    line-height: 1.6;\r
    resize: vertical;\r
    font-family: inherit;\r
    color: #333;\r
    background: transparent;\r
}\r
\r
.mpv-notes-textarea::placeholder {\r
    color: #999;\r
}\r
\r
.mpv-notes-dialog-footer {\r
    display: flex;\r
    align-items: center;\r
    justify-content: space-between;\r
    padding: 12px 20px;\r
    border-top: 1px solid rgba(0, 0, 0, 0.1);\r
    background: rgba(0, 0, 0, 0.02);\r
}\r
\r
.mpv-notes-char-count {\r
    font-size: 12px;\r
    color: #999;\r
}\r
\r
.mpv-notes-dialog-actions {\r
    display: flex;\r
    gap: 8px;\r
}\r
\r
.mpv-notes-btn-cancel,\r
.mpv-notes-btn-save {\r
    padding: 8px 16px;\r
    border: none;\r
    border-radius: 6px;\r
    font-size: 13px;\r
    font-weight: 500;\r
    cursor: pointer;\r
    transition: all 0.15s ease;\r
}\r
\r
.mpv-notes-btn-cancel {\r
    background: transparent;\r
    color: #666;\r
}\r
\r
.mpv-notes-btn-cancel:hover {\r
    background: rgba(0, 0, 0, 0.06);\r
}\r
\r
.mpv-notes-btn-save {\r
    background: var(--vp-primary-color, #007AFF);\r
    color: white;\r
}\r
\r
.mpv-notes-btn-save:hover {\r
    filter: brightness(1.1);\r
}\r
\r
/* ===== 進度條標記 ===== */\r
\r
.mpv-notes-markers {\r
    position: absolute;\r
    top: 0;\r
    left: 0;\r
    right: 0;\r
    bottom: 0;\r
    pointer-events: none;\r
}\r
\r
.mpv-notes-marker {\r
    position: absolute;\r
    top: 50%;\r
    width: 8px;\r
    height: 8px;\r
    background: #FFD60A;\r
    border-radius: 50%;\r
    transform: translate(-50%, -50%);\r
    cursor: pointer;\r
    pointer-events: auto;\r
    transition: transform var(--vp-transition-duration, 0.15s),\r
                box-shadow var(--vp-transition-duration, 0.15s);\r
    box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.3);\r
    z-index: 10;\r
}\r
\r
.mpv-notes-marker:hover {\r
    transform: translate(-50%, -50%) scale(1.4);\r
    box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.4),\r
                0 2px 8px rgba(0, 0, 0, 0.3);\r
}\r
\r
/* ===== Marker Tooltip ===== */\r
\r
.mpv-notes-marker-tooltip {\r
    position: absolute;\r
    bottom: 100%;\r
    left: 50%;\r
    transform: translateX(-50%);\r
    margin-bottom: 8px;\r
    padding: 8px 12px;\r
    min-width: 120px;\r
    max-width: 250px;\r
    width: max-content;\r
    background: rgba(0, 0, 0, 0.85);\r
    color: #fff;\r
    font-size: 12px;\r
    line-height: 1.4;\r
    border-radius: 6px;\r
    text-align: left;\r
    opacity: 0;\r
    visibility: hidden;\r
    transition: opacity 0.15s, visibility 0.15s;\r
    pointer-events: none;\r
}\r
\r
.mpv-notes-marker:hover .mpv-notes-marker-tooltip {\r
    opacity: 1;\r
    visibility: visible;\r
}\r
\r
/* ===== Dark Theme Support ===== */\r
\r
:host([data-theme="dark"]) .mpv-notes-dialog-content,\r
.vp-container[data-theme="dark"] .mpv-notes-dialog-content {\r
    background: rgba(40, 40, 40, 0.98);\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-dialog-title,\r
.vp-container[data-theme="dark"] .mpv-notes-dialog-title {\r
    color: #fff;\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-dialog-time,\r
.vp-container[data-theme="dark"] .mpv-notes-dialog-time {\r
    color: #aaa;\r
    background: rgba(255, 255, 255, 0.1);\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-textarea,\r
.vp-container[data-theme="dark"] .mpv-notes-textarea {\r
    color: #fff;\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-textarea::placeholder,\r
.vp-container[data-theme="dark"] .mpv-notes-textarea::placeholder {\r
    color: #666;\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-dialog-footer,\r
.vp-container[data-theme="dark"] .mpv-notes-dialog-footer {\r
    background: rgba(255, 255, 255, 0.03);\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-btn-cancel,\r
.vp-container[data-theme="dark"] .mpv-notes-btn-cancel {\r
    color: #aaa;\r
}\r
\r
:host([data-theme="dark"]) .mpv-notes-btn-cancel:hover,\r
.vp-container[data-theme="dark"] .mpv-notes-btn-cancel:hover {\r
    background: rgba(255, 255, 255, 0.1);\r
}\r
`;
const NotePlugin = {
  name: "notes",
  version: "1.0.0",
  dependencies: ["progress"],
  styles,
  install(player, options = {}) {
    return new NoteManager(player, {
      order: options.order ?? 75,
      maxLength: options.maxLength ?? 500
    });
  }
};
class NoteManager {
  constructor(player, options) {
    this.name = "notes";
    this.notes = [];
    this.toggleBtn = null;
    this.dialogEl = null;
    this.markersContainer = null;
    this._enabled = true;
    this._dialogVisible = false;
    this.eventCleanups = [];
    this.player = player;
    this.options = options;
    this.initialize();
  }
  initialize() {
    const shadowRoot = this.player.shadowRoot;
    if (!shadowRoot)
      return;
    this.createToggleButton(shadowRoot);
    this.createDialog(shadowRoot);
    this.createMarkersContainer(shadowRoot);
    this.player.addEventListener("durationchange", () => {
      this.renderMarkers();
    });
  }
  /**
   * 創建工具列按鈕
   */
  createToggleButton(shadowRoot) {
    const container = shadowRoot.querySelector(".vp-controls-right");
    if (!container)
      return;
    this.toggleBtn = document.createElement("button");
    this.toggleBtn.className = "vp-btn vp-notes-btn";
    this.toggleBtn.title = "新增筆記 (N)";
    this.toggleBtn.dataset.order = String(this.options.order);
    this.toggleBtn.innerHTML = `
            <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor">
                <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V5h14v14z"/>
                <path d="M7 7h10v2H7zM7 11h10v2H7zM7 15h7v2H7z"/>
            </svg>
        `;
    this.addEvent(this.toggleBtn, "click", (e) => {
      e.stopPropagation();
      this.showDialog();
    });
    this.insertByOrder(container, this.toggleBtn);
  }
  /**
   * 創建筆記輸入對話框
   */
  createDialog(shadowRoot) {
    this.dialogEl = document.createElement("div");
    this.dialogEl.className = "mpv-notes-dialog hidden";
    this.dialogEl.innerHTML = `
            <div class="mpv-notes-dialog-content">
                <div class="mpv-notes-dialog-header">
                    <span class="mpv-notes-dialog-title">新增筆記</span>
                    <span class="mpv-notes-dialog-time"></span>
                </div>
                <textarea
                    class="mpv-notes-textarea"
                    placeholder="在此輸入筆記內容..."
                    maxlength="${this.options.maxLength}"
                ></textarea>
                <div class="mpv-notes-dialog-footer">
                    <span class="mpv-notes-char-count">0 / ${this.options.maxLength}</span>
                    <div class="mpv-notes-dialog-actions">
                        <button class="mpv-notes-btn-cancel">取消</button>
                        <button class="mpv-notes-btn-save">儲存</button>
                    </div>
                </div>
            </div>
        `;
    const textarea = this.dialogEl.querySelector(".mpv-notes-textarea");
    const charCount = this.dialogEl.querySelector(".mpv-notes-char-count");
    const cancelBtn = this.dialogEl.querySelector(".mpv-notes-btn-cancel");
    const saveBtn = this.dialogEl.querySelector(".mpv-notes-btn-save");
    this.addEvent(textarea, "input", () => {
      charCount.textContent = `${textarea.value.length} / ${this.options.maxLength}`;
    });
    this.addEvent(cancelBtn, "click", () => {
      this.hideDialog();
    });
    this.addEvent(saveBtn, "click", () => {
      this.saveNote(textarea.value);
    });
    this.addEvent(this.dialogEl, "click", (e) => {
      if (e.target === this.dialogEl) {
        this.hideDialog();
      }
    });
    shadowRoot.appendChild(this.dialogEl);
  }
  /**
   * 創建進度條標記容器
   */
  createMarkersContainer(shadowRoot) {
    const progressBar = shadowRoot.querySelector(".mpv-progress-bar, .vp-progress-bar");
    if (!progressBar)
      return;
    this.markersContainer = document.createElement("div");
    this.markersContainer.className = "mpv-notes-markers";
    progressBar.appendChild(this.markersContainer);
  }
  /**
   * 顯示對話框
   */
  showDialog() {
    if (!this.dialogEl)
      return;
    const wasPlaying = !this.player.paused;
    if (wasPlaying) {
      this.player.pause();
    }
    const timeEl = this.dialogEl.querySelector(".mpv-notes-dialog-time");
    if (timeEl) {
      timeEl.textContent = this.formatTime(this.player.currentTime);
    }
    const textarea = this.dialogEl.querySelector(".mpv-notes-textarea");
    const charCount = this.dialogEl.querySelector(".mpv-notes-char-count");
    if (textarea) {
      textarea.value = "";
      textarea.focus();
    }
    if (charCount) {
      charCount.textContent = `0 / ${this.options.maxLength}`;
    }
    this.dialogEl.classList.remove("hidden");
    this._dialogVisible = true;
  }
  /**
   * 隱藏對話框
   */
  hideDialog() {
    if (!this.dialogEl)
      return;
    this.dialogEl.classList.add("hidden");
    this._dialogVisible = false;
  }
  /**
   * 儲存筆記
   */
  saveNote(content) {
    const trimmedContent = content.trim();
    if (!trimmedContent) {
      this.hideDialog();
      return;
    }
    const note = {
      content: trimmedContent,
      videoTime: this.player.currentTime,
      createdAt: /* @__PURE__ */ new Date()
    };
    this.notes.push(note);
    this.renderMarkers();
    this.hideDialog();
    console.log("[NotePlugin] Note saved:", note);
  }
  /**
   * 渲染進度條標記
   */
  renderMarkers() {
    if (!this.markersContainer)
      return;
    this.markersContainer.innerHTML = "";
    const duration = this.player.duration;
    if (!duration)
      return;
    for (let i = 0; i < this.notes.length; i++) {
      const note = this.notes[i];
      const marker = document.createElement("div");
      marker.className = "mpv-notes-marker";
      marker.dataset.noteIndex = String(i);
      const percent = note.videoTime / duration * 100;
      marker.style.left = `${percent}%`;
      if (note.color) {
        marker.style.background = note.color;
      }
      const tooltip = document.createElement("div");
      tooltip.className = "mpv-notes-marker-tooltip";
      tooltip.textContent = note.content;
      marker.appendChild(tooltip);
      this.addEvent(marker, "mouseenter", (e) => {
        e.stopPropagation();
        this.player.tooltipEnabled = false;
      });
      this.addEvent(marker, "mouseleave", () => {
        this.player.tooltipEnabled = true;
      });
      this.addEvent(marker, "click", (e) => {
        e.stopPropagation();
        this.player.currentTime = note.videoTime;
      });
      this.markersContainer.appendChild(marker);
    }
  }
  // ===== 工具方法 =====
  formatTime(seconds) {
    if (!isFinite(seconds))
      return "--:--";
    const mins = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    return `${mins}:${secs.toString().padStart(2, "0")}`;
  }
  insertByOrder(container, element) {
    const order = parseInt(element.dataset.order || "999", 10);
    const children = Array.from(container.children);
    const insertBefore = children.find((child) => {
      const childOrder = parseInt(child.dataset.order || "999", 10);
      return childOrder > order;
    });
    if (insertBefore) {
      container.insertBefore(element, insertBefore);
    } else {
      container.appendChild(element);
    }
  }
  addEvent(target, event, handler) {
    target.addEventListener(event, handler);
    this.eventCleanups.push(() => {
      target.removeEventListener(event, handler);
    });
  }
  // ===== 公開 API =====
  /** 獲取所有筆記 */
  getNotes() {
    return [...this.notes];
  }
  /** 新增筆記（程式化方式） */
  addNote(content, videoTime) {
    const note = {
      content,
      videoTime: videoTime ?? this.player.currentTime,
      createdAt: /* @__PURE__ */ new Date()
    };
    this.notes.push(note);
    this.renderMarkers();
    return note;
  }
  /** 刪除筆記 */
  removeNote(index) {
    if (index < 0 || index >= this.notes.length)
      return false;
    this.notes.splice(index, 1);
    this.renderMarkers();
    return true;
  }
  /** 清空所有筆記 */
  clearNotes() {
    this.notes = [];
    this.renderMarkers();
  }
  /** 匯出筆記（JSON 格式） */
  exportNotes() {
    return JSON.stringify(this.notes, null, 2);
  }
  /** 匯入筆記（JSON 格式） */
  importNotes(json) {
    try {
      const data = JSON.parse(json);
      if (!Array.isArray(data))
        return false;
      this.notes = data.map((item) => ({
        content: String(item.content || ""),
        videoTime: Number(item.videoTime) || 0,
        createdAt: new Date(item.createdAt || Date.now()),
        color: item.color
      }));
      this.renderMarkers();
      return true;
    } catch {
      return false;
    }
  }
  enable() {
    this._enabled = true;
    this.toggleBtn?.classList.remove("disabled");
  }
  disable() {
    this._enabled = false;
    this.toggleBtn?.classList.add("disabled");
    this.hideDialog();
  }
  get enabled() {
    return this._enabled;
  }
  destroy() {
    for (const cleanup of this.eventCleanups) {
      cleanup();
    }
    this.eventCleanups = [];
    this.toggleBtn?.remove();
    this.dialogEl?.remove();
    this.markersContainer?.remove();
  }
}
exports.NoteManager = NoteManager;
exports.NotePlugin = NotePlugin;
//# sourceMappingURL=index.cjs.map
