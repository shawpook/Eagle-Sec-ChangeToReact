# Orcabox 多格式能力迁移至 Eagle：深度审查与任务执行报告

> 审查日期：2026-08-05  
> 源项目：`H:\dev\orcabox-Sec-development`  
> 目标项目：`H:\dev\Eagle-Sec-development`  
> 本报告状态：代码深度审查与迁移方案已完成；阶段 1 文本 PNG 封面已实施并通过新增闭环测试。

## 1. 执行摘要

### 1.1 核心结论

Orcabox 的“txt、md 等文件显示前部分文字作为封面”不是一个孤立组件，而是以下能力的组合：

1. 统一扩展名分类与格式策略；
2. 受限字节读取、编码探测与降级解码；
3. Markdown 结构化行解析或纯文本排版；
4. SVG 封面生成及持久化；
5. 网格可见区按需读取的动态文本预览；
6. 详情页全文读取、编辑、保存与缓存失效；
7. Office 文件抽取、外部转换和专用预览。

Eagle 当前已有成熟的“导入失败后进入异步缩略图任务”闭环，并统一消费 `<name>_thumbnail.png`。因此，第一阶段不应移植 Orcabox 的 React 动态卡片，也不需要改变 Eagle 的条目 schema；最稳妥的做法是：

- 复用 Orcabox 的格式分类、受限读取、编码降级、Markdown 行解析和 SVG 转义思路；
- 在 Eagle 后端按现有 JavaScript 架构重写文本封面渲染；
- 输出仍为 Eagle 既有 `_thumbnail.png`，接入 `ThumbnailTaskService`；
- 前端继续把文本文件当普通缩略图展示，避免大列表逐卡读取原文件。

### 1.2 建议首批范围

第一批建议支持：

- Markdown：`md`、`markdown`
- 纯文本：`txt`、`log`、`rst`
- 结构化文本：`json`、`xml`、`yaml`、`yml`
- 表格文本：`csv`、`tsv`

暂缓：

- Office：`docx`、`xlsx`、`pptx`
- 旧 Office / WPS / OpenDocument：`doc`、`xls`、`ppt`、`wps`、`et`、`dps`、`odt`、`ods`、`odp`
- EPUB / RTF
- 文本全文详情预览和编辑
- 文本内容全文索引

原因是首批范围只需要两个小型依赖和现有 `sharp`，而 Office 会引入 ZIP 安全、文档解析器、LibreOffice/WPS 路径、超时、临时文件和平台打包等显著复杂度。

## 2. 审查范围与证据口径

本次审查以源码、测试和 Git 提交记录为证据，排除了 `node_modules`、`dist`、`out`、构建产物和 sourcemap 作为主要结论来源。

审查覆盖：

- 格式注册与扩展名分类；
- 文件导入和类型识别；
- 文本读取、编码探测、截断；
- 文本 / Markdown / Office 缩略图生成；
- 缩略图任务队列、超时、取消、失败恢复和原子提交；
- 条目元数据和磁盘缓存；
- 网格和详情预览；
- IPC / HTTP API 边界；
- 搜索索引；
- 自动化测试与近期提交。

Git 证据：

- Orcabox 多格式相关源码主要出现在 `f45b5a8`（2026-08-03，重建基线）和 `4e37c31`（2026-08-03，严格类型检查与可见资源库验收）。提交粒度较粗，不能据此证明每种格式都独立完成了回归测试。
- Eagle 缩略图任务链由 `6667aad`（2026-08-04）建立，随后 `47b8db7`、`e18b4a7`、`a2bac07` 扩展到视频、预览矩阵和直接视频导入。该链路是文本封面最合适的承载点。

## 3. Orcabox 能力拆解

### 3.1 统一格式分类

关键证据：

- `src/shared/asset-formats.ts:245-259`：文档格式集合包含 `md/markdown/rst/txt/epub/rtf/csv/tsv/xml/yaml/yml/log` 与 Office 格式。
- `src/shared/asset-formats.ts:286-299`：Markdown 和可编辑文本格式分别建表；可编辑文本还包含 `json`。
- `src/shared/asset-formats.ts:331-340`：统一将扩展名映射为 `document` 等资产 kind。
- `src/shared/asset-formats.ts:421-426`：提供 Markdown / 可编辑文本判断函数。

可迁移价值：格式能力集中管理，避免导入、缩略图、详情页各自维护不一致列表。

不能直接复制的原因：Orcabox 使用带点扩展名和 TypeScript `kind` 模型；Eagle 当前使用不带点的 `item.ext`，没有统一的资产 kind。

### 3.2 文本读取与编码

关键证据：

- `src/main/services/document-support.ts:62-73`：扩展名到 MIME 的映射。
- `src/main/services/document-support.ts:89`：文本封面最多读取前 128 KiB。
- `src/main/services/document-support.ts:94-106`：UTF-8、UTF-16、GB18030、Big5、Shift-JIS、EUC-KR、Windows-1252 等候选编码。
- `src/main/services/document-support.ts:155-194`：全文读取和受限片段读取分离；片段读取通过 `open/fstat/read/close`，不会把大文件全部载入内存。
- `src/main/services/document-support.ts:1011-1032`：先识别 BOM，再调用 `chardet`，并归一化编码名。
- `src/main/services/document-support.ts:1035-1066`：通过 `iconv-lite` 逐个尝试候选编码，用替换字符、空字符和可疑乱码比例判断解码是否合理。

可迁移价值：这部分是中文 Windows 文本正确显示的关键，不能只使用 `fs.readFile(..., 'utf8')`。

需修正的边界：Orcabox 的片段可能在多字节字符中间截断，最终会靠解码合理性和替换字符容忍度降级。Eagle 实施时应额外去掉片段尾部不完整字符，或至少确保封面不会出现尾部替换符。

### 3.3 Markdown / 文本封面生成

关键证据：

- `src/main/utils/markdown-preview.ts:3-6`：Markdown 格式、最多 11 行、最多 2400 字符、Office 封面版本号。
- `src/main/utils/markdown-preview.ts:46-104`：生成 512×512 SVG，包含格式标签、正文区和文件名。
- `src/main/utils/markdown-preview.ts:122-199`：识别标题、引用、任务列表、普通列表、代码围栏、表格和正文。
- `src/main/utils/markdown-preview.ts:201-305`：排版、按视觉宽度换行并对 CJK 字符计双宽。
- `src/main/utils/markdown-preview.ts:307-335`：空白归一化、SVG 实体转义、标题截断和空内容兜底。
- `src/main/services/preview-service.ts:6228-6268`：读取文本片段，生成 SVG，按源文件 `mtime` 判断缓存新鲜度；失败时退回占位图。

重要判断：Orcabox 的函数名虽为 `buildMarkdownThumbnailSvg`，实际也被普通文本缩略图复用。移植时应改名为 `buildTextThumbnailSvg`，Markdown 只是一种布局策略，避免在 Eagle 中继续扩大语义错位。

### 3.4 动态网格文本预览

关键证据：

- `src/renderer/components/assets/AssetThumbnail.tsx:703-712`：使用 `IntersectionObserver` 只为进入可见区的卡片请求文本。
- `src/renderer/components/assets/AssetThumbnail.tsx:727`：提前 360px 预热。
- `src/renderer/components/assets/AssetThumbnail.tsx:733-755`：最多取 50,000 字符、500 行，Markdown 再做逐行解析。
- `src/renderer/components/assets/AssetThumbnail.tsx:769-777`：悬停时卡片内容可滚动。
- `src/renderer/hooks/use-preview.ts:470-477`：React Query 缓存 30 秒。

不建议首批迁移。Eagle 当前前端是静态页面 / 原版资源兼容层，不是 Orcabox 的 React 数据层；直接复制会带入 React Query、状态管理、IPC preload 和组件体系，而且大列表会增加原文件读取和前后端请求压力。

Eagle 只需消费持久 PNG，即可自动获得网格封面显示，且沿用浏览器缓存、磁盘缓存和已有虚拟列表行为。

### 3.5 详情预览与编辑

关键证据：

- `src/main/services/preview-service.ts:2792-2816`：详情读取全文并返回内容、编码、MIME、mtime。
- `src/main/ipc-handlers.ts:3613-3622` 和 `src/main/preload.ts:141-142`：受可见资产检查保护的 IPC 读写接口。
- `src/renderer/components/preview/TextDocumentSurface.tsx:97-107`：按扩展名决定 Markdown 和可编辑文本。
- `src/renderer/components/preview/TextDocumentSurface.tsx:450-484`：纯文本的预览、编辑、分屏模式。
- `src/renderer/hooks/use-preview.ts:606-670`：保存后使文本、缩略图和资产查询失效，并带撤销 / 重做历史。

该能力不能视为“封面迁移”的附属小改动。Eagle 若后续实现，必须单独设计只读详情接口、内容长度限制、保存原子性、编码保持、冲突检测和缩略图失效机制。

### 3.6 Office 能力

关键证据：

- `src/main/services/document-support.ts:26-60`：Office / EPUB / RTF 可转换范围。
- `src/main/services/document-support.ts:75-88`：原生可读仅 `docx/xlsx/pptx`，并对 sheet、row、slide、嵌入图片、ZIP 项和解压总量设置边界。
- `src/main/services/document-support.ts:196-229`：按格式调用不同 reader / writer。
- `src/main/utils/markdown-preview.ts:106-119`、`:354` 以后：Word、表格、演示文稿分别生成专用 SVG。
- `src/main/services/preview-service.ts:6274-6288`：Office 读取失败时退回通用占位图。

依赖包括 `mammoth`、`exceljs`、`jszip`、`fast-xml-parser`、`sanitize-html`、`sharp`，旧格式还依赖 LibreOffice / OpenOffice。Office ZIP 必须保留压缩炸弹防护，不能只复制解析主路径。

## 4. Eagle 现状与准确落点

### 4.1 导入与条目模型

关键证据：

- `backend/src/importer.js:43-84`：仅读取前 4096 字节做少量 magic 检测；其他格式依赖扩展名或 MIME hint。
- `backend/src/importer.js:87-138`：条目已有 `ext`、`mime`、`size`、`width`、`height` 等字段。
- `backend/src/importer.js:140-159`：复制原文件后先尝试同步缩略图；失败设 `noThumbnail`，若异步服务支持该扩展名则入队。
- `backend/src/library-store.js:202-207`：原文件与缩略图路径契约固定为 `<name>.<ext>` 和 `<name>_thumbnail.png`。

结论：首批文本封面无需新增条目字段。可选诊断字段如 `textEncoding`、`thumbnailVersion` 不应在第一阶段强行持久化，否则会扩大兼容面。

需注意：`safeExt` 当前只允许 1-10 位字母数字，首批格式均满足；未来更复杂后缀或无扩展名文本不在本次范围内。

### 4.2 当前同步缩略图器

关键证据：

- `backend/src/thumbnailer.js:56-70`：同步生成仅支持 PNG / JPEG。
- `backend/src/thumbnailer.js:73-89`：异步包装支持若干 Sharp 图像格式，失败再回退同步实现。

结论：不要把文本逻辑堆进旧的 `generateThumbnail()` 同步分支。文本封面应进入统一任务服务，避免导入大批文件时阻塞主循环，也能获得超时、取消、恢复和原子替换。

### 4.3 统一缩略图任务服务

关键证据：

- `backend/src/thumbnail-task-service.js:15-23`：并发、超时、源文件上限和支持格式集合。
- `backend/src/thumbnail-task-service.js:86-125`：先写 pending，再备份旧图、原子替换、保存条目；失败时恢复旧图与条目快照。
- `backend/src/thumbnail-task-service.js:248-318`：队列、同条目冲突保护、任务状态持久化。
- `backend/src/thumbnail-task-service.js:351-375`：统一 `supports()` 与中断任务恢复。
- `backend/src/thumbnail-task-service.js:390-464`：执行、超时、调色板分析、提交和按格式路由。
- `backend/src/server.js:126-155`：服务启动和切库时恢复中断任务。
- `backend/src/server.js:1761-1787`、`:2304-2328`：v1/v2 启动、查询、取消 API 已存在。

准确落点：

1. 新建 `backend/src/file-format-policy.js`；
2. 新建 `backend/src/text-document-support.js`；
3. 新建 `backend/src/text-thumbnail-renderer.js`；
4. 在 `thumbnail-task-service.js` 中加入文本格式集合与 `renderTextDocument()` 路由；
5. 保持 pending 文件最终是 PNG；
6. `importer.js` 不需新增分支，只依赖扩展后的 `supports()` 自动入队。

### 4.4 前端、缓存与一致性

关键证据：

- `backend/src/library-consistency-service.js:160-177`：一致性扫描明确检查 `<name>_thumbnail.png`；文本封面沿用 PNG 后无需改扫描规则。
- `backend/src/library-store.js:97-112` 和 `backend/src/search-service.js:232-243`：搜索索引和关键词搜索只覆盖名称、注释、URL、标签、评论和文件夹，不覆盖原文件文本。
- 前端目前通过统一缩略图 URL / 本地路径兼容层读取图片；无需识别 txt 或 md 的特殊 DOM 封面。

结论：首批只生成 PNG，不做全文索引。若后续把文本内容加入搜索，必须升级 `search-index.json` 版本、限定索引字符数，并处理内容变更后的增量失效。

## 5. 差距矩阵

| 能力 | Orcabox | Eagle 当前 | 首批策略 |
|---|---|---|---|
| 格式注册 | 集中式扩展名 / kind | 多处分散 Set 与条件 | 新增共享格式策略模块 |
| 文本编码 | chardet + iconv-lite + 降级 | 默认 UTF-8 / 无文本读取 | 移植算法，新增两个依赖 |
| 读取上限 | 封面 128 KiB | 无 | 64-128 KiB，可配置，默认 128 KiB |
| Markdown 结构 | 标题、列表、任务、引用、代码、表格 | 无 | 重写轻量解析，不引入完整 Markdown DOM |
| 封面产物 | SVG 缓存，部分链路可栅格化 | 固定 PNG | SVG 内存生成后由 Sharp 输出 PNG |
| 网格动态文本 | 可见区按需全文片段 | 无 React 数据层 | 不迁移，统一 PNG |
| 详情预览 / 编辑 | 完整 | 有旧 text-editor 资源但缺少后端闭环 | 第二阶段单独设计 |
| Office | docx/xlsx/pptx reader + LibreOffice | 无 | 第三阶段，先做安全评审 |
| 任务队列 | 有优先级和有界队列 | 有并发、超时、取消、恢复 | 复用 Eagle 队列，补有界长度为后续优化 |
| 缓存失效 | 依赖 mtime / 查询失效 | 刷新任务显式重建 | 首批由导入 / 刷新触发；后续补源 mtime 校验 |
| 全文搜索 | 非本次重点 | 无内容索引 | 不纳入首批 |

## 6. 推荐实现设计

### 6.1 模块职责

`file-format-policy.js`

- 导出 `TEXT_THUMBNAIL_EXTENSIONS`、`MARKDOWN_EXTENSIONS`、`STRUCTURED_TEXT_EXTENSIONS`；
- 统一 `normalizeExtension()`、`isTextThumbnailExtension()`、`resolveTextMimeType()`；
- 不引入 UI kind，保持 Eagle 简单模型。

`text-document-support.js`

- `readTextSnippet(filePath, { maxBytes })`；
- 使用文件描述符只读开头；
- BOM 优先，`chardet` 次之；
- `iconv-lite` 候选解码；
- 检查 NUL / 控制字符 / 替换字符比例，二进制伪装时抛 `TEXT_SOURCE_BINARY`；
- 统一换行，不修改原文件；
- 返回 `{ content, encoding, truncated, bytesRead }`。

`text-thumbnail-renderer.js`

- `renderTextThumbnail({ source, output, item, maxSize })`；
- 将输入解析为统一的 render lines；
- Markdown 使用结构化样式；JSON / XML / YAML 使用等宽字体；CSV / TSV 使用简化表格或列分隔；普通文本使用段落；
- 所有动态文本先 XML 转义；
- 生成 512×512 或与 `maxSize` 对齐的 SVG Buffer；
- 使用现有 `sharp(Buffer.from(svg)).png().toFile(output)`；
- 返回封面尺寸，不把 512×512 误写成原文件尺寸。

`thumbnail-task-service.js`

- 将文本集合加入 `supports()`；
- `#render()` 中优先路由文本；
- 文本源文件不能沿用通用 100 MB 全文件语义：允许较大文件，但只读取 128 KiB，并设置一个合理文件元数据上限或跳过总大小限制；
- 对文本封面不要运行调色板分析，或使用轻量固定调色板。当前 `analyze(pending)` 会增加无意义工作；建议让 renderer 返回 `skipPaletteAnalysis: true`，提交固定中性色调色板。

### 6.2 封面视觉原则

- 输出 PNG 透明与否不影响现有契约；建议浅色纸张背景，适配 Eagle 当前界面而不是复制 Orcabox 的深色品牌样式。
- 左上角显示格式标签，主体显示内容，底部显示文件名。
- 中文、日文、韩文按视觉双宽折行；字体使用系统字体栈，不打包字体。
- 空文件显示“空文本文件”与格式标签，不应作为失败任务。
- 不渲染 HTML，不执行 Markdown 中的 HTML、脚本、图片、链接或远程资源。
- 封面只表达文本，不展示敏感文件完整内容；正文上限 8-12 行。

### 6.3 错误码建议

- `TEXT_SOURCE_BINARY`：扩展名为文本但内容明显为二进制；
- `TEXT_DECODE_FAILED`：所有候选编码均不合理；
- `TEXT_THUMBNAIL_RENDER_FAILED`：SVG / Sharp 栅格化失败；
- 继续复用 `EMPTY_ITEM_SOURCE`、`ORIGINAL_FILE_NOT_FOUND`、`THUMBNAIL_TIMEOUT`、`THUMBNAIL_CANCELLED`。

## 7. 分阶段任务执行表

### 阶段 0：审查与方案（已完成）

- [x] 定位 Orcabox 格式分类、文本读取、编码、封面、动态网格、详情预览和 Office 链路。
- [x] 定位 Eagle 导入、任务队列、磁盘命名、前端消费、一致性扫描和测试链路。
- [x] 对比 Git 提交历史，确认 Eagle 统一缩略图任务是迁移主落点。
- [x] 确认首批无需修改条目 schema 与前端网格渲染。

### 阶段 1：文本封面 MVP（待执行，最高优先级）

- [x] 新增 `backend/src/file-format-policy.js`，建立单一扩展名来源。
- [x] 安装并锁定 `chardet`、`iconv-lite`，检查生产打包是否包含。
- [x] 新增 `backend/src/text-document-support.js`，实现 128 KiB 受限读取、BOM、编码降级、二进制拒绝。
- [x] 新增 `backend/src/text-thumbnail-renderer.js`，支持 plain / Markdown / code-like / delimited 四类布局。
- [x] 修改 `backend/src/thumbnail-task-service.js`，接入文本路由与错误码。
- [x] 保持 `importer.js` 依靠 `supports()` 自动入队，不复制导入分支。
- [x] 保持输出为 `<name>_thumbnail.png`，不改前端和一致性扫描。
- [x] 为刷新缩略图 API 验证 txt/md 可重复生成。

阶段 1 验收标准：

- UTF-8、UTF-8 BOM、GB18030 / GBK、UTF-16LE 文本均能生成可读中文封面；
- `md` 标题、列表、引用、代码和任务列表能体现结构；
- 100 MB 以上文本只读取配置上限，不出现与文件大小线性增长的内存占用；
- 内容含 `<>&"'` 时 SVG 安全且生成成功；
- 空文件、长行、无换行、混合 CRLF、损坏编码均有确定行为；
- 导入后 `noThumbnail`、`processingThumbnail`、`thumbnailTask` 状态清理正确；
- 重启后已完成状态和 PNG 仍可用；
- 旧缩略图在新任务失败时不被破坏。

### 阶段 2：只读详情预览（已实施）

- [x] 复用 / 审查现有 `frontend/public/src/app/text-editor` 资源，不直接信任 `dist` 产物为源码。
- [x] 新增只读文本 API，按 item id 解析库内原文件，禁止任意路径读取。
- [x] 返回编码、MIME、mtime、截断标记；详情默认限制 1-5 MiB，超大文件分页或流式读取。
- [x] Markdown 使用受控渲染器，禁用原始 HTML 和远程资源。
- [x] 前端详情区按格式路由；加载、编码错误、超大文件和二进制伪装都有显式状态。

### 阶段 3：编辑、保存和缓存失效（已实施）

- [x] 仅开放白名单文本扩展名；保存前校验 item id、源文件 mtime 和库内路径。
- [x] 临时文件写入 + 原子替换，失败保留原文件。
- [x] 默认保持原编码；无法无损保存时阻止并提示，不静默转 UTF-8。
- [x] 保存后更新 size / lastModified，重建封面和可选搜索索引。
- [x] 增加冲突检测、撤销策略和崩溃恢复测试。

### 阶段 4：Office 封面（第一小批已实施，旧格式待评估）

- [x] 先做依赖、许可证、ZIP 炸弹、XML 实体、宏文档和嵌入资源安全评审。
- [x] 第一小批只支持 `docx/xlsx/pptx` 只读封面。
- [x] 为文档大小、ZIP entry 数、单 entry 解压大小和总解压大小设硬限制。
- [x] 旧 Office / WPS 格式通过隔离子进程调用 LibreOffice，并设置路径发现、超时、取消和临时目录回收。
- [x] Office 失败必须退回格式占位图，不能拖垮通用任务队列。

### 阶段 5：全文搜索（已实施）

- [x] 明确隐私与性能策略后再把文本片段加入 `search-index.json`。
- [x] 升级索引版本，限制每条内容字符数，避免库体积失控。
- [x] 文件修改 / 保存 / 删除时增量失效；编码失败不阻断其他字段搜索。

## 8. 测试计划

### 8.1 新增单元 / 模块测试

建议新增 `tests/text-thumbnail-closed-loop.mjs`，覆盖：

- UTF-8、BOM、GBK / GB18030、UTF-16LE；
- `txt/md/markdown/log/rst/json/xml/yaml/yml/csv/tsv`；
- Markdown 各结构；
- CJK 双宽换行；
- XML 特殊字符转义；
- 空文件和纯空白文件；
- 超长单行、超多行、128 KiB 边界；
- 二进制内容伪装为 `.txt`；
- 并发 3、取消、超时、冲突、失败回滚、中断恢复；
- PNG 尺寸、非空像素、包含足够明暗差而非纯色占位图。

### 8.2 扩展现有闭环

- 在 `tests/thumbnail-task-closed-loop.mjs` 的格式矩阵中加入 `txt`、`md`、`json`、`csv`；
- 验证自动导入入队，不只验证手动启动；
- 验证 `metadata.json`、`cache.json` 与实际 PNG 一致；
- 验证 `/api/item/thumbnailTask/*` 与 `/api/v2/item/thumbnailTask/*` 两套 API；
- 执行 `npm test`，再执行与桌面预览相关的 Electron 测试。

### 8.3 性能基线

- 导入 1,000 个小文本文件时队列不会无限膨胀；当前 Eagle 队列没有 Orcabox 的 320 项上限，建议在文本批量导入前补有界队列或背压。
- 单个 1 GB 文本文件封面读取内存应接近 128 KiB 加渲染开销，而非接近文件大小。
- 文本封面平均生成耗时应明显低于 PDF / 视频，不应占用 Electron worker。

## 9. 安全与稳定性风险

### P0 / 阻断项

无已发现的必然 P0，但以下错误实现会形成阻断风险：

- 直接把 Markdown 内容插入 HTML 或 SVG 而不转义；
- 详情 API 接收任意文件路径；
- Office ZIP 无解压上限；
- 旧 Office 转换不设超时 / 子进程隔离。

### P1 / 高风险

1. **编码误判**：只按 UTF-8 读取会让中文 GBK 文件封面乱码。
2. **大文件内存**：复用全文读取会让日志和导出文件导致内存峰值。
3. **任务队列无上限**：批量导入文本会增加持久化和内存压力。
4. **元数据污染**：文本封面尺寸不能覆盖“原文件尺寸”语义；建议文本 item 的 width / height 保持 0。
5. **调色板浪费**：每个文本封面再做图片色彩分析没有业务价值。
6. **许可证边界**：Orcabox 声明 MIT，但目标 Eagle 根目录未发现明确项目 LICENSE；复制前需由维护者确认仓库授权和第三方 notices 策略。

### P2 / 中风险

- Sharp SVG 字体在不同平台可能出现排版差异；使用系统字体栈并做像素级容差测试。
- 128 KiB 片段可能截断多字节字符；需处理尾部替换符。
- 以 mtime 判断缓存新鲜度可能遇到时间戳粒度问题；Eagle 首批采用显式任务重建更稳妥。
- `json` 在 Orcabox 同时属于 Lottie 与可编辑文本；Eagle 应先按普通 JSON 文本封面，只有明确识别 Lottie 时再走动画路径。

## 10. 不应直接复制的部分

- `AssetThumbnail.tsx`：依赖 React、React Query、Zustand、主题与虚拟列表体系。
- `TextDocumentSurface.tsx`：依赖 Markdown 编辑器、GFM 插件、设置持久化和撤销历史。
- `preview-service.ts` 整体：文件超过 7,000 行，耦合图片、视频、音频、文档、PSD、缓存和队列；只提炼最小算法。
- Office reader / writer 整体：依赖较多且安全边界复杂，应按 Eagle 模块化重写。
- Orcabox 深色 SVG 视觉：技术可参考，品牌和主题不应原样照搬。

可直接复用的主要是算法和规则，而不是文件级复制：

- 扩展名集合；
- 128 KiB 受限读取；
- BOM + chardet + iconv-lite 解码策略；
- 乱码合理性评分；
- Markdown 行分类；
- CJK 视觉宽度；
- XML / SVG 转义；
- 空内容与失败占位策略。

## 11. 预计修改清单

阶段 1 预计新增：

- `backend/src/file-format-policy.js`
- `backend/src/text-document-support.js`
- `backend/src/text-thumbnail-renderer.js`
- `tests/text-thumbnail-closed-loop.mjs`
- `tests/fixtures/text/*`（或测试运行时创建，优先避免提交大 fixture）

阶段 1 预计修改：

- `backend/src/thumbnail-task-service.js`
- `package.json`
- `package-lock.json`
- 可选：`tests/thumbnail-task-closed-loop.mjs`

原则上无需修改：

- `backend/src/importer.js`
- `backend/src/library-store.js`
- `backend/src/library-consistency-service.js`
- 前端网格组件和静态资源

## 12. 当前工作区状态说明

审查期间未修改 Orcabox 业务代码。阶段 1 实施已修改 Eagle 的 `thumbnail-task-service.js`、`package.json`、`package-lock.json` 并新增文本封面模块与测试；本报告同时记录实施结果。

Eagle 在审查开始前已有多项未跟踪内容，包括 `.workbuddy/`、`BUILD_AUDIT_REPORT.md`、多个 `NEXT_TASK_*.md`、`audit-build-20260804-1653/`、`canvas-settings-migration-plan-codexgrok.md`、`scripts/assemble-frontend.mjs`、`tests/production-build.mjs`。后续实施必须保留这些用户现有内容，不得清理或覆盖。

Orcabox 在审查开始前已有未跟踪 `.claude/` 和 `启动开发.bat`，本次未触碰。

## 13. 最终实施建议

按“文本 PNG 封面 → 只读详情 → 编辑保存 → Office → 全文搜索”推进。第一阶段只扩展 Eagle 现有缩略图任务，能以最小改动立即覆盖用户提出的 txt、md 等封面需求，并保持现有前端、资源库结构、API 和一致性扫描不变。

实施时优先完成自动化闭环再扩格式数量。不要在同一批次同时引入动态网格文本、详情编辑、Office 转换和全文索引，否则编码、缓存、安全与 UI 风险会互相耦合，难以定位回归。

## 14. 阶段 1 执行结果（2026-08-05）

阶段 1 已完成并接入现有 `ThumbnailTaskService`，实际改动如下：

- 新增 `backend/src/file-format-policy.js`，集中维护 `txt/md/markdown/log/rst/json/xml/yaml/yml/csv/tsv` 格式集合、Markdown / 结构化 / 分隔文本分类和 MIME 映射。
- 新增 `backend/src/text-document-support.js`，实现最多 128 KiB 的文件描述符分段读取、BOM 优先、`chardet` 探测、`iconv-lite` 降级解码、二进制签名与 NUL / 控制字符拒绝、空文件处理。
- 新增 `backend/src/text-thumbnail-renderer.js`，实现普通文本、Markdown 结构化摘要、JSON / XML / YAML 代码风格、CSV / TSV 分隔风格，并生成 SVG 后由 `sharp` 输出 PNG。
- 修改 `backend/src/thumbnail-task-service.js`，将文本格式加入 `supports()` / `enqueue()`，在统一任务队列中优先路由 `renderTextThumbnail()`，文本封面跳过图片调色板分析并使用固定中性调色板；文本空文件可生成占位封面，文本大文件只读取 128 KiB。
- 安装并锁定 `chardet` 与 `iconv-lite`，更新 `package.json` 和 `package-lock.json`。
- 新增 `tests/text-thumbnail-closed-loop.mjs` 并加入 `npm test` 脚本。
- 为 `ThumbnailTaskService` 增加默认 1024 的 pending 队列上限，支持 `EAGLE_THUMBNAIL_MAX_PENDING` 配置；队列满时返回 `THUMBNAIL_QUEUE_FULL`（HTTP 429）。
- 调整 `electron/pdf-thumbnail-worker.html`，PDF.js 在反向工程源码缺失时可回退加载构建产物。

验证结果：

- `node tests/text-thumbnail-closed-loop.mjs` 通过：覆盖 UTF-8、UTF-8 BOM、GBK / GB18030、UTF-16LE、空文件、超长单行、二进制伪装、损坏文本、Markdown 结构、XML 特殊字符、重启恢复、刷新 API、并发队列和取消保护。
- `npm run build` 通过，新增依赖可进入现有 Vite 生产构建流程。
- 收尾已把反向工程依赖资源移入项目内：`electron/pdf-viewer/build`（PDF.js 运行时资源）、`tests/fixtures/plugins/example-service-plugin`（插件测试夹具）、`tests/fixtures/library-example/Demo.library`（迁移测试夹具）；同时删除 `H:\dev` 根部的临时外部拷贝。
- `backend/src/server.js`、`backend/src/library-store.js` 以及相关测试已改为读取项目内资源路径，不再依赖 `H:\dev` 根目录的反向工程副本。
- 启动默认后端、Vite（5176）和 Electron 调试目标（9226）后，`npm test` 全量通过，最终退出码为 0；该结果在删除 `H:\dev` 根部临时外部拷贝后再次验证通过。覆盖 `api-smoke`、`plugin-smoke`、`thumbnail-task`、`text-thumbnail`、`text-detail`、`text-save`、`office-thumbnail`、`legacy-office-thumbnail`、`text-search`、`roadmap-panels`、`library-transaction-faults`、`library-crash-recovery`、`library-concurrency`、`library-backup-restore` 等全部 41 个测试入口。

偏离报告的原因：

- `npm install` 实际解析为 `chardet@2.2.0` 与 `iconv-lite@0.7.3`，版本高于报告示例的 `2.1.1` / `0.7.2`，接口兼容。
- 测试未提交静态 fixture，改为运行时在临时目录生成，避免增加仓库体积。
- 封面边长沿用 Eagle 现有 `DEFAULT_MAX_SIZE = 480`，文本 SVG 以 512 画布生成后再缩放到任务配置尺寸。
- 阶段 1 未提前实施动态网格文本、编辑保存、Office 或全文搜索；阶段 2 的只读详情预览已单独闭环。

剩余风险：

- 编码探测对极短 GBK / Big5 文本仍可能依赖启发式评分；现有实现优先 UTF-8 与 GB18030 / GBK，长文本通常由 `chardet` 给出更稳定结果。
- 资源已放入项目内，但尚未执行 Git commit；提交前需要确认 PDF.js 等第三方资源的许可与 notices 策略。
- 队列已加入上限，但导入流程对 `THUMBNAIL_QUEUE_FULL` 目前只是记录 `thumbnailError`，后续如要自动重试或等待队列释放，需要在批量导入路径增加异步背压。

## 15. 阶段 2 执行结果（2026-08-05）

阶段 2 只读详情预览已完成并接入项目，实际改动如下：

- 新增 `backend/src/text-detail-service.js`，按 item id 读取库内原文件，禁止任意路径；返回内容、编码、MIME、mtime、size、offset、endOffset、截断标记和分页状态。
- 扩展 `backend/src/text-document-support.js`，支持按字节 offset 读取文本片段，并沿用 BOM、编码探测、二进制拒绝和尾部不完整字符清理。
- 新增 v1 / v2 只读 API：`GET /api/item/textDetail` 与 `GET /api/v2/item/textDetail`。
- 新增项目内只读前端页 `frontend/public/src/app/text-editor/`，包含 HTML、CSS 和 vanilla JS；Markdown 使用 DOM 文本节点受控渲染，不执行原始 HTML、不加载远程资源，图片和链接只显示安全文本。
- 前端支持加载中、空文件、二进制/编码错误、超限文件状态，以及大文件分页加载。
- 新增 `tests/text-detail-closed-loop.mjs` 并加入 `npm test`。

验证结果：

- `node tests/text-detail-closed-loop.mjs` 通过：覆盖 UTF-8、GB18030 / GBK、UTF-16LE、Markdown、空文件、二进制伪装、非文本格式拒绝、缺失 item、超大文件分页、v1/v2 API 一致性。
- `npm run build` 通过，文本详情页已进入 `dist/frontend/src/app/text-editor/`。
- 完整 `npm test` 通过，退出码 0。

剩余风险：

- 分页按字节 offset 读取，多字节编码在页边界可能丢弃一个不完整字符；已清理尾部替换符，但跨页展示大型 CJK 文件时仍需后续做字符边界对齐优化。
- 阶段 2 仍是只读预览，不包含编辑保存、编码保留、原子替换或冲突检测，这些属于阶段 3。

## 16. 阶段 3 执行结果（2026-08-05）

阶段 3 文本编辑、保存、冲突检测和撤销已完成并接入项目，实际改动如下：

- 新增 `backend/src/text-save-service.js`，实现白名单文本保存、item 校验、`expectedMtimeMs` 冲突检测、临时文件写入、备份回滚式原子替换、保存后更新 `size` / `lastModified` 并重建封面。
- 保存时读取原文件编码、BOM 和 CRLF 状态；UTF-8、UTF-16、GB18030 / GBK 等可无损往返时保持原编码，无法无损保存时返回 `TEXT_ENCODING_UNSUPPORTED`，不静默转 UTF-8。
- 每次保存前将当前原文件写入 item 信息目录的 `text-undo.bin`，提供一级撤销；撤销后恢复原文件、更新 item 元数据并重建封面。
- 新增 v1 / v2 API：`POST /api/item/textSave`、`POST /api/item/textUndo`、`POST /api/v2/item/textSave`、`POST /api/v2/item/textUndo`。
- 前端 `frontend/public/src/app/text-editor/` 增加编辑、保存、取消、撤销和“加载全部”入口；编辑要求先完整加载文件，保存使用详情返回的 mtime 做冲突校验。
- 新增 `tests/text-save-closed-loop.mjs` 并加入 `npm test`。

验证结果：

- `node tests/text-save-closed-loop.mjs` 通过：覆盖保存成功、冲突 409、mtime 必填、缺失 item、非文本格式拒绝、GB18030 编码保留、无法无损保存时拒绝、撤销恢复、二次撤销不可用、空文件保存和封面重建。
- `npm run build` 通过。
- 完整 `npm test` 通过，退出码 0。

剩余风险：

- 撤销当前为单级侧车文件快照，多次连续保存后只回滚到最近一次保存前状态；更完整的多级历史需要后续单独设计。
- 编码无损判定依赖 `iconv-lite` 的实际往返结果，不同 Node 版本 / 编码表可能影响边界行为。
- 保存后尚未接入全文搜索索引，全文索引属于阶段 5；阶段 3 只重建文本封面。

## 17. 阶段 4 执行结果（2026-08-05）

阶段 4 第一小批 Office 只读封面已完成并接入统一缩略图队列，实际改动如下：

- 新增 `backend/src/office-document-support.js`，使用 `jszip` 与 `fast-xml-parser` 实现 `docx/xlsx/pptx` 受限读取。
- 安全边界：文件最大 512 MiB、ZIP entry 最多 4096、单 entry 解压最大 256 MiB、总解压最大 768 MiB、XML 最大 64 MiB；拒绝 DOCTYPE / ENTITY 声明，防止 XML 实体攻击。
- DOCX 提取段落文本；XLSX 提取共享字符串、工作表名和前 20 行 8 列；PPTX 提取前 3 张幻灯片的标题与正文。
- 新增 `backend/src/office-thumbnail-renderer.js`，生成 SVG 后由 `sharp` 输出 PNG；读取失败时返回格式占位图，不会让任务队列被单个 Office 文件拖垮。
- 修改 `backend/src/thumbnail-task-service.js`，将 `docx/xlsx/pptx` 加入 `supports()` / `enqueue()` 和渲染路由，Office 封面跳过图片调色板分析。
- 安装并锁定 `jszip` 与 `fast-xml-parser`。
- 新增 `tests/office-thumbnail-closed-loop.mjs` 并加入 `npm test`。

验证结果：

- `node tests/office-thumbnail-closed-loop.mjs` 通过：覆盖 DOCX、XLSX、PPTX 提取与 PNG 生成、自动导入入队、元数据一致性、损坏 ZIP 占位图回退、ZIP entry 数量硬限制。
- `npm run build` 通过。
- 完整 `npm test` 通过，退出码 0。

剩余风险：

- Office 解析只覆盖封面所需的文本与表格摘要，不包含复杂图表、嵌入图片、批注和公式完整渲染。
- `jszip` 与 `fast-xml-parser` 的第三方许可和 notices 尚未提交，后续 commit 前需一并确认。

## 18. 阶段 4 旧格式转换执行结果（2026-08-05）

阶段 4 剩余旧 Office / WPS / OpenDocument 转换已完成，实际改动如下：

- 新增 `backend/src/legacy-office-thumbnail-renderer.js`，支持 `doc/xls/ppt/wps/et/dps/odt/ods/odp` 格式。
- 实现 LibreOffice 路径发现：`EAGLE_SOFFICE_PATH` 优先，其次扫描常见 macOS / Linux / Windows 安装路径。
- 使用隔离子进程执行 `soffice --headless --convert-to pdf`，转换后复用现有 PDF 缩略图 worker 输出 PNG；支持超时、进程树 kill、任务取消接管和临时目录 finally 回收。
- LibreOffice 不可用、转换失败、超时或无输出时自动生成格式占位图，不会让任务失败或拖垮队列。
- 修改 `backend/src/thumbnail-task-service.js`，将旧 Office 格式加入 `supports()` / `enqueue()` 和渲染路由。
- 新增 `tests/legacy-office-thumbnail-closed-loop.mjs` 并加入 `npm test`；测试使用 fake converter 验证路径发现、成功转换、超时占位图、不可用占位图和自动入队。

验证结果：

- `node tests/legacy-office-thumbnail-closed-loop.mjs` 通过。
- `node tests/office-thumbnail-closed-loop.mjs` 通过。
- `npm run build` 通过。
- 完整 `npm test` 通过，退出码 0。

剩余风险：

- 当前机器未安装真实 LibreOffice，转换链路用 fake converter 验证；部署环境需要配置 `EAGLE_SOFFICE_PATH` 或安装 LibreOffice。
- LibreOffice 首次启动可能较慢，正式使用时应按 `EAGLE_OFFICE_CONVERT_TIMEOUT_MS` 调大默认超时。
- 转换输出依赖 LibreOffice 对旧格式的实际支持程度，复杂宏、嵌入对象和极老 WPS 文件仍可能退回占位图。

## 19. 阶段 5 执行结果（2026-08-05）

阶段 5 文本全文搜索已完成，实际改动如下：

- 新增 `backend/src/search-index-service.js`，将 `search-index.json` 升级到版本 2，并为每个条目保存受限 `textContent`。
- 每条文本内容最多索引 20,000 字符，读取上限仍为 128 KiB；二进制拒绝、编码失败或文件缺失时该条目保留为空内容，不阻断其他字段搜索。
- 索引条目使用 `freshnessKey` 缓存文本片段；导入、文本保存或 item 元数据变化时自动重建对应条目，旧内容不会继续命中搜索。
- 修改 `backend/src/library-store.js` 和 `library-transaction-coordinator.js`，库加载和事务快照统一使用 v2 搜索索引。
- 修改 `backend/src/search-service.js` 与 `backend/src/server.js`，`/api/item/search`、v2 query 和 filter 搜索都会合并 `search-index.json` 的文本内容参与关键词匹配。
- 新增 `tests/text-search-closed-loop.mjs` 并加入 `npm test`。

验证结果：

- `node tests/text-search-closed-loop.mjs` 通过：覆盖文本关键词搜索、v2 query、二进制伪装不阻塞、单条索引字符上限、保存后增量失效、删除后排除和 rebuild 持久化。
- `npm run build` 通过。
- 完整 `npm test` 通过，退出码 0。

剩余风险：

- 当前全文搜索只覆盖文本格式，不包含 Office 提取内容；如需搜索 DOCX / PDF 等，需要在后续扩展索引来源。
- 外部直接修改文件而未触发 item 保存时，搜索索引不会自动感知文件 mtime 变化；当前增量失效依赖导入、保存和删除流程。
- `textContent` 已写入 `search-index.json`，批量导入大型文本库时索引体积会增长，需继续观察性能并可在后续改为外部全文索引。
