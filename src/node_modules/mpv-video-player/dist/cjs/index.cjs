"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const PluginManager = require("../PluginManager-d3c7a0cc.cjs");
const components_index = require("./components/index.cjs");
const webglRenderer = require("../webgl-renderer-ab6e5034.cjs");
const plugins_index = require("./plugins/index.cjs");
const ControlPlugin = require("../ControlPlugin-320fa16e.cjs");
const plugins_playButton_index = require("./plugins/play-button/index.cjs");
const plugins_frameStep_index = require("./plugins/frame-step/index.cjs");
const plugins_volume_index = require("./plugins/volume/index.cjs");
const plugins_timeDisplay_index = require("./plugins/time-display/index.cjs");
const plugins_progress_index = require("./plugins/progress/index.cjs");
const plugins_speed_index = require("./plugins/speed/index.cjs");
const plugins_loopButton_index = require("./plugins/loop-button/index.cjs");
const plugins_fullscreen_index = require("./plugins/fullscreen/index.cjs");
const plugins_keyboard_index = require("./plugins/keyboard/index.cjs");
const plugins_abLoop_index = require("./plugins/ab-loop/index.cjs");
const plugins_thumbnail_index = require("./plugins/thumbnail/index.cjs");
const plugins_hdr_index = require("./plugins/hdr/index.cjs");
const plugins_debug_index = require("./plugins/debug/index.cjs");
const plugins_notes_index = require("./plugins/notes/index.cjs");
require("../index-bf534bb9.cjs");
require("../utils-5b8f86d8.cjs");
var NetworkState = /* @__PURE__ */ ((NetworkState2) => {
  NetworkState2[NetworkState2["NETWORK_EMPTY"] = 0] = "NETWORK_EMPTY";
  NetworkState2[NetworkState2["NETWORK_IDLE"] = 1] = "NETWORK_IDLE";
  NetworkState2[NetworkState2["NETWORK_LOADING"] = 2] = "NETWORK_LOADING";
  NetworkState2[NetworkState2["NETWORK_NO_SOURCE"] = 3] = "NETWORK_NO_SOURCE";
  return NetworkState2;
})(NetworkState || {});
var ReadyState = /* @__PURE__ */ ((ReadyState2) => {
  ReadyState2[ReadyState2["HAVE_NOTHING"] = 0] = "HAVE_NOTHING";
  ReadyState2[ReadyState2["HAVE_METADATA"] = 1] = "HAVE_METADATA";
  ReadyState2[ReadyState2["HAVE_CURRENT_DATA"] = 2] = "HAVE_CURRENT_DATA";
  ReadyState2[ReadyState2["HAVE_FUTURE_DATA"] = 3] = "HAVE_FUTURE_DATA";
  ReadyState2[ReadyState2["HAVE_ENOUGH_DATA"] = 4] = "HAVE_ENOUGH_DATA";
  return ReadyState2;
})(ReadyState || {});
class TimeRangesImpl {
  constructor(ranges = []) {
    this.ranges = ranges;
  }
  get length() {
    return this.ranges.length;
  }
  start(index) {
    if (index < 0 || index >= this.ranges.length) {
      throw new DOMException("Index out of bounds", "IndexSizeError");
    }
    return this.ranges[index].start;
  }
  end(index) {
    if (index < 0 || index >= this.ranges.length) {
      throw new DOMException("Index out of bounds", "IndexSizeError");
    }
    return this.ranges[index].end;
  }
  /** 添加時間範圍（內部使用） */
  addRange(start, end) {
    let left = 0;
    let right = this.ranges.length;
    while (left < right) {
      const mid = left + right >>> 1;
      if (this.ranges[mid].start < start) {
        left = mid + 1;
      } else {
        right = mid;
      }
    }
    this.ranges.splice(left, 0, { start, end });
    const merged = [];
    for (const range of this.ranges) {
      if (merged.length === 0 || merged[merged.length - 1].end < range.start) {
        merged.push(range);
      } else {
        merged[merged.length - 1].end = Math.max(merged[merged.length - 1].end, range.end);
      }
    }
    this.ranges = merged;
  }
  /** 清空所有範圍（內部使用） */
  clear() {
    this.ranges = [];
  }
  /** 設置完整緩衝（內部使用） */
  setFullyBuffered(duration) {
    this.ranges = [{ start: 0, end: duration }];
  }
}
class MediaErrorImpl {
  constructor(code, message = "") {
    this.MEDIA_ERR_ABORTED = 1;
    this.MEDIA_ERR_NETWORK = 2;
    this.MEDIA_ERR_DECODE = 3;
    this.MEDIA_ERR_SRC_NOT_SUPPORTED = 4;
    this.code = code;
    this.message = message;
  }
}
MediaErrorImpl.MEDIA_ERR_ABORTED = 1;
MediaErrorImpl.MEDIA_ERR_NETWORK = 2;
MediaErrorImpl.MEDIA_ERR_DECODE = 3;
MediaErrorImpl.MEDIA_ERR_SRC_NOT_SUPPORTED = 4;
class VideoEventManager {
  constructor(element) {
    this.eventHandlers = /* @__PURE__ */ new Map();
    this.firedEvents = /* @__PURE__ */ new Set();
    this.loadId = 0;
    this.element = element;
    this.state = this.createInitialState();
    this.previousState = {};
  }
  /** 創建初始狀態 */
  createInitialState() {
    return {
      networkState: 0,
      readyState: 0,
      paused: true,
      ended: false,
      seeking: false,
      currentTime: 0,
      duration: 0,
      volume: 1,
      muted: false,
      playbackRate: 1,
      videoWidth: 0,
      videoHeight: 0,
      buffered: new TimeRangesImpl(),
      loadProgress: 0,
      error: null
    };
  }
  // ===== 狀態訪問器 =====
  get networkState() {
    return this.state.networkState;
  }
  get readyState() {
    return this.state.readyState;
  }
  get paused() {
    return this.state.paused;
  }
  get ended() {
    return this.state.ended;
  }
  get seeking() {
    return this.state.seeking;
  }
  get currentTime() {
    return this.state.currentTime;
  }
  get duration() {
    return this.state.duration;
  }
  get volume() {
    return this.state.volume;
  }
  get muted() {
    return this.state.muted;
  }
  get playbackRate() {
    return this.state.playbackRate;
  }
  get videoWidth() {
    return this.state.videoWidth;
  }
  get videoHeight() {
    return this.state.videoHeight;
  }
  get buffered() {
    return this.state.buffered || new TimeRangesImpl();
  }
  get error() {
    return this.state.error;
  }
  // ===== on* 事件處理器管理 =====
  setEventHandler(type, handler) {
    this.eventHandlers.set(type, handler);
  }
  getEventHandler(type) {
    return this.eventHandlers.get(type) || null;
  }
  // ===== 事件觸發 =====
  /** 觸發媒體事件 */
  emit(type, eventInit) {
    const event = new Event(type, { bubbles: false, cancelable: false, ...eventInit });
    const handler = this.eventHandlers.get(type);
    if (handler) {
      handler.call(this.element, event);
    }
    this.element.dispatchEvent(event);
  }
  /** 觸發錯誤事件 */
  emitError(code, message) {
    this.state.error = new MediaErrorImpl(code, message);
    this.state.networkState = 1;
    this.state.readyState = 0;
    const event = new ErrorEvent("error", {
      message,
      bubbles: false,
      cancelable: false
    });
    const handler = this.eventHandlers.get("error");
    if (handler) {
      handler.call(this.element, event);
    }
    this.element.dispatchEvent(event);
  }
  // ===== 載入流程事件 =====
  /** 開始載入新資源 - 觸發完整載入序列 */
  startLoad() {
    this.loadId++;
    this.firedEvents.clear();
    this.state = this.createInitialState();
    this.state.networkState = 2;
    this.emit("emptied");
    this.emit("loadstart");
  }
  /** 中止載入 - abort 事件 */
  abortLoad() {
    if (this.state.networkState === 2) {
      this.state.networkState = 1;
      this.state.error = new MediaErrorImpl(MediaErrorImpl.MEDIA_ERR_ABORTED, "Load aborted");
      this.emit("abort");
    }
  }
  /** 更新載入進度 - progress 事件 */
  updateProgress(loaded, total) {
    const currentLoadId = this.loadId;
    if (this.state.networkState !== 2)
      return;
    this.state.loadProgress = total > 0 ? loaded / total : 0;
    if (this.state.duration > 0 && this.state.buffered instanceof TimeRangesImpl) {
      this.state.buffered.setFullyBuffered(this.state.duration * this.state.loadProgress);
    }
    if (currentLoadId === this.loadId) {
      this.emit("progress");
    }
  }
  /** 載入暫停 - suspend 事件 */
  suspendLoad() {
    if (this.state.networkState === 2) {
      this.state.networkState = 1;
      this.emit("suspend");
    }
  }
  /** 數據停滯 - stalled 事件 */
  stalled() {
    this.emit("stalled");
  }
  /** 元數據已載入 */
  loadedMetadata(duration, videoWidth, videoHeight) {
    const currentLoadId = this.loadId;
    this.state.duration = duration;
    this.state.videoWidth = videoWidth;
    this.state.videoHeight = videoHeight;
    this.state.readyState = 1;
    if (currentLoadId === this.loadId && !this.firedEvents.has("loadedmetadata")) {
      this.firedEvents.add("loadedmetadata");
      this.emit("loadedmetadata");
      this.emit("durationchange");
    }
  }
  /** 第一幀已載入 */
  loadedData() {
    const currentLoadId = this.loadId;
    this.state.readyState = 2;
    if (currentLoadId === this.loadId && !this.firedEvents.has("loadeddata")) {
      this.firedEvents.add("loadeddata");
      this.emit("loadeddata");
    }
  }
  /** 可以開始播放 */
  canPlay() {
    const currentLoadId = this.loadId;
    this.state.readyState = 3;
    if (currentLoadId === this.loadId && !this.firedEvents.has("canplay")) {
      this.firedEvents.add("canplay");
      this.emit("canplay");
    }
  }
  /** 可以播放到結尾 */
  canPlayThrough() {
    const currentLoadId = this.loadId;
    this.state.readyState = 4;
    this.state.networkState = 1;
    if (this.state.duration > 0 && this.state.buffered instanceof TimeRangesImpl) {
      this.state.buffered.setFullyBuffered(this.state.duration);
    }
    if (currentLoadId === this.loadId && !this.firedEvents.has("canplaythrough")) {
      this.firedEvents.add("canplaythrough");
      this.emit("canplaythrough");
      this.emit("suspend");
    }
  }
  // ===== 播放控制事件 =====
  /** 開始播放 - play 和 playing 事件 */
  play() {
    const wasPaused = this.state.paused;
    this.state.paused = false;
    this.state.ended = false;
    if (wasPaused) {
      this.emit("play");
    }
    this.emit("playing");
  }
  /** 暫停播放 - pause 事件 */
  pause() {
    if (!this.state.paused) {
      this.state.paused = true;
      this.emit("pause");
    }
  }
  /** 播放結束 - ended 事件 */
  fireEnded() {
    if (!this.state.ended) {
      this.state.ended = true;
      this.state.paused = true;
      this.emit("pause");
      this.emit("ended");
    }
  }
  /** 等待數據 - waiting 事件 */
  waiting() {
    this.emit("waiting");
  }
  // ===== 搜尋事件 =====
  /** 開始搜尋 */
  startSeek() {
    if (!this.state.seeking) {
      this.state.seeking = true;
      this.emit("seeking");
    }
  }
  /** 搜尋完成 */
  endSeek(newTime) {
    if (this.state.seeking) {
      this.state.seeking = false;
      this.state.currentTime = newTime;
      this.emit("seeked");
      this.emit("timeupdate");
    }
  }
  // ===== 時間更新 =====
  /** 更新當前時間 */
  updateTime(time) {
    this.state.currentTime = time;
    this.emit("timeupdate");
  }
  /** 更新時長 */
  updateDuration(duration) {
    if (this.state.duration !== duration) {
      this.state.duration = duration;
      if (this.state.readyState >= 4 && this.state.buffered instanceof TimeRangesImpl) {
        this.state.buffered.setFullyBuffered(duration);
      }
      this.emit("durationchange");
    }
  }
  // ===== 音量和速率 =====
  /** 更新音量 */
  updateVolume(volume, muted) {
    if (this.state.volume !== volume || this.state.muted !== muted) {
      this.state.volume = volume;
      this.state.muted = muted;
      this.emit("volumechange");
    }
  }
  /** 更新播放速率 */
  updatePlaybackRate(rate) {
    if (this.state.playbackRate !== rate) {
      this.state.playbackRate = rate;
      this.emit("ratechange");
    }
  }
  // ===== 尺寸更新 =====
  /** 更新視頻尺寸 */
  updateVideoSize(width, height) {
    if (this.state.videoWidth !== width || this.state.videoHeight !== height) {
      this.state.videoWidth = width;
      this.state.videoHeight = height;
      this.emit("resize");
    }
  }
  // ===== 狀態同步 =====
  /** 從外部狀態同步（來自 mpv） */
  syncState(externalState) {
    const prev = {
      currentTime: this.state.currentTime,
      duration: this.state.duration,
      paused: this.state.paused,
      volume: this.state.volume,
      ended: this.state.ended
    };
    this.state.currentTime = externalState.position;
    this.state.paused = externalState.paused;
    this.state.volume = externalState.volume / 100;
    if (externalState.duration > 0 && externalState.duration !== this.state.duration) {
      this.state.duration = externalState.duration;
      if (this.state.buffered instanceof TimeRangesImpl) {
        this.state.buffered.setFullyBuffered(externalState.duration);
      }
    }
    return {
      timeChanged: prev.currentTime !== this.state.currentTime,
      durationChanged: prev.duration !== this.state.duration,
      pausedChanged: prev.paused !== this.state.paused,
      volumeChanged: prev.volume !== this.state.volume,
      wasEnded: !prev.ended && this.state.ended
    };
  }
  /** 重置狀態 */
  reset() {
    this.loadId++;
    this.firedEvents.clear();
    this.state = this.createInitialState();
  }
}
const DEGRADATION_LEVELS = [
  1,
  // Level 0: 100% (無降級)
  0.75,
  // Level 1: 75%
  0.5,
  // Level 2: 50%
  0.33,
  // Level 3: 33%
  0.25
  // Level 4: 25% (最低)
];
const DEFAULT_CONFIG = {
  degradationThreshold: 0.7,
  recoveryThreshold: 0.95,
  stabilityDuration: 1500,
  minimumFps: 12,
  sampleWindowSize: 30,
  enabled: false,
  // 禁用 JS 端降級，由 C++ 端動態控制
  screenRefreshRate: 0
  // 0 = auto-detect
};
async function detectScreenRefreshRate() {
  return new Promise((resolve) => {
    const samples = [];
    let lastTime = 0;
    let frameCount = 0;
    const targetSamples = 10;
    const measure = (timestamp) => {
      if (lastTime > 0) {
        const delta = timestamp - lastTime;
        if (delta > 0 && delta < 100) {
          samples.push(delta);
        }
      }
      lastTime = timestamp;
      frameCount++;
      if (samples.length >= targetSamples) {
        const avgDelta = samples.reduce((a, b) => a + b, 0) / samples.length;
        const detectedFps = Math.round(1e3 / avgDelta);
        const commonRates = [60, 75, 90, 120, 144, 165, 240];
        const closest = commonRates.reduce(
          (prev, curr) => Math.abs(curr - detectedFps) < Math.abs(prev - detectedFps) ? curr : prev
        );
        console.log(`[AdaptiveFps] Screen refresh rate detected: ~${detectedFps}fps → ${closest}Hz`);
        resolve(closest);
      } else if (frameCount < 20) {
        requestAnimationFrame(measure);
      } else {
        console.log("[AdaptiveFps] Could not detect refresh rate, defaulting to 60Hz");
        resolve(60);
      }
    };
    requestAnimationFrame(measure);
  });
}
class AdaptiveFpsController {
  constructor(config = {}) {
    this.nativeFps = 24;
    this.screenRefreshRate = 60;
    this.screenRefreshRateDetected = false;
    this.frameTimestamps = [];
    this.actualFps = 0;
    this.currentLevel = 0;
    this.degradationStartTime = 0;
    this.recoveryStartTime = 0;
    this.onDegradation = null;
    this.onRecovery = null;
    this.totalFrames = 0;
    this.degradedFrames = 0;
    this.isPaused = false;
    this.config = { ...DEFAULT_CONFIG, ...config };
    if (this.config.screenRefreshRate > 0) {
      this.screenRefreshRate = this.config.screenRefreshRate;
      this.screenRefreshRateDetected = true;
    } else {
      this.detectAndSetScreenRefreshRate();
    }
  }
  /**
   * 異步偵測並設定屏幕刷新率
   */
  async detectAndSetScreenRefreshRate() {
    try {
      const rate = await detectScreenRefreshRate();
      this.screenRefreshRate = rate;
      this.screenRefreshRateDetected = true;
      console.log(`[AdaptiveFps] Using screen refresh rate: ${rate}Hz`);
    } catch {
      console.warn("[AdaptiveFps] Failed to detect screen refresh rate, using 60Hz");
      this.screenRefreshRate = 60;
      this.screenRefreshRateDetected = true;
    }
  }
  /**
   * 獲取目標 FPS（考慮屏幕刷新率上限）
   * 這是理論上能達到的最大 FPS
   */
  getTargetFps() {
    return Math.min(this.nativeFps, this.screenRefreshRate);
  }
  /**
   * 獲取屏幕刷新率
   */
  getScreenRefreshRate() {
    return this.screenRefreshRate;
  }
  /**
   * 手動設定屏幕刷新率
   */
  setScreenRefreshRate(rate) {
    if (rate > 0) {
      this.screenRefreshRate = rate;
      this.screenRefreshRateDetected = true;
      console.log(`[AdaptiveFps] Screen refresh rate manually set to ${rate}Hz`);
    }
  }
  /**
   * 設定視頻原生 FPS
   */
  setNativeFps(fps) {
    if (fps > 0 && fps !== this.nativeFps) {
      this.nativeFps = fps;
      this.reset();
      const targetFps = this.getTargetFps();
      if (fps > this.screenRefreshRate) {
        console.log(`[AdaptiveFps] Native FPS: ${fps} → capped to ${targetFps}fps (screen: ${this.screenRefreshRate}Hz)`);
      } else {
        console.log(`[AdaptiveFps] Native FPS set to ${fps}`);
      }
    }
  }
  /**
   * 記錄一個幀的渲染完成
   * 應在每次成功渲染幀後調用
   */
  recordFrame(timestamp) {
    if (this.isPaused) {
      return;
    }
    this.totalFrames++;
    if (this.currentLevel > 0) {
      this.degradedFrames++;
    }
    this.frameTimestamps.push(timestamp);
    while (this.frameTimestamps.length > this.config.sampleWindowSize) {
      this.frameTimestamps.shift();
    }
    this.calculateActualFps();
    if (this.config.enabled) {
      this.evaluateDegradation(timestamp);
    }
  }
  /**
   * 獲取當前應該使用的有效幀間隔 (ms)
   */
  getEffectiveFrameInterval() {
    const effectiveFps = this.getEffectiveFps();
    return 1e3 / effectiveFps * 0.95;
  }
  /**
   * 獲取當前有效 FPS
   * 考慮：屏幕刷新率上限 + 降級層級 + 最小 FPS 保障
   */
  getEffectiveFps() {
    const targetFps = this.getTargetFps();
    const ratio = DEGRADATION_LEVELS[this.currentLevel];
    const degradedFps = targetFps * ratio;
    return Math.max(degradedFps, this.config.minimumFps);
  }
  /**
   * 獲取完整的 FPS 統計資訊
   */
  getStats() {
    const targetFps = this.getTargetFps();
    const effectiveFps = this.getEffectiveFps();
    const ratio = DEGRADATION_LEVELS[this.currentLevel];
    return {
      nativeFps: this.nativeFps,
      screenRefreshRate: this.screenRefreshRate,
      targetFps,
      effectiveFps,
      actualFps: this.actualFps,
      degradationLevel: this.currentLevel,
      degradationRatio: ratio,
      isDegraded: this.currentLevel > 0,
      isRecovering: this.recoveryStartTime > 0,
      isScreenLimited: this.nativeFps > this.screenRefreshRate
    };
  }
  /**
   * 設定降級回調
   */
  onDegradationChange(callback) {
    this.onDegradation = callback;
  }
  /**
   * 設定恢復回調
   */
  onRecoveryChange(callback) {
    this.onRecovery = callback;
  }
  /**
   * 手動強制降級到指定層級
   */
  forceDegradation(level) {
    const newLevel = Math.max(0, Math.min(level, DEGRADATION_LEVELS.length - 1));
    if (newLevel !== this.currentLevel) {
      const oldLevel = this.currentLevel;
      this.currentLevel = newLevel;
      console.log(`[AdaptiveFps] Forced degradation: level ${oldLevel} → ${newLevel}`);
      if (this.onDegradation) {
        this.onDegradation(this.getStats(), "manual");
      }
    }
  }
  /**
   * 重置控制器狀態
   */
  reset() {
    this.frameTimestamps = [];
    this.actualFps = 0;
    this.currentLevel = 0;
    this.degradationStartTime = 0;
    this.recoveryStartTime = 0;
    this.isPaused = false;
    console.log("[AdaptiveFps] Controller reset");
  }
  /**
   * 銷毀控制器，清理所有引用
   * 防止回調函數持有對已銷毀組件的引用導致內存洩漏
   */
  destroy() {
    this.reset();
    this.onDegradation = null;
    this.onRecovery = null;
    console.log("[AdaptiveFps] Controller destroyed");
  }
  /**
   * 暫停 FPS 樣本收集
   * 當視頻暫停、seeking、或其他非正常播放狀態時調用
   * 會清空已收集的時間戳，避免恢復播放後計算出錯誤的 FPS
   */
  pauseCollection() {
    if (this.isPaused)
      return;
    this.isPaused = true;
    this.frameTimestamps = [];
    this.degradationStartTime = 0;
    this.recoveryStartTime = 0;
  }
  /**
   * 恢復 FPS 樣本收集
   * 當視頻恢復正常播放時調用
   */
  resumeCollection() {
    if (!this.isPaused)
      return;
    this.isPaused = false;
  }
  /**
   * 更新配置
   */
  updateConfig(config) {
    this.config = { ...this.config, ...config };
    if (!this.config.enabled && this.currentLevel > 0) {
      this.currentLevel = 0;
      console.log("[AdaptiveFps] Disabled - reset to full FPS");
    }
  }
  /**
   * 獲取效能統計摘要（用於 debug/監控）
   */
  getSummary() {
    const stats = this.getStats();
    const screenLimitedNote = stats.isScreenLimited ? ` (capped by ${stats.screenRefreshRate}Hz screen)` : "";
    return `Native: ${stats.nativeFps.toFixed(1)}fps | Target: ${stats.targetFps.toFixed(1)}fps${screenLimitedNote} | Effective: ${stats.effectiveFps.toFixed(1)}fps | Actual: ${stats.actualFps.toFixed(1)}fps | Level: ${stats.degradationLevel}`;
  }
  // ===== Private Methods =====
  calculateActualFps() {
    if (this.frameTimestamps.length < 2) {
      this.actualFps = 0;
      return;
    }
    const first = this.frameTimestamps[0];
    const last = this.frameTimestamps[this.frameTimestamps.length - 1];
    const duration = last - first;
    if (duration > 0) {
      this.actualFps = (this.frameTimestamps.length - 1) / duration * 1e3;
    }
  }
  evaluateDegradation(timestamp) {
    if (this.frameTimestamps.length < Math.min(10, this.config.sampleWindowSize)) {
      return;
    }
    const effectiveFps = this.getEffectiveFps();
    const performanceRatio = this.actualFps / effectiveFps;
    if (performanceRatio < this.config.degradationThreshold) {
      if (this.degradationStartTime === 0) {
        this.degradationStartTime = timestamp;
      }
      this.recoveryStartTime = 0;
      if (timestamp - this.degradationStartTime >= this.config.stabilityDuration) {
        this.performDegradation();
        this.degradationStartTime = 0;
      }
    } else if (performanceRatio >= this.config.recoveryThreshold && this.currentLevel > 0) {
      if (this.recoveryStartTime === 0) {
        this.recoveryStartTime = timestamp;
      }
      this.degradationStartTime = 0;
      if (timestamp - this.recoveryStartTime >= this.config.stabilityDuration) {
        this.performRecovery();
        this.recoveryStartTime = 0;
      }
    } else {
      this.degradationStartTime = 0;
      if (performanceRatio < this.config.recoveryThreshold) {
        this.recoveryStartTime = 0;
      }
    }
  }
  performDegradation() {
    if (this.currentLevel >= DEGRADATION_LEVELS.length - 1) {
      console.log(`[AdaptiveFps] Already at minimum level (${this.currentLevel})`);
      return;
    }
    const oldLevel = this.currentLevel;
    this.currentLevel++;
    const stats = this.getStats();
    const reason = `Actual FPS (${this.actualFps.toFixed(1)}) < threshold (${(stats.effectiveFps * this.config.degradationThreshold).toFixed(1)})`;
    console.log(`[AdaptiveFps] Degraded: level ${oldLevel} → ${this.currentLevel} | ${stats.nativeFps}fps → ${stats.effectiveFps.toFixed(1)}fps | ${reason}`);
    this.frameTimestamps = [];
    if (this.onDegradation) {
      this.onDegradation(stats, reason);
    }
  }
  performRecovery() {
    if (this.currentLevel <= 0) {
      return;
    }
    const oldLevel = this.currentLevel;
    this.currentLevel--;
    const stats = this.getStats();
    const reason = `Actual FPS (${this.actualFps.toFixed(1)}) reached recovery threshold`;
    console.log(`[AdaptiveFps] Recovered: level ${oldLevel} → ${this.currentLevel} | Effective FPS now ${stats.effectiveFps.toFixed(1)}`);
    this.frameTimestamps = [];
    if (this.onRecovery) {
      this.onRecovery(stats, reason);
    }
  }
}
const darkTheme = {
  name: "dark",
  variables: {
    "primary-color": "#3b82f6",
    "primary-hover-color": "#2563eb",
    "controls-bg": "rgba(0, 0, 0, 0.8)",
    "menu-bg": "rgba(0, 0, 0, 0.9)",
    "tooltip-bg": "rgba(0, 0, 0, 0.9)",
    "text-color": "#ffffff",
    "text-secondary-color": "rgba(255, 255, 255, 0.7)",
    "progress-bg": "rgba(255, 255, 255, 0.2)",
    "progress-buffered-color": "rgba(255, 255, 255, 0.4)",
    "progress-played-color": "#3b82f6",
    "button-color": "#ffffff",
    "button-hover-color": "#3b82f6",
    "button-active-color": "#2563eb"
  }
};
const lightTheme = {
  name: "light",
  variables: {
    "primary-color": "#2563eb",
    "primary-hover-color": "#1d4ed8",
    "controls-bg": "rgba(255, 255, 255, 0.95)",
    "menu-bg": "rgba(255, 255, 255, 0.98)",
    "tooltip-bg": "rgba(0, 0, 0, 0.8)",
    "text-color": "#1f2937",
    "text-secondary-color": "rgba(31, 41, 55, 0.7)",
    "progress-bg": "rgba(0, 0, 0, 0.1)",
    "progress-buffered-color": "rgba(0, 0, 0, 0.2)",
    "progress-played-color": "#2563eb",
    "button-color": "#1f2937",
    "button-hover-color": "#2563eb",
    "button-active-color": "#1d4ed8"
  }
};
const CSS_VAR_PREFIX = "--vp-";
class ThemeManager {
  constructor(shadowRoot) {
    this.shadowRoot = null;
    this.currentTheme = null;
    this.registeredThemes = /* @__PURE__ */ new Map();
    this.styleElement = null;
    this.shadowRoot = shadowRoot ?? null;
    this.registerTheme(darkTheme);
    this.registerTheme(lightTheme);
  }
  /**
   * 設置 Shadow Root（如果構造時未提供）
   */
  setShadowRoot(shadowRoot) {
    this.shadowRoot = shadowRoot;
  }
  /**
   * 確保樣式元素存在
   */
  ensureStyleElement() {
    if (this.styleElement) {
      return this.styleElement;
    }
    this.styleElement = document.createElement("style");
    this.styleElement.setAttribute("data-theme", "custom");
    if (this.shadowRoot) {
      this.shadowRoot.appendChild(this.styleElement);
    } else {
      document.head.appendChild(this.styleElement);
    }
    return this.styleElement;
  }
  /**
   * 生成 CSS 變數字符串
   */
  generateCSSVariables(variables) {
    const lines = [];
    for (const [key, value] of Object.entries(variables)) {
      if (value === void 0)
        continue;
      const varName = key.startsWith("--") ? key : `${CSS_VAR_PREFIX}${key}`;
      lines.push(`  ${varName}: ${value};`);
    }
    return `:host {
${lines.join("\n")}
}`;
  }
  apply(theme) {
    let themeObj;
    if (typeof theme === "string") {
      const registered = this.registeredThemes.get(theme);
      if (!registered) {
        console.warn(`[ThemeManager] Theme "${theme}" not found`);
        return;
      }
      themeObj = registered;
    } else {
      themeObj = theme;
    }
    const styleEl = this.ensureStyleElement();
    styleEl.textContent = this.generateCSSVariables(themeObj.variables);
    this.currentTheme = themeObj.name;
    console.log(`[ThemeManager] Applied theme: ${themeObj.name}`);
  }
  setVariable(name, value) {
    const target = this.shadowRoot ?? document.documentElement;
    const varName = name.startsWith("--") ? name : `${CSS_VAR_PREFIX}${name}`;
    target.style.setProperty(varName, value);
  }
  getCurrentTheme() {
    return this.currentTheme;
  }
  reset() {
    if (this.styleElement) {
      this.styleElement.textContent = "";
    }
    this.currentTheme = null;
  }
  registerTheme(theme) {
    this.registeredThemes.set(theme.name, theme);
  }
  getThemeNames() {
    return [...this.registeredThemes.keys()];
  }
  /**
   * 獲取已註冊的主題
   */
  getTheme(name) {
    return this.registeredThemes.get(name);
  }
  /**
   * 銷毀主題管理器
   */
  destroy() {
    if (this.styleElement) {
      this.styleElement.remove();
      this.styleElement = null;
    }
    this.currentTheme = null;
  }
}
const MEDIA_EVENTS = [
  // 載入相關事件
  "loadstart",
  "progress",
  "suspend",
  "abort",
  "error",
  "emptied",
  "stalled",
  // 元數據相關事件
  "loadedmetadata",
  "loadeddata",
  "durationchange",
  // 就緒相關事件
  "canplay",
  "canplaythrough",
  // 播放相關事件
  "play",
  "playing",
  "pause",
  "ended",
  "waiting",
  // 搜尋相關事件
  "seeking",
  "seeked",
  "timeupdate",
  // 參數相關事件
  "volumechange",
  "ratechange",
  // 尺寸相關事件
  "resize"
];
function applyMediaEventHandlers(target) {
  for (const event of MEDIA_EVENTS) {
    const propName = event === "error" ? "onmediaerror" : `on${event}`;
    Object.defineProperty(target, propName, {
      get() {
        return this.eventManager.getEventHandler(event);
      },
      set(handler) {
        this.eventManager.setEventHandler(event, handler);
      },
      enumerable: true,
      configurable: true
    });
  }
}
let videoPlayerModule = null;
let moduleLoadError = null;
function getPossibleModulePaths() {
  const paths = [];
  if (typeof process === "undefined" || typeof require === "undefined") {
    console.warn("[NativeController] Not in Node.js environment");
    return paths;
  }
  const envPath = process.env.MPV_NATIVE_MODULE_PATH;
  if (envPath) {
    console.log("[NativeController] Using path from MPV_NATIVE_MODULE_PATH:", envPath);
    paths.push(envPath);
    return paths;
  }
  const path = require("path");
  const moduleName = "video_player.node";
  const nativeSubPath = path.join("native", "video-player", "build", "Release", moduleName);
  const appRoot = require("app-root-path");
  const appRootPath = appRoot.path;
  paths.push(
    path.join(appRootPath, "node_modules", "mpv-video-player", nativeSubPath),
    path.join(appRootPath, "node_modules", "@eagle-app", "mpv-video-player", nativeSubPath)
  );
  const isElectron = !!(process.versions && process.versions.electron);
  if (!isElectron) {
    return paths;
  }
  const resourcesPath = process.resourcesPath;
  const isPackaged = !!resourcesPath && !resourcesPath.includes("node_modules");
  if (isPackaged && resourcesPath) {
    paths.push(
      path.join(resourcesPath, "app.asar.unpacked", nativeSubPath),
      path.join(resourcesPath, "native-addon", moduleName)
    );
  } else {
    const cwd = process.cwd();
    paths.push(
      path.join(cwd, "..", "mpv-player", nativeSubPath),
      path.join(cwd, "packages", "mpv-player", nativeSubPath)
    );
  }
  return paths;
}
function loadNativeModule() {
  if (videoPlayerModule) {
    return videoPlayerModule;
  }
  if (moduleLoadError) {
    throw moduleLoadError;
  }
  const possiblePaths = getPossibleModulePaths();
  console.log("[NativeController] Looking for native module...");
  for (const modulePath of possiblePaths) {
    try {
      console.log(`[NativeController]   Trying: ${modulePath}`);
      videoPlayerModule = require(modulePath);
      console.log(`[NativeController]   SUCCESS: Loaded from ${modulePath}`);
      return videoPlayerModule;
    } catch (err) {
      console.log(`[NativeController]   FAILED: ${err.message}`);
      continue;
    }
  }
  moduleLoadError = new Error("Native module not found. Tried paths:\n" + possiblePaths.join("\n"));
  throw moduleLoadError;
}
function isNativeModuleAvailable() {
  try {
    loadNativeModule();
    return true;
  } catch {
    return false;
  }
}
class NativeController {
  /**
   * 創建 NativeController 實例
   * @param options.hwdec - 硬體解碼模式（省略則使用 C++ 平台預設值）
   * @param options.thumbnailMode - 縮圖模式：預先套用 scale=320px + fps=24 濾鏡
   */
  constructor(options) {
    this.controller = null;
    this.eventCallbacks = /* @__PURE__ */ new Set();
    this.currentVideoPath = null;
    this._muted = false;
    this._previousVolume = 100;
    this._loop = false;
    this._destroyed = false;
    try {
      const module2 = loadNativeModule();
      const mpvOptions = {
        thumbnailMode: options?.thumbnailMode ?? false
      };
      if (options?.hwdec !== void 0) {
        mpvOptions.hwdec = options.hwdec;
      }
      this.controller = new module2.MpvController(mpvOptions);
      this.controller.onEvent((event) => {
        this.handleEvent(event);
      });
      console.log("[NativeController] Instance created");
    } catch (error) {
      console.error("[NativeController] Failed to create instance:", error);
      throw error;
    }
  }
  /**
   * 處理來自 native 的事件
   */
  handleEvent(event) {
    for (const callback of this.eventCallbacks) {
      try {
        callback(event);
      } catch (error) {
        console.error("[NativeController] Event callback error:", error);
      }
    }
  }
  /**
   * 訂閱事件
   */
  onEvent(callback) {
    this.eventCallbacks.add(callback);
    return () => {
      this.eventCallbacks.delete(callback);
    };
  }
  // ========================================================================
  // 播放控制
  // ========================================================================
  loadFile(filePath) {
    if (!this.controller || this._destroyed)
      return false;
    const cleanPath = NativeController.stripFileUrlQuery(filePath);
    const success = this.controller.loadFile(cleanPath);
    if (success) {
      this.currentVideoPath = cleanPath;
    }
    return success;
  }
  /**
   * Strip query parameters from file:// URLs to avoid playback failures.
   * Non-file URLs and plain paths are returned as-is.
   */
  static stripFileUrlQuery(filePath) {
    if (filePath.startsWith("file:///")) {
      const qIndex = filePath.indexOf("?");
      if (qIndex !== -1) {
        return filePath.slice(0, qIndex);
      }
    }
    return filePath;
  }
  play() {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.play();
  }
  pause() {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.pause();
  }
  stop() {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.stop();
  }
  seek(position, mode = "absolute") {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.seek(position, mode);
  }
  togglePlay() {
    if (!this.controller || this._destroyed)
      return false;
    if (this.controller.isPaused()) {
      return this.controller.play();
    } else {
      return this.controller.pause();
    }
  }
  // ========================================================================
  // 狀態查詢
  // ========================================================================
  getPosition() {
    if (!this.controller || this._destroyed)
      return 0;
    return this.controller.getPosition();
  }
  getDuration() {
    if (!this.controller || this._destroyed)
      return 0;
    return this.controller.getDuration();
  }
  isPaused() {
    if (!this.controller || this._destroyed)
      return true;
    return this.controller.isPaused();
  }
  isPlaying() {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.isPlaying();
  }
  getState() {
    if (!this.controller || this._destroyed) {
      return { position: 0, duration: 0, paused: true, volume: 0, playing: false };
    }
    return {
      position: this.controller.getPosition(),
      duration: this.controller.getDuration(),
      paused: this.controller.isPaused(),
      volume: this.controller.getVolume(),
      playing: this.controller.isPlaying()
    };
  }
  getVideoSize() {
    if (!this.controller || this._destroyed) {
      return { width: 0, height: 0, rotation: 0, renderWidth: 0, renderHeight: 0, fps: 0, format: "yuv420" };
    }
    return this.controller.getVideoSize();
  }
  // ========================================================================
  // 屬性設置
  // ========================================================================
  setVolume(volume) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.setVolume(volume);
  }
  setSpeed(speed) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.setSpeed(speed);
  }
  setMute(muted) {
    if (!this.controller || this._destroyed)
      return false;
    if (muted && !this._muted) {
      this._previousVolume = this.controller.getVolume();
      this._muted = true;
      return this.controller.setVolume(0);
    } else if (!muted && this._muted) {
      this._muted = false;
      return this.controller.setVolume(this._previousVolume);
    }
    return true;
  }
  getMute() {
    return this._muted;
  }
  setLoop(loop) {
    this._loop = loop;
    return true;
  }
  getLoop() {
    return this._loop;
  }
  setFastSeek(enabled) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.setFastSeek?.(enabled) ?? true;
  }
  /**
   * 設置目標渲染分辨率
   *
   * 這會透過 mpv 的 vf scale 濾鏡在解碼時縮放視頻，
   * 比在渲染後縮放更高效（減少 glReadPixels 數據量）。
   *
   * 與性能監控的 throughput_scale 協同工作：
   * - 最終尺寸 = min(target, video × throughput_scale, video)
   * - 永遠不放大，最大就是視頻原始尺寸
   *
   * @param width 目標寬度
   * @param height 目標高度
   */
  setTargetResolution(width, height) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.setTargetResolution?.(width, height) ?? false;
  }
  // ========================================================================
  // 幀渲染（核心功能）
  // ========================================================================
  /**
   * 渲染一幀並返回數據
   *
   * @param targetWidth 目標寬度（用於 GPU 縮放）
   * @param targetHeight 目標高度（用於 GPU 縮放）
   * @returns 幀數據或 null
   */
  renderFrame(targetWidth, targetHeight) {
    if (!this.controller || this._destroyed)
      return null;
    const buffer = this.controller.renderFrame(targetWidth, targetHeight);
    if (!buffer)
      return null;
    const size = this.controller.getVideoSize();
    const width = size.renderWidth || size.width;
    const height = size.renderHeight || size.height;
    if (width <= 0 || height <= 0)
      return null;
    const rgbaExpected = width * height * 4;
    const yuvExpected = width * height + Math.floor(width / 2) * Math.floor(height / 2) * 2;
    const bufferSize = buffer.byteLength;
    let format;
    if (bufferSize === rgbaExpected) {
      format = "rgba";
    } else if (bufferSize === yuvExpected) {
      format = "yuv420";
    } else {
      return null;
    }
    return {
      buffer,
      width,
      height,
      rotation: size.rotation,
      format
    };
  }
  /**
   * 獲取幀元數據（不渲染新幀）
   */
  getFrameMetadata() {
    if (!this.controller || this._destroyed)
      return null;
    const size = this.controller.getVideoSize();
    if (size.width <= 0 || size.height <= 0)
      return null;
    return {
      width: size.renderWidth || size.width,
      height: size.renderHeight || size.height,
      rotation: size.rotation,
      format: size.format
    };
  }
  // ========================================================================
  // 元數據和 HDR
  // ========================================================================
  getMetadata() {
    if (!this.controller || this._destroyed)
      return null;
    const size = this.controller.getVideoSize();
    const nativeMeta = this.controller.getMetadata?.();
    return {
      duration: this.controller.getDuration(),
      width: nativeMeta?.width ?? size.width,
      height: nativeMeta?.height ?? size.height,
      rotation: nativeMeta?.rotation ?? size.rotation,
      fps: nativeMeta?.fps ?? size.fps,
      format: nativeMeta?.format ?? "",
      videoCodec: nativeMeta?.videoCodec ?? "",
      audioCodec: nativeMeta?.audioCodec ?? "",
      bitrate: nativeMeta?.bitrate ?? 0,
      sampleRate: nativeMeta?.sampleRate ?? 0,
      channels: nativeMeta?.channels ?? 0,
      isHdr: nativeMeta?.isHdr ?? false,
      hdrPrimaries: nativeMeta?.hdrPrimaries ?? "",
      hdrTransfer: nativeMeta?.hdrTransfer ?? "",
      hdrMaxLuminance: nativeMeta?.hdrMaxLuminance ?? 0
    };
  }
  getHdrInfo() {
    if (!this.controller || this._destroyed)
      return null;
    return this.controller.getHdrInfo?.() ?? null;
  }
  setHdrMode(enabled) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.setHdrMode?.(enabled) ?? false;
  }
  // ========================================================================
  // 其他功能
  // ========================================================================
  checkFileSupport(filePath) {
    if (!this.controller || this._destroyed) {
      return { supported: false, hasVideo: false, hasAudio: false, error: "Controller destroyed" };
    }
    return this.controller.checkFileSupport(filePath);
  }
  requestScreenshot(time) {
    if (!this.controller || this._destroyed)
      return false;
    return this.controller.getScreenshotAt?.(time) ?? false;
  }
  getCurrentVideoPath() {
    return this.currentVideoPath;
  }
  isDestroyed() {
    return this._destroyed;
  }
  // ========================================================================
  // 清理
  // ========================================================================
  destroy() {
    if (this._destroyed)
      return;
    this._destroyed = true;
    this.eventCallbacks.clear();
    if (this.controller) {
      try {
        this.controller.destroy();
      } catch (error) {
        console.error("[NativeController] Error during destroy:", error);
      }
      this.controller = null;
    }
    console.log("[NativeController] Instance destroyed");
  }
}
const _InstanceManager = class _InstanceManager2 {
  constructor(config) {
    this.controller = null;
    this.controllerInitPromise = null;
    this.eventUnsubscribe = null;
    this.disconnectCleanupTimer = null;
    this.wasPlayingBeforeDisconnect = false;
    this._scrubbing = false;
    this.instanceId = config.instanceId;
    this.config = config;
    this.currentHwdec = config.instanceOptions?.hwdec;
    _InstanceManager2.allInstances.add(this);
    _InstanceManager2.registerBeforeUnload();
  }
  /**
   * 檢查直連模式是否可用
   */
  static isAvailable() {
    return isNativeModuleAvailable();
  }
  /**
   * 註冊 beforeunload 監聽器
   */
  static registerBeforeUnload() {
    if (this.beforeUnloadRegistered)
      return;
    this.beforeUnloadRegistered = true;
    window.addEventListener("beforeunload", () => {
      console.log(`[InstanceManager] beforeunload: destroying ${this.allInstances.size} instances`);
      const instances = Array.from(this.allInstances);
      for (const instance of instances) {
        try {
          instance.destroySync();
        } catch (error) {
          console.error("[InstanceManager] Failed to destroy instance on beforeunload:", error);
        }
      }
    });
    console.log("[InstanceManager] beforeunload handler registered");
  }
  /**
   * 更新配置
   */
  updateConfig(config) {
    this.config = { ...this.config, ...config };
    if (config.onVideoEvent && this.controller) {
      this.unsubscribeEvents();
      this.subscribeEvents();
    }
  }
  /**
   * 獲取實例 ID
   */
  getInstanceId() {
    return this.instanceId;
  }
  /**
   * 檢查實例是否已創建
   */
  isCreated() {
    return this.controller !== null && !this.controller.isDestroyed();
  }
  /**
   * 確保實例存在（懶加載）
   */
  async ensureInstance() {
    if (this.controller && !this.controller.isDestroyed()) {
      return true;
    }
    if (this.controllerInitPromise) {
      return this.controllerInitPromise;
    }
    this.controllerInitPromise = this.createControllerInternal();
    return this.controllerInitPromise;
  }
  /**
   * 獲取當前 hwdec 設定
   */
  getCurrentHwdec() {
    return this.currentHwdec;
  }
  /**
   * 檢查是否需要重建
   */
  needsRecreate(newHwdec) {
    return this.isCreated() && newHwdec !== this.currentHwdec;
  }
  /**
   * 重建實例
   */
  async recreate(options) {
    if (this.controller) {
      await this.destroy();
    }
    if (options?.hwdec !== void 0) {
      this.currentHwdec = options.hwdec;
    }
    return this.ensureInstance();
  }
  /**
   * 內部方法：創建控制器
   */
  async createControllerInternal() {
    try {
      this.controller = new NativeController({
        hwdec: this.currentHwdec
      });
      this.controller.setFastSeek(false);
      this.subscribeEvents();
      console.log(`[InstanceManager] Instance created: ${this.instanceId} (hwdec: ${this.currentHwdec})`);
      return true;
    } catch (error) {
      console.error(`[InstanceManager] Failed to create instance:`, error);
      this.controllerInitPromise = null;
      return false;
    }
  }
  /**
   * 訂閱事件
   */
  subscribeEvents() {
    if (!this.controller || !this.config.onVideoEvent)
      return;
    this.eventUnsubscribe = this.controller.onEvent(this.config.onVideoEvent);
  }
  /**
   * 取消事件訂閱
   */
  unsubscribeEvents() {
    if (this.eventUnsubscribe) {
      this.eventUnsubscribe();
      this.eventUnsubscribe = null;
    }
  }
  /**
   * 銷毀實例
   */
  async destroy() {
    _InstanceManager2.allInstances.delete(this);
    this.cancelScheduledCleanup();
    this.unsubscribeEvents();
    if (this.controller) {
      this.controller.destroy();
      this.controller = null;
    }
    this.controllerInitPromise = null;
    console.log(`[InstanceManager] Instance destroyed: ${this.instanceId}`);
  }
  /**
   * 同步銷毀
   */
  destroySync() {
    _InstanceManager2.allInstances.delete(this);
    this.cancelScheduledCleanup();
    this.unsubscribeEvents();
    if (this.controller) {
      this.controller.destroy();
      this.controller = null;
    }
    this.controllerInitPromise = null;
  }
  // ===== 斷開連接生命週期管理 =====
  handleDisconnect(wasPaused) {
    if (!this.controller)
      return;
    this.wasPlayingBeforeDisconnect = !wasPaused;
    if (!wasPaused) {
      this.controller.pause();
    }
    this.scheduleCleanup();
    console.log(`[InstanceManager] Disconnected, cleanup scheduled: ${this.instanceId}`);
  }
  handleReconnect() {
    const wasCancelled = this.cancelScheduledCleanup();
    if (wasCancelled) {
      console.log(`[InstanceManager] Reconnected before cleanup: ${this.instanceId}`);
    }
    const shouldResume = this.wasPlayingBeforeDisconnect;
    this.wasPlayingBeforeDisconnect = false;
    return shouldResume;
  }
  handleDisconnectImmediate(wasPaused) {
    if (!this.controller)
      return;
    this.wasPlayingBeforeDisconnect = !wasPaused;
    this.unsubscribeEvents();
    if (!wasPaused) {
      this.controller.pause();
    }
    this.scheduleCleanup();
    console.log(`[InstanceManager] Disconnected immediately: ${this.instanceId}`);
  }
  scheduleCleanup() {
    if (this.disconnectCleanupTimer)
      return;
    this.disconnectCleanupTimer = setTimeout(() => {
      this.disconnectCleanupTimer = null;
      if (this.controller) {
        console.log(`[InstanceManager] Disconnect cleanup timeout: ${this.instanceId}`);
        this.destroy();
      }
    }, _InstanceManager2.DISCONNECT_CLEANUP_DELAY_MS);
  }
  cancelScheduledCleanup() {
    if (this.disconnectCleanupTimer) {
      clearTimeout(this.disconnectCleanupTimer);
      this.disconnectCleanupTimer = null;
      return true;
    }
    return false;
  }
  // ===== 視頻控制 API（直接調用，不走 IPC）=====
  load(src) {
    if (!this.controller)
      return false;
    return this.controller.loadFile(src);
  }
  play() {
    if (!this.controller)
      return false;
    return this.controller.play();
  }
  pause() {
    if (!this.controller)
      return false;
    return this.controller.pause();
  }
  seek(position, mode = "absolute") {
    if (!this.controller)
      return false;
    return this.controller.seek(position, mode);
  }
  togglePlay() {
    if (!this.controller)
      return false;
    return this.controller.togglePlay();
  }
  getState() {
    if (!this.controller)
      return null;
    return this.controller.getState();
  }
  getPosition() {
    if (!this.controller)
      return 0;
    return this.controller.getPosition();
  }
  getDuration() {
    if (!this.controller)
      return 0;
    return this.controller.getDuration();
  }
  isPaused() {
    if (!this.controller)
      return true;
    return this.controller.isPaused();
  }
  setVolume(volume) {
    if (!this.controller)
      return false;
    return this.controller.setVolume(volume);
  }
  setSpeed(speed) {
    if (!this.controller)
      return false;
    return this.controller.setSpeed(speed);
  }
  setMute(muted) {
    if (!this.controller)
      return false;
    return this.controller.setMute(muted);
  }
  getMute() {
    if (!this.controller)
      return false;
    return this.controller.getMute();
  }
  setLoop(loop) {
    if (!this.controller)
      return false;
    return this.controller.setLoop(loop);
  }
  getLoop() {
    if (!this.controller)
      return false;
    return this.controller.getLoop();
  }
  setFastSeek(enabled) {
    if (!this.controller)
      return false;
    return this.controller.setFastSeek(enabled);
  }
  /**
   * 設置目標渲染分辨率
   *
   * 透過 mpv vf scale 濾鏡在解碼時縮放，比渲染後縮放更高效。
   * 與性能監控的 throughput_scale 協同工作。
   */
  setTargetResolution(width, height) {
    if (!this.controller)
      return false;
    return this.controller.setTargetResolution(width, height);
  }
  // ===== 幀渲染（核心優勢：直接調用，無 IPC）=====
  /**
   * 渲染一幀
   *
   * 這是直連模式的核心優勢：直接在 Renderer 進程調用 native 渲染，
   * 無需 IPC 傳輸幀數據。
   */
  renderFrame(targetWidth, targetHeight) {
    if (!this.controller)
      return null;
    return this.controller.renderFrame(targetWidth, targetHeight);
  }
  /**
   * 獲取幀元數據
   */
  getFrameMetadata() {
    if (!this.controller)
      return null;
    return this.controller.getFrameMetadata();
  }
  /**
   * 獲取視頻尺寸
   */
  getVideoSize() {
    if (!this.controller)
      return null;
    return this.controller.getVideoSize();
  }
  // ===== 元數據和 HDR =====
  getMetadata() {
    if (!this.controller)
      return null;
    return this.controller.getMetadata();
  }
  getHdrInfo() {
    if (!this.controller)
      return null;
    return this.controller.getHdrInfo();
  }
  setHdrMode(enabled) {
    if (!this.controller)
      return false;
    return this.controller.setHdrMode(enabled);
  }
  // ===== 其他 =====
  requestScreenshot(time) {
    if (!this.controller)
      return false;
    return this.controller.requestScreenshot(time);
  }
  /**
   * 訂閱視頻事件
   *
   * 用於截圖等需要監聽特定事件的功能。
   * 返回取消訂閱函數。
   *
   * @param callback 事件回調
   * @returns 取消訂閱函數
   */
  onEvent(callback) {
    if (!this.controller) {
      return () => {
      };
    }
    return this.controller.onEvent(callback);
  }
  checkFileSupport(filePath) {
    if (!this.controller)
      return { supported: false, hasVideo: false, hasAudio: false, error: "No controller" };
    return this.controller.checkFileSupport(filePath);
  }
  setScrubbing(enabled) {
    this._scrubbing = enabled;
  }
  isScrubbing() {
    return this._scrubbing;
  }
  // ===== 兼容 InstanceManager API（返回 Promise 版本）=====
  /**
   * 異步獲取單幀（兼容 InstanceManager 接口）
   * 直連模式下，這等同於 renderFrame
   */
  async getFrame() {
    return this.renderFrame();
  }
  /**
   * 異步獲取 HDR 信息
   */
  async getHdrInfoAsync() {
    return this.getHdrInfo();
  }
  /**
   * 異步設置 HDR 模式
   */
  async setHdrModeAsync(enabled) {
    return this.setHdrMode(enabled);
  }
  /**
   * 異步載入視頻
   */
  async loadAsync(src) {
    return this.load(src);
  }
  /**
   * 異步播放
   */
  async playAsync() {
    return this.play();
  }
  /**
   * 異步暫停
   */
  async pauseAsync() {
    return this.pause();
  }
  /**
   * 異步 seek
   */
  async seekAsync(position, mode = "absolute") {
    this.seek(position, mode);
  }
  /**
   * 異步獲取狀態
   */
  async getStateAsync() {
    return this.getState();
  }
  /**
   * 異步獲取元數據
   */
  async getMetadataAsync() {
    const meta = this.getMetadata();
    if (!meta)
      return null;
    return {
      fps: meta.fps,
      width: meta.width,
      height: meta.height,
      isHdr: meta.isHdr,
      hdrPrimaries: meta.hdrPrimaries,
      hdrTransfer: meta.hdrTransfer,
      hdrMaxLuminance: meta.hdrMaxLuminance,
      videoCodec: meta.videoCodec,
      audioCodec: meta.audioCodec,
      format: meta.format,
      bitrate: meta.bitrate,
      rotation: meta.rotation,
      sampleRate: meta.sampleRate,
      channels: meta.channels
    };
  }
  // ===== 幀推送控制（兼容 API，直連模式不使用）=====
  async startFramePush(_targetResolution) {
    return true;
  }
  async stopFramePush() {
  }
  async updateFramePushResolution(_resolution) {
  }
};
_InstanceManager.DISCONNECT_CLEANUP_DELAY_MS = 5e3;
_InstanceManager.allInstances = /* @__PURE__ */ new Set();
_InstanceManager.beforeUnloadRegistered = false;
let InstanceManager = _InstanceManager;
function generateInstanceId() {
  return `mpv-native-${Date.now()}-${Math.random().toString(36).substring(2, 11)}`;
}
const _RenderManager = class _RenderManager2 {
  constructor(config) {
    this.animationFrameId = null;
    this.renderActive = false;
    this.lastTargetResolution = "";
    this.frameQueue = [];
    this.videoWidth = 0;
    this.videoHeight = 0;
    this.currentFormat = "yuv420";
    this.lastFrameTime = 0;
    this.lastRenderTime = 0;
    this.lastResolutionCheckTime = 0;
    this.currentLoadId = 0;
    this.targetResolutionDebounceTimer = null;
    this.pendingTargetResolution = null;
    this.config = config;
  }
  /**
   * 更新配置
   */
  updateConfig(config) {
    this.config = { ...this.config, ...config };
  }
  /**
   * 計算目標解析度
   */
  getTargetResolution() {
    const size = this.config.getElementSize();
    if (!size || size.width <= 0 || size.height <= 0) {
      return void 0;
    }
    const dpr = window.devicePixelRatio || 1;
    const result = {
      width: Math.ceil(size.width * dpr),
      height: Math.ceil(size.height * dpr)
    };
    return result;
  }
  /**
   * 設置目標分辨率（帶防抖）
   * @param width 目標寬度
   * @param height 目標高度
   * @param immediate 是否立即執行（跳過防抖）
   */
  setTargetResolutionDebounced(width, height, immediate = false) {
    const newTarget = `${width}x${height}`;
    if (newTarget === this.lastTargetResolution) {
      return;
    }
    this.lastTargetResolution = newTarget;
    this.pendingTargetResolution = { width, height };
    if (immediate) {
      this.applyPendingTargetResolution();
      return;
    }
    if (this.targetResolutionDebounceTimer) {
      clearTimeout(this.targetResolutionDebounceTimer);
    }
    this.targetResolutionDebounceTimer = setTimeout(() => {
      this.applyPendingTargetResolution();
    }, _RenderManager2.TARGET_RESOLUTION_DEBOUNCE_MS);
  }
  /**
   * 應用待處理的目標分辨率
   */
  applyPendingTargetResolution() {
    if (!this.pendingTargetResolution) {
      return;
    }
    const { width, height } = this.pendingTargetResolution;
    this.config.instanceManager.setTargetResolution(width, height);
    this.pendingTargetResolution = null;
    this.targetResolutionDebounceTimer = null;
  }
  /**
   * 啟動渲染循環
   */
  async startRenderLoop() {
    if (this.animationFrameId !== null)
      return;
    const loadId = ++this.currentLoadId;
    const targetResolution = this.getTargetResolution();
    this.renderActive = true;
    this.frameQueue.length = 0;
    if (targetResolution) {
      this.setTargetResolutionDebounced(
        targetResolution.width,
        targetResolution.height,
        true
        // immediate
      );
      console.log(`[RenderManager] Starting, target: ${this.lastTargetResolution}`);
    } else {
      console.log("[RenderManager] Starting");
    }
    this.lastRenderTime = 0;
    this.lastResolutionCheckTime = 0;
    const renderLoop = (timestamp) => {
      const isConnected = this.config.isConnected?.() ?? true;
      if (!this.renderActive || loadId !== this.currentLoadId || !isConnected)
        return;
      this.animationFrameId = requestAnimationFrame(renderLoop);
      this.renderFrame(timestamp);
    };
    this.animationFrameId = requestAnimationFrame(renderLoop);
  }
  /**
   * 渲染單幀
   */
  renderFrame(timestamp) {
    const state = this.config.getPlaybackState();
    const frameInterval = this.config.fpsController.getEffectiveFrameInterval();
    const timeSinceLastRender = timestamp - this.lastRenderTime;
    let targetResolution = this.getTargetResolution();
    if (state.scrubbing && targetResolution) {
      targetResolution = {
        width: Math.min(targetResolution.width, _RenderManager2.SCRUBBING_MAX_WIDTH),
        height: Math.min(targetResolution.height, _RenderManager2.SCRUBBING_MAX_HEIGHT)
      };
    }
    if (targetResolution) {
      this.setTargetResolutionDebounced(
        targetResolution.width,
        targetResolution.height,
        state.scrubbing
        // scrubbing 時 immediate
      );
    }
    let shouldRender;
    if (state.seeking || state.scrubbing) {
      shouldRender = true;
    } else if (state.paused) {
      shouldRender = timeSinceLastRender >= _RenderManager2.PAUSED_INTERVAL;
    } else {
      shouldRender = timeSinceLastRender >= frameInterval;
    }
    if (!shouldRender)
      return;
    if (state.scrubbing && this.frameQueue.length > 0) {
      const maxFrames = Math.min(3, this.frameQueue.length);
      for (let i = 0; i < maxFrames; i++) {
        const frame2 = this.frameQueue.shift();
        this.renderFrameToWebGL(frame2, timestamp, state.paused, state.seeking);
      }
      return;
    }
    const frame = this.config.instanceManager.renderFrame();
    if (!frame)
      return;
    this.renderFrameToWebGL(frame, timestamp, state.paused, state.seeking);
    if (timestamp - this.lastResolutionCheckTime >= _RenderManager2.RESOLUTION_CHECK_INTERVAL) {
      this.lastResolutionCheckTime = timestamp;
      this.checkStalled(timestamp, state.paused);
    }
  }
  /**
   * 渲染幀到 WebGL
   */
  renderFrameToWebGL(frame, timestamp, paused, seeking) {
    this.lastRenderTime = timestamp;
    if (!paused && !seeking) {
      this.config.fpsController.recordFrame(performance.now());
    }
    this.lastFrameTime = timestamp;
    if (this.videoWidth !== frame.width || this.videoHeight !== frame.height) {
      this.videoWidth = frame.width;
      this.videoHeight = frame.height;
      this.config.onVideoSizeChange?.(frame.width, frame.height);
    }
    this.currentFormat = frame.format;
    this.config.renderer.updateFrameFromBuffer(
      frame.buffer,
      frame.width,
      frame.height,
      frame.rotation,
      frame.format
    );
  }
  /**
   * 檢查 stalled 狀態
   */
  checkStalled(timestamp, paused) {
    if (!paused && this.lastFrameTime > 0 && timestamp - this.lastFrameTime > _RenderManager2.STALLED_THRESHOLD) {
      this.config.onStalled?.();
    }
  }
  /**
   * 停止渲染循環
   */
  async stopRenderLoop() {
    this.currentLoadId++;
    if (this.animationFrameId !== null) {
      cancelAnimationFrame(this.animationFrameId);
      this.animationFrameId = null;
    }
    if (this.targetResolutionDebounceTimer) {
      clearTimeout(this.targetResolutionDebounceTimer);
      this.targetResolutionDebounceTimer = null;
      this.pendingTargetResolution = null;
    }
    if (this.renderActive) {
      this.renderActive = false;
      this.frameQueue.length = 0;
      console.log("[RenderManager] Stopped");
    }
  }
  /**
   * 重置渲染狀態
   */
  reset() {
    this.currentLoadId++;
    this.videoWidth = 0;
    this.videoHeight = 0;
    this.lastFrameTime = 0;
    this.frameQueue.length = 0;
    this.lastTargetResolution = "";
    if (this.targetResolutionDebounceTimer) {
      clearTimeout(this.targetResolutionDebounceTimer);
      this.targetResolutionDebounceTimer = null;
      this.pendingTargetResolution = null;
    }
  }
  /**
   * 獲取當前視頻尺寸
   */
  getVideoSize() {
    return { width: this.videoWidth, height: this.videoHeight };
  }
  /**
   * 獲取當前格式
   */
  getCurrentFormat() {
    return this.currentFormat;
  }
  /**
   * 是否正在渲染
   */
  isRendering() {
    return this.renderActive;
  }
  /**
   * 清除緩存
   */
  clearCache() {
    this.frameQueue.length = 0;
  }
  /**
   * 清空幀隊列
   */
  clearFrameQueue() {
    this.frameQueue.length = 0;
  }
  /**
   * 獲取幀隊列長度
   */
  getFrameQueueLength() {
    return this.frameQueue.length;
  }
  /**
   * Seek 後渲染幀（用於 scrubbing 時緩衝或暫停時預覽）
   *
   * - 渲染循環運行中：推送到 Queue（rAF 消費，最多 3 幀追趕）
   * - 渲染循環停止：直接渲染到 WebGL（暫停時 seek 預覽）
   *
   * @param useLowRes 是否使用低分辨率（scrubbing 用）
   * @param waitForFrame 是否等待幀就緒（精準 seek 結束時用）
   */
  pollFrameAfterSeek(useLowRes = false, waitForFrame = false) {
    let targetResolution = this.getTargetResolution();
    if (useLowRes && targetResolution) {
      targetResolution = {
        width: Math.min(targetResolution.width, _RenderManager2.SCRUBBING_MAX_WIDTH),
        height: Math.min(targetResolution.height, _RenderManager2.SCRUBBING_MAX_HEIGHT)
      };
    }
    if (targetResolution) {
      this.setTargetResolutionDebounced(
        targetResolution.width,
        targetResolution.height,
        true
        // seek 時 immediate
      );
    }
    let frame = this.config.instanceManager.renderFrame();
    if (!frame && waitForFrame) {
      let retries = 0;
      const maxRetries = 10;
      const retryInterval = 16;
      const tryGetFrame = () => {
        frame = this.config.instanceManager.renderFrame();
        if (frame && frame.width > 0 && frame.height > 0) {
          this.renderFrameToWebGL(frame, performance.now(), true, true);
        } else if (retries < maxRetries) {
          retries++;
          setTimeout(tryGetFrame, retryInterval);
        }
      };
      setTimeout(tryGetFrame, retryInterval);
      return;
    }
    if (frame && frame.width > 0 && frame.height > 0) {
      if (this.renderActive) {
        this.frameQueue.push({
          buffer: frame.buffer,
          width: frame.width,
          height: frame.height,
          rotation: frame.rotation,
          format: frame.format
        });
        while (this.frameQueue.length > _RenderManager2.MAX_FRAME_QUEUE_SIZE) {
          this.frameQueue.shift();
        }
      } else {
        this.renderFrameToWebGL(frame, performance.now(), true, true);
      }
    }
  }
  /**
   * 停止快速輪詢（兼容 API）
   */
  stopFastPoll() {
  }
  /**
   * 渲染單幀（用於初始化第一幀或暫停時預覽）
   * 不啟動渲染循環，直接渲染到 WebGL
   */
  renderSingleFrame() {
    const frame = this.config.instanceManager.renderFrame();
    if (!frame || frame.width <= 0 || frame.height <= 0) {
      return false;
    }
    this.renderFrameToWebGL(frame, performance.now(), true, false);
    return true;
  }
};
_RenderManager.MAX_FRAME_QUEUE_SIZE = 8;
_RenderManager.SCRUBBING_MAX_WIDTH = 1280;
_RenderManager.SCRUBBING_MAX_HEIGHT = 720;
_RenderManager.PAUSED_INTERVAL = 100;
_RenderManager.STALLED_THRESHOLD = 3e3;
_RenderManager.RESOLUTION_CHECK_INTERVAL = 500;
_RenderManager.TARGET_RESOLUTION_DEBOUNCE_MS = 150;
let RenderManager = _RenderManager;
const DEFAULT_FPS = 24;
const _MediaFacade = class _MediaFacade2 {
  constructor(config) {
    this._currentTime = 0;
    this._duration = 0;
    this._paused = true;
    this._volume = 1;
    this._playbackRate = 1;
    this._defaultPlaybackRate = 1;
    this._readyState = ReadyState.HAVE_NOTHING;
    this._networkState = NetworkState.NETWORK_EMPTY;
    this._seeking = false;
    this._currentSrc = "";
    this._videoWidth = 0;
    this._videoHeight = 0;
    this._sourceWidth = 0;
    this._sourceHeight = 0;
    this._fps = DEFAULT_FPS;
    this._videoCodec = "";
    this._audioCodec = "";
    this._containerFormat = "";
    this._bitrate = 0;
    this._rotation = 0;
    this._sampleRate = 0;
    this._channels = 0;
    this._isHdr = false;
    this._hdrPrimaries = "";
    this._hdrTransfer = "";
    this._hdrMaxLuminance = 0;
    this._ended = false;
    this._lastSeekTime = 0;
    this._scrubbing = false;
    this.lastVolume = 1;
    this.syncInterval = null;
    this.skipNextStateSync = false;
    this.currentLoadId = 0;
    this._usingFastSeek = false;
    this._preciseSeekTarget = null;
    this._preciseSeekTimer = null;
    this.pendingScreenshot = null;
    this.config = config;
  }
  // ===== HTMLMediaElement 屬性 Getters =====
  get currentTime() {
    return this._currentTime;
  }
  get duration() {
    return this._duration;
  }
  get paused() {
    return this._paused;
  }
  get volume() {
    return this._volume;
  }
  get playbackRate() {
    return this._playbackRate;
  }
  get defaultPlaybackRate() {
    return this._defaultPlaybackRate;
  }
  get readyState() {
    return this._readyState;
  }
  get networkState() {
    return this._networkState;
  }
  get seeking() {
    return this._seeking;
  }
  get currentSrc() {
    return this._currentSrc;
  }
  get videoWidth() {
    return this._videoWidth;
  }
  get videoHeight() {
    return this._videoHeight;
  }
  get sourceWidth() {
    return this._sourceWidth;
  }
  get sourceHeight() {
    return this._sourceHeight;
  }
  get fps() {
    return this._fps;
  }
  get videoCodec() {
    return this._videoCodec;
  }
  get audioCodec() {
    return this._audioCodec;
  }
  get containerFormat() {
    return this._containerFormat;
  }
  get bitrate() {
    return this._bitrate;
  }
  get rotation() {
    return this._rotation;
  }
  get sampleRate() {
    return this._sampleRate;
  }
  get channels() {
    return this._channels;
  }
  get isHdr() {
    return this._isHdr;
  }
  get hdrPrimaries() {
    return this._hdrPrimaries;
  }
  get hdrTransfer() {
    return this._hdrTransfer;
  }
  get hdrMaxLuminance() {
    return this._hdrMaxLuminance;
  }
  get scrubbing() {
    return this._scrubbing;
  }
  get ended() {
    return this._ended;
  }
  // ===== HTMLMediaElement 屬性 Setters =====
  set currentTime(value) {
    this._currentTime = value;
    this._lastSeekTime = Date.now();
    this.seekTo(value);
  }
  set volume(value) {
    const vol = Math.max(0, Math.min(1, value));
    const oldVol = this._volume;
    this._volume = vol;
    this.config.instanceManager.setVolume(vol * 100);
    if (oldVol !== vol) {
      this.config.eventManager.updateVolume(vol, this.config.getMuted());
    }
  }
  set playbackRate(value) {
    const oldRate = this._playbackRate;
    this._playbackRate = value;
    this.config.instanceManager.setSpeed(value);
    if (oldRate !== value) {
      this.config.eventManager.updatePlaybackRate(value);
    }
  }
  set defaultPlaybackRate(value) {
    this._defaultPlaybackRate = value;
  }
  // ===== 播放控制方法 =====
  async play() {
    try {
      if (this._ended) {
        this._ended = false;
        this.seekTo(0);
      }
      const success = this.config.instanceManager.play();
      if (success) {
        this._paused = false;
        this.skipNextStateSync = true;
        if (this.config.isConnected()) {
          this.config.renderManager.startRenderLoop();
          this.startStateSync();
        }
        this.config.eventManager.play();
        this.config.fpsController.resumeCollection();
      }
    } catch (error) {
      console.error("[MediaFacade] Play error:", error);
      this.config.eventManager.emitError(MediaErrorImpl.MEDIA_ERR_ABORTED, "Play failed");
      throw error;
    }
  }
  pause() {
    this._paused = true;
    const success = this.config.instanceManager.pause();
    if (success) {
      this.skipNextStateSync = true;
      this.config.eventManager.pause();
      this.config.fpsController.pauseCollection();
      this.stopStateSync();
    }
  }
  toggle() {
    if (this._paused) {
      this.play();
    } else {
      this.pause();
    }
  }
  /**
   * 前進 n 幀
   */
  nextFrame(n = 1) {
    const frameDuration = 1 / this._fps;
    const targetTime = Math.min(this._currentTime + n * frameDuration, this._duration);
    this.currentTime = targetTime;
  }
  /**
   * 後退 n 幀
   */
  prevFrame(n = 1) {
    const frameDuration = 1 / this._fps;
    const targetTime = Math.max(this._currentTime - n * frameDuration, 0);
    this.currentTime = targetTime;
  }
  /**
   * 計算解碼壓力，決定是否需要 fast seek
   *
   * 公式：FPS × 面積（width × height）
   * - 720p 60fps = 55M → 不需要
   * - 1080p 30fps = 62M → 不需要
   * - 1080p 60fps = 124M → 需要
   * - 4K 30fps = 248M → 需要
   */
  needsFastSeek() {
    const fps = this._fps;
    const width = this._sourceWidth || this._videoWidth;
    const height = this._sourceHeight || this._videoHeight;
    if (width === 0 || height === 0 || fps <= 0) {
      console.log("[MediaFacade] needsFastSeek: metadata not ready, defaulting to fast seek");
      return true;
    }
    const decodePressure = fps * width * height;
    const needsFast = decodePressure > _MediaFacade2.DECODE_PRESSURE_THRESHOLD;
    console.log(`[MediaFacade] needsFastSeek: ${needsFast} (${width}x${height}@${fps}fps = ${Math.round(decodePressure / 1e6)}M, threshold=${Math.round(_MediaFacade2.DECODE_PRESSURE_THRESHOLD / 1e6)}M)`);
    return needsFast;
  }
  /**
   * 設置 scrubbing 模式（兩階段 Seek 系統）
   *
   * 根據解碼壓力決定 seek 模式：
   * - 低壓力（FPS × 面積 <= 100M）：精準 seek
   * - 高壓力（FPS × 面積 > 100M）：fast seek + 停頓後補精準幀
   */
  setScrubbing(enabled) {
    const wasScrubbing = this._scrubbing;
    this._scrubbing = enabled;
    this.config.instanceManager.setScrubbing(enabled);
    if (enabled && !wasScrubbing) {
      const useFastSeek = this.needsFastSeek();
      if (useFastSeek) {
        this._usingFastSeek = true;
        this.config.instanceManager.setFastSeek(true);
      } else {
        this._usingFastSeek = false;
      }
      if (this.config.isConnected()) {
        this.config.renderManager.startRenderLoop();
      }
    } else if (!enabled && wasScrubbing) {
      if (this._preciseSeekTimer) {
        clearTimeout(this._preciseSeekTimer);
        this._preciseSeekTimer = null;
      }
      if (this._usingFastSeek) {
        if (this._preciseSeekTarget !== null) {
          const target = this._preciseSeekTarget;
          this._preciseSeekTarget = null;
          this.config.instanceManager.setFastSeek(false);
          this.config.instanceManager.seek(target, "absolute");
          this.config.renderManager.pollFrameAfterSeek(false, true);
        } else {
          this.config.instanceManager.setFastSeek(false);
        }
        this._usingFastSeek = false;
      }
    }
  }
  /**
   * 載入視頻
   */
  async load(src, lifecycleSignal) {
    if (!src) {
      this._networkState = NetworkState.NETWORK_EMPTY;
      this._currentSrc = "";
      return false;
    }
    const loadId = ++this.currentLoadId;
    const isCancelled = () => {
      return loadId !== this.currentLoadId || lifecycleSignal?.aborted === true;
    };
    try {
      if (this._networkState === NetworkState.NETWORK_LOADING) {
        this.config.eventManager.abortLoad();
      }
      this.config.renderManager.stopRenderLoop();
      this.stopStateSync();
      this.resetState();
      this.config.eventManager.startLoad();
      this.config.eventManager.updateProgress(0, 100);
      this.setInitialTargetResolution();
      const success = this.config.instanceManager.load(src);
      if (isCancelled())
        return false;
      if (success) {
        this.config.eventManager.updateProgress(50, 100);
        let retries = 0;
        let frameData = this.config.instanceManager.renderFrame();
        while (!frameData && retries < 50) {
          if (isCancelled())
            return false;
          await new Promise((r) => setTimeout(r, 50));
          frameData = this.config.instanceManager.renderFrame();
          retries++;
          this.config.eventManager.updateProgress(50 + retries, 100);
        }
        if (isCancelled())
          return false;
        if (frameData) {
          this._videoWidth = frameData.width;
          this._videoHeight = frameData.height;
        }
        const state = this.config.instanceManager.getState();
        if (isCancelled())
          return false;
        if (state) {
          this._duration = state.duration;
          this._volume = state.volume / 100;
          if (!state.paused) {
            this.config.instanceManager.pause();
          }
          this._paused = true;
        }
        await new Promise((r) => setTimeout(r, 150));
        if (isCancelled())
          return false;
        const metadata = this.config.instanceManager.getMetadata();
        if (metadata) {
          this.applyMetadata(metadata);
        }
        if (isCancelled())
          return false;
        this._readyState = ReadyState.HAVE_METADATA;
        this.config.eventManager.loadedMetadata(this._duration, this._videoWidth, this._videoHeight);
        this._readyState = ReadyState.HAVE_CURRENT_DATA;
        this.config.eventManager.loadedData();
        this._readyState = ReadyState.HAVE_FUTURE_DATA;
        this.config.eventManager.canPlay();
        this._readyState = ReadyState.HAVE_ENOUGH_DATA;
        this._networkState = NetworkState.NETWORK_IDLE;
        this._currentSrc = src;
        this.config.canvas.classList.remove("no-video");
        this.config.eventManager.canPlayThrough();
        if (this.config.getMuted()) {
          this.setMuted(true);
          this.config.eventManager.updateVolume(0, true);
        }
        if (this._defaultPlaybackRate !== 1) {
          this.playbackRate = this._defaultPlaybackRate;
        }
        this.config.instanceManager.setFastSeek(false);
        this.startStateSync();
        if (this.config.isConnected()) {
          this.config.renderManager.renderSingleFrame();
        }
        if (this.config.getAutoplay() && this._paused) {
          this.play();
        }
        return true;
      } else {
        this._networkState = NetworkState.NETWORK_NO_SOURCE;
        this._readyState = ReadyState.HAVE_NOTHING;
        this.config.eventManager.emitError(
          MediaErrorImpl.MEDIA_ERR_SRC_NOT_SUPPORTED,
          "Failed to load video: format not supported or file not found"
        );
        return false;
      }
    } catch (error) {
      if (isCancelled())
        return false;
      console.error("[MediaFacade] Load error:", error);
      this._networkState = NetworkState.NETWORK_NO_SOURCE;
      this._readyState = ReadyState.HAVE_NOTHING;
      this.config.eventManager.emitError(
        MediaErrorImpl.MEDIA_ERR_NETWORK,
        error instanceof Error ? error.message : "Unknown error"
      );
      return false;
    }
  }
  /**
   * 中止載入
   */
  abort() {
    if (this._networkState === NetworkState.NETWORK_LOADING) {
      this.currentLoadId++;
      this._networkState = NetworkState.NETWORK_IDLE;
      this.config.eventManager.abortLoad();
    }
  }
  // ===== 內部方法 =====
  seekTo(position) {
    this._ended = false;
    this._seeking = true;
    this.config.eventManager.startSeek();
    this.config.fpsController.pauseCollection();
    try {
      if (this._scrubbing && this._usingFastSeek) {
        this._preciseSeekTarget = position;
        if (this._preciseSeekTimer) {
          clearTimeout(this._preciseSeekTimer);
        }
        this._preciseSeekTimer = setTimeout(() => {
          this.executePreciseSeek();
        }, _MediaFacade2.PRECISE_SEEK_DELAY_MS);
        this.config.instanceManager.seek(position, "absolute");
        this.config.renderManager.pollFrameAfterSeek(true);
      } else if (this._scrubbing) {
        this.config.instanceManager.seek(position, "absolute");
        this.config.renderManager.pollFrameAfterSeek(true);
      } else {
        this.config.instanceManager.seek(position, "absolute");
        if (this._paused) {
          this.config.renderManager.pollFrameAfterSeek(false);
        }
      }
      this._seeking = false;
      this.config.eventManager.endSeek(position);
      if (!this._paused) {
        this.config.fpsController.resumeCollection();
      }
    } catch (error) {
      this._seeking = false;
      console.error("[MediaFacade] Seek error:", error);
      this.config.eventManager.endSeek(this._currentTime);
      if (!this._paused) {
        this.config.fpsController.resumeCollection();
      }
    }
  }
  /**
   * 執行精準 seek（兩階段系統的第二階段）
   *
   * 當用戶停頓拖拽 100ms 後，臨時切換到精準模式，
   * seek 到目標位置，然後恢復快速 seek 模式。
   */
  executePreciseSeek() {
    if (this._preciseSeekTarget === null)
      return;
    const target = this._preciseSeekTarget;
    this._preciseSeekTarget = null;
    this._preciseSeekTimer = null;
    this.config.instanceManager.setFastSeek(false);
    this.config.instanceManager.seek(target, "absolute");
    this.config.renderManager.pollFrameAfterSeek(this._scrubbing, true);
    if (this._scrubbing && this._usingFastSeek) {
      this.config.instanceManager.setFastSeek(true);
    }
  }
  resetState() {
    this._currentTime = 0;
    this._duration = 0;
    this._paused = true;
    this._ended = false;
    this._readyState = ReadyState.HAVE_NOTHING;
    this._networkState = NetworkState.NETWORK_LOADING;
    this._videoWidth = 0;
    this._videoHeight = 0;
    this._sourceWidth = 0;
    this._sourceHeight = 0;
    this._videoCodec = "";
    this._audioCodec = "";
    this._containerFormat = "";
    this._bitrate = 0;
    this._rotation = 0;
    this._sampleRate = 0;
    this._channels = 0;
    this._seeking = false;
    this._currentSrc = "";
    this._fps = DEFAULT_FPS;
    this._isHdr = false;
    this._hdrPrimaries = "";
    this._hdrTransfer = "";
    this._hdrMaxLuminance = 0;
    this.skipNextStateSync = false;
    this._scrubbing = false;
    this._usingFastSeek = false;
    this._preciseSeekTarget = null;
    if (this._preciseSeekTimer) {
      clearTimeout(this._preciseSeekTimer);
      this._preciseSeekTimer = null;
    }
    this.config.instanceManager.setScrubbing(false);
    this.config.canvas.classList.add("no-video");
    this.config.renderer.resetState();
    this.config.fpsController.reset();
    this.config.renderManager.reset();
  }
  /**
   * 設置初始 target resolution（在 load 之前調用）
   * 讓 vf 濾鏡在第一幀就使用正確的目標尺寸
   */
  setInitialTargetResolution() {
    const canvas = this.config.canvas;
    const rect = canvas.getBoundingClientRect();
    if (rect.width > 0 && rect.height > 0) {
      const dpr = window.devicePixelRatio || 1;
      const targetWidth = Math.ceil(rect.width * dpr);
      const targetHeight = Math.ceil(rect.height * dpr);
      this.config.instanceManager.setTargetResolution(targetWidth, targetHeight);
    }
  }
  applyMetadata(metadata) {
    if (!metadata)
      return;
    if (metadata.width > 0 && metadata.height > 0) {
      this._sourceWidth = metadata.width;
      this._sourceHeight = metadata.height;
    }
    if (metadata.fps > 0) {
      this._fps = metadata.fps;
      this.config.fpsController.setNativeFps(metadata.fps);
    }
    this._isHdr = metadata.isHdr;
    this._hdrPrimaries = metadata.hdrPrimaries;
    this._hdrTransfer = metadata.hdrTransfer;
    this._hdrMaxLuminance = metadata.hdrMaxLuminance;
    this._videoCodec = metadata.videoCodec || "";
    this._audioCodec = metadata.audioCodec || "";
    this._containerFormat = metadata.format || "";
    this._bitrate = metadata.bitrate || 0;
    this._rotation = metadata.rotation || 0;
    this._sampleRate = metadata.sampleRate || 0;
    this._channels = metadata.channels || 0;
    console.log(`[MediaFacade] Video metadata - ${this._sourceWidth}×${this._sourceHeight}, fps: ${this._fps}, codec: ${this._videoCodec}/${this._audioCodec}, bitrate: ${this._bitrate}, audio: ${this._sampleRate}Hz/${this._channels}ch, HDR: ${this._isHdr}${this._isHdr ? ` (${this._hdrTransfer}, ${this._hdrPrimaries})` : ""}`);
  }
  // ===== 狀態同步 =====
  startStateSync() {
    if (this.syncInterval !== null)
      return;
    const capturedLoadId = this.currentLoadId;
    this.syncInterval = window.setInterval(() => {
      if (!this.config.isConnected())
        return;
      if (capturedLoadId !== this.currentLoadId) {
        this.stopStateSync();
        return;
      }
      try {
        const state = this.config.instanceManager.getState();
        if (!this.config.isConnected())
          return;
        if (capturedLoadId !== this.currentLoadId)
          return;
        if (state) {
          const oldPaused = this._paused;
          const oldDuration = this._duration;
          const timeSinceLastSeek = Date.now() - this._lastSeekTime;
          if (!this._seeking && timeSinceLastSeek > 300) {
            this._currentTime = state.position;
          }
          this._paused = state.paused;
          if (state.duration > 0 && state.duration !== this._duration) {
            this._duration = state.duration;
          }
          this.config.eventManager.updateTime(this._currentTime);
          if (oldDuration !== this._duration) {
            this.config.eventManager.updateDuration(this._duration);
          }
          if (oldPaused !== this._paused && !this.skipNextStateSync) {
            if (this._paused) {
              this.config.eventManager.pause();
            } else {
              this.config.eventManager.play();
            }
          }
          this.skipNextStateSync = false;
          if (!this._ended && this._duration > 0 && this._currentTime >= this._duration - 0.05) {
            if (this.config.getLoop()) {
              this.config.instanceManager.seek(0, "absolute");
              this.config.instanceManager.play();
              this.skipNextStateSync = true;
            } else {
              this._ended = true;
              this._paused = true;
              this.config.instanceManager.pause();
              this.config.eventManager.fireEnded();
              this.stopStateSync();
              return;
            }
          }
          this.syncStateToSignals();
        }
      } catch (error) {
        if (capturedLoadId !== this.currentLoadId)
          return;
        console.error("[MediaFacade] State sync error:", error);
      }
    }, 100);
  }
  stopStateSync() {
    if (this.syncInterval !== null) {
      clearInterval(this.syncInterval);
      this.syncInterval = null;
    }
  }
  syncStateToSignals() {
    const ms = this.config.mediaState;
    ms.currentTime.set(this._currentTime);
    ms.duration.set(this._duration);
    ms.paused.set(this._paused);
    ms.playing.set(!this._paused);
    ms.volume.set(this._volume);
    ms.muted.set(this._volume === 0);
    ms.playbackRate.set(this._playbackRate);
    ms.seeking.set(this._seeking);
    ms.videoWidth.set(this._videoWidth);
    ms.videoHeight.set(this._videoHeight);
    ms.src.set(this._currentSrc);
    ms.loop.set(this.config.getLoop());
    ms.isHdr.set(this._isHdr);
    ms.fps.set(this._fps);
    ms.ended.set(this.ended);
  }
  // ===== Muted 相關 =====
  setMuted(value) {
    if (value) {
      this.lastVolume = this._volume;
      this.config.instanceManager.setVolume(0);
    } else {
      this.config.instanceManager.setVolume(this.lastVolume * 100);
    }
  }
  getLastVolume() {
    return this.lastVolume;
  }
  setLastVolume(value) {
    this.lastVolume = value;
  }
  // ===== 視頻尺寸更新 =====
  updateVideoSize(width, height) {
    this._videoWidth = width;
    this._videoHeight = height;
  }
  // ===== 當前載入 ID =====
  incrementLoadId() {
    this.currentLoadId++;
  }
  getCurrentLoadId() {
    return this.currentLoadId;
  }
  // ===== 獲取播放狀態 =====
  getPlaybackState() {
    return {
      paused: this._paused,
      seeking: this._seeking,
      scrubbing: this._scrubbing
    };
  }
  // ===== Metadata & HDR API =====
  /**
   * 獲取完整視頻元數據
   */
  async getMetadata() {
    const meta = this.config.instanceManager.getMetadata();
    if (!meta)
      return null;
    return {
      duration: this._duration,
      width: meta.width,
      height: meta.height,
      rotation: meta.rotation,
      format: meta.format,
      videoCodec: meta.videoCodec,
      audioCodec: meta.audioCodec,
      bitrate: meta.bitrate,
      fps: meta.fps,
      isHdr: meta.isHdr,
      hdrPrimaries: meta.hdrPrimaries,
      hdrTransfer: meta.hdrTransfer,
      hdrMaxLuminance: meta.hdrMaxLuminance,
      sampleRate: meta.sampleRate,
      channels: meta.channels
    };
  }
  /**
   * 獲取 HDR 資訊
   */
  async getHdrInfo() {
    return this.config.instanceManager.getHdrInfo();
  }
  /**
   * 設置 HDR 模式
   */
  async setHdrMode(enabled) {
    return this.config.instanceManager.setHdrMode(enabled);
  }
  /**
   * 擷取指定時間的視頻截圖
   *
   * @param time 時間點（秒，支援小數）
   * @param options 截圖選項
   * @returns ImageData 或 null（失敗時）
   *
   * @example
   * ```typescript
   * // 基本用法
   * const img = await video.screenshot(20.12);
   *
   * // 限制最大尺寸
   * const thumb = await video.screenshot(20.12, { maxWidth: 320, maxHeight: 240 });
   *
   * // 繪製到 Canvas
   * const canvas = document.createElement('canvas');
   * canvas.width = img.width;
   * canvas.height = img.height;
   * canvas.getContext('2d').putImageData(img, 0, 0);
   * ```
   */
  async screenshot(time, options) {
    if (!Number.isFinite(time) || time < 0) {
      console.warn("[MediaFacade] Invalid screenshot time:", time);
      return null;
    }
    if (options?.maxWidth !== void 0 && (!Number.isInteger(options.maxWidth) || options.maxWidth <= 0)) {
      console.warn("[MediaFacade] Invalid maxWidth:", options.maxWidth);
      return null;
    }
    if (options?.maxHeight !== void 0 && (!Number.isInteger(options.maxHeight) || options.maxHeight <= 0)) {
      console.warn("[MediaFacade] Invalid maxHeight:", options.maxHeight);
      return null;
    }
    if (this.pendingScreenshot) {
      await this.pendingScreenshot;
    }
    this.pendingScreenshot = this.executeScreenshot(time, options);
    try {
      return await this.pendingScreenshot;
    } finally {
      this.pendingScreenshot = null;
    }
  }
  /**
   * 內部執行截圖邏輯
   */
  executeScreenshot(time, options) {
    return new Promise((resolve) => {
      let unsubscribe = null;
      let timeoutId = null;
      const cleanup = () => {
        if (unsubscribe) {
          unsubscribe();
          unsubscribe = null;
        }
        if (timeoutId) {
          clearTimeout(timeoutId);
          timeoutId = null;
        }
      };
      unsubscribe = this.config.instanceManager.onEvent((event) => {
        if (event.type === _MediaFacade2.SCREENSHOT_EVENT_TYPE) {
          cleanup();
          try {
            const width = event.width;
            const height = event.height;
            const buffer = event.buffer;
            const imageData = this.bufferToImageData(buffer, width, height);
            if (options?.maxWidth || options?.maxHeight) {
              const scaled = this.scaleImageData(
                imageData,
                options.maxWidth,
                options.maxHeight
              );
              resolve(scaled);
            } else {
              resolve(imageData);
            }
          } catch (error) {
            console.error("[MediaFacade] Screenshot processing error:", error);
            resolve(null);
          }
        }
      });
      const success = this.config.instanceManager.requestScreenshot(time);
      if (!success) {
        cleanup();
        resolve(null);
        return;
      }
      timeoutId = setTimeout(() => {
        console.warn("[MediaFacade] Screenshot timeout");
        cleanup();
        resolve(null);
      }, _MediaFacade2.SCREENSHOT_TIMEOUT_MS);
    });
  }
  /**
   * 將 RGBA buffer 轉換為 ImageData
   * @throws 如果 buffer 大小不符合預期
   */
  bufferToImageData(buffer, width, height) {
    const expectedSize = width * height * _MediaFacade2.RGBA_BYTES_PER_PIXEL;
    if (buffer.byteLength !== expectedSize) {
      throw new Error(
        `Buffer size mismatch: expected ${expectedSize}, got ${buffer.byteLength}`
      );
    }
    const data = new Uint8ClampedArray(buffer);
    return new ImageData(data, width, height);
  }
  /**
   * 縮放 ImageData
   */
  scaleImageData(source, maxWidth, maxHeight) {
    const { width: targetWidth, height: targetHeight } = this.calculateTargetSize(
      source.width,
      source.height,
      maxWidth,
      maxHeight
    );
    if (targetWidth === source.width && targetHeight === source.height) {
      return source;
    }
    const srcCanvas = document.createElement("canvas");
    srcCanvas.width = source.width;
    srcCanvas.height = source.height;
    const srcCtx = srcCanvas.getContext("2d");
    if (!srcCtx) {
      throw new Error("Failed to get 2d context for source canvas");
    }
    srcCtx.putImageData(source, 0, 0);
    const dstCanvas = document.createElement("canvas");
    dstCanvas.width = targetWidth;
    dstCanvas.height = targetHeight;
    const dstCtx = dstCanvas.getContext("2d");
    if (!dstCtx) {
      throw new Error("Failed to get 2d context for destination canvas");
    }
    dstCtx.drawImage(srcCanvas, 0, 0, targetWidth, targetHeight);
    return dstCtx.getImageData(0, 0, targetWidth, targetHeight);
  }
  /**
   * 計算目標尺寸（保持比例）
   */
  calculateTargetSize(srcWidth, srcHeight, maxWidth, maxHeight) {
    if (!maxWidth && !maxHeight) {
      return { width: srcWidth, height: srcHeight };
    }
    const aspectRatio = srcWidth / srcHeight;
    let width = srcWidth;
    let height = srcHeight;
    if (maxWidth && width > maxWidth) {
      width = maxWidth;
      height = Math.round(width / aspectRatio);
    }
    if (maxHeight && height > maxHeight) {
      height = maxHeight;
      width = Math.round(height * aspectRatio);
    }
    return { width, height };
  }
};
_MediaFacade.PRECISE_SEEK_DELAY_MS = 100;
_MediaFacade.DECODE_PRESSURE_THRESHOLD = 1e8;
_MediaFacade.SCREENSHOT_TIMEOUT_MS = 5e3;
_MediaFacade.SCREENSHOT_EVENT_TYPE = "screenshot";
_MediaFacade.RGBA_BYTES_PER_PIXEL = 4;
let MediaFacade = _MediaFacade;
const _ControlsManager = class _ControlsManager2 {
  constructor(config) {
    this._mode = "auto";
    this._userMode = "auto";
    this._initialized = false;
    this._visible = true;
    this.hideTimeout = null;
    this.cleanups = [];
    this.config = config;
  }
  /**
   * 初始化控制列（只應在 connectedCallback 中調用一次）
   */
  initialize() {
    if (this._initialized)
      return;
    this._initialized = true;
    this.bindCoreEvents();
    this.bindStateClassEvents();
    this.applyMode();
  }
  /**
   * 獲取控制列模式
   */
  get mode() {
    return this._mode;
  }
  /**
   * 設置控制列模式
   */
  set mode(value) {
    if (["always", "auto", "none"].includes(value)) {
      this._userMode = value;
      this._mode = value === "always" && !!document.fullscreenElement ? "auto" : value;
      if (this._initialized) {
        this.applyMode();
        if (this._mode === "auto") {
          this.scheduleHide();
        }
      }
    }
  }
  /**
   * 獲取用戶設定的控制列模式（不受全螢幕覆蓋影響）
   */
  get userMode() {
    return this._userMode;
  }
  /**
   * 控制列是否可見
   */
  get visible() {
    return this._visible;
  }
  /**
   * 是否已初始化
   */
  get initialized() {
    return this._initialized;
  }
  /**
   * 顯示控制列
   */
  show() {
    this._visible = true;
    this.config.host.classList.remove("controls-hidden");
  }
  /**
   * 隱藏控制列
   */
  hide() {
    if (this._mode === "auto") {
      this._visible = false;
      this.config.host.classList.add("controls-hidden");
    }
  }
  /**
   * 取消隱藏計時器
   */
  cancelHide() {
    if (this.hideTimeout) {
      clearTimeout(this.hideTimeout);
      this.hideTimeout = null;
    }
  }
  /**
   * 排程隱藏控制列
   */
  scheduleHide() {
    this.cancelHide();
    if (this._mode === "auto") {
      this.hideTimeout = window.setTimeout(() => {
        this.hide();
      }, _ControlsManager2.HIDE_DELAY);
    }
  }
  /**
   * 應用控制列模式
   */
  applyMode() {
    const { host } = this.config;
    host.classList.remove("controls-hidden", "controls-none");
    switch (this._mode) {
      case "always":
        this._visible = true;
        break;
      case "none":
        host.classList.add("controls-none");
        this._visible = false;
        break;
      case "auto":
      default:
        this._visible = true;
        break;
    }
  }
  /**
   * 銷毀並清理資源
   */
  destroy() {
    this.cancelHide();
    for (const cleanup of this.cleanups) {
      cleanup();
    }
    this.cleanups = [];
    this._initialized = false;
  }
  // ===== 內部方法 =====
  /**
   * 添加事件監聽器並追蹤以便清理
   */
  on(target, event, handler) {
    target.addEventListener(event, handler);
    this.cleanups.push(() => target.removeEventListener(event, handler));
  }
  /**
   * 綁定控制列核心事件
   */
  bindCoreEvents() {
    const { host, videoWrapper, controlsEl } = this.config;
    this.on(host, "mousedown", () => {
      host.focus();
    });
    this.on(videoWrapper, "click", () => this.config.toggle());
    this.on(controlsEl, "dblclick", (e) => {
      e.stopPropagation();
    });
    this.on(videoWrapper, "mousemove", () => {
      if (this._mode === "auto") {
        this.show();
        this.scheduleHide();
      }
    });
    this.on(controlsEl, "mouseenter", () => {
      if (this._mode === "auto") {
        this.cancelHide();
        this.show();
      }
    });
    this.on(host, "mouseleave", () => {
      if (this._mode === "auto") {
        this.scheduleHide();
      }
    });
    if (this._mode === "auto") {
      this.scheduleHide();
    }
  }
  /**
   * 綁定狀態類別事件（供 CSS 使用）
   */
  bindStateClassEvents() {
    const { host } = this.config;
    this.on(host, "play", () => host.classList.toggle("playing", true));
    this.on(host, "pause", () => host.classList.toggle("playing", false));
    this.on(host, "ended", () => host.classList.remove("playing"));
    this.on(host, "loadedmetadata", () => host.classList.remove("no-source"));
    this.on(host, "emptied", () => host.classList.add("no-source"));
    this.on(host, "loadstart", () => host.classList.add("loading"));
    this.on(host, "error", (e) => {
      host.classList.remove("loading");
      host.classList.add("error");
      const message = e.message || host.error?.message || "Unknown error";
      this.config.videoWrapper.setAttribute("data-error", message);
    });
    this.on(host, "seeking", () => host.classList.add("seeking"));
    this.on(host, "seeked", () => host.classList.remove("seeking"));
    this.on(host, "waiting", () => host.classList.add("buffering"));
    this.on(host, "canplay", () => {
      host.classList.remove("loading", "buffering", "error");
      this.config.videoWrapper.removeAttribute("data-error");
    });
    this.on(host, "volumechange", () => {
      host.classList.toggle("muted", this.config.getVolume() === 0);
    });
    this.on(document, "fullscreenchange", () => {
      const isFullscreen = !!document.fullscreenElement;
      host.classList.toggle("fullscreen", isFullscreen);
      if (isFullscreen) {
        if (this._userMode === "always") {
          this._mode = "auto";
          this.applyMode();
          this.scheduleHide();
        }
      } else {
        this._mode = this._userMode;
        this.cancelHide();
        this.applyMode();
      }
    });
  }
};
_ControlsManager.HIDE_DELAY = 3e3;
let ControlsManager = _ControlsManager;
const OBSERVED_ATTRIBUTES = [
  "src",
  "loop",
  "autoplay",
  "muted",
  "controls",
  "controls-mode",
  "poster",
  "width",
  "height",
  "playbackrate"
];
class AttributeObserver {
  constructor(host, handlers) {
    this.host = host;
    this.handlers = handlers;
  }
  /**
   * 處理屬性變化
   * 對應 Custom Element 的 attributeChangedCallback
   */
  handleChange(name, oldValue, newValue) {
    if (oldValue === newValue)
      return;
    switch (name) {
      case "src":
        if (newValue) {
          this.handlers.onSrcChange(newValue);
        }
        break;
      case "loop":
        break;
      case "autoplay":
        break;
      case "muted":
        this.handlers.onMutedChange(newValue !== null);
        break;
      case "width":
        if (newValue) {
          this.host.style.width = `${newValue}px`;
        }
        break;
      case "height":
        if (newValue) {
          this.host.style.height = `${newValue}px`;
        }
        break;
      case "playbackrate":
        if (newValue) {
          this.handlers.onPlaybackRateChange(parseFloat(newValue));
        }
        break;
      case "controls":
        break;
      case "controls-mode":
        if (newValue && ["always", "auto", "none"].includes(newValue)) {
          if (this.handlers.isConnected() && this.handlers.isControlsInitialized()) {
            this.handlers.onControlsModeChange(newValue);
          }
        }
        break;
    }
  }
}
const shadowStyles = `/**\r
 * MpvVideoElement Core Styles\r
 *\r
 * 包含核心框架樣式和控制列樣式\r
 * 注意：Phase 6 重構尚未完成，控制列代碼仍在核心中\r
 */\r
\r
/* ===== CSS 變量定義（默認亮色主題） ===== */\r
:host {\r
    /* 主要顏色 */\r
    --vp-primary-color: #2563eb;\r
    --vp-primary-hover-color: #1d4ed8;\r
\r
    /* 背景顏色 */\r
    --vp-controls-bg: rgba(255, 255, 255, 0.9);\r
    --vp-menu-bg: rgba(255, 255, 255, 0.9);\r
    --vp-tooltip-bg: rgba(0, 0, 0, 0.8);\r
\r
    /* 文字顏色 */\r
    --vp-text-color: #2c2f32;\r
    --vp-text-secondary-color: rgba(44, 47, 50, 0.5);\r
\r
    /* 進度條 */\r
    --vp-progress-bg: rgba(80, 85, 91, 0.25);\r
    --vp-progress-buffered-color: rgba(80, 85, 91, 0.15);\r
    --vp-progress-played-color: #50555b;\r
\r
    /* 按鈕 */\r
    --vp-button-color: #50555b;\r
    --vp-button-hover-bg: rgba(44, 47, 50, 0.08);\r
    --vp-button-active-bg: rgba(44, 47, 50, 0.12);\r
\r
    /* 邊框和陰影 */\r
    --vp-border-color: rgba(0, 0, 0, 0.1);\r
    --vp-shadow-color: rgba(0, 0, 0, 0.15);\r
\r
    /* 尺寸 */\r
    --vp-controls-height: 48px;\r
    --vp-button-size: 24px;\r
    --vp-icon-size: 24px;\r
\r
    /* 動畫 */\r
    --vp-transition-duration: 0.15s;\r
}\r
\r
/* ===== Host Element ===== */\r
:host {\r
    display: block;\r
    width: 100%;\r
    height: 100%;\r
    background: transparent;\r
    position: relative;\r
    overflow: hidden;\r
    font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;\r
    user-select: none;\r
    -webkit-user-select: none;\r
}\r
\r
:host * {\r
    box-sizing: border-box;\r
}\r
\r
:host(:focus) {\r
    outline: none;\r
}\r
\r
:host(:focus-visible) {\r
    outline: 2px solid var(--vp-button-color);\r
    outline-offset: -2px;\r
}\r
\r
/* ===== Video Area ===== */\r
.vp-video-wrapper {\r
    position: absolute;\r
    top: 0;\r
    left: 0;\r
    width: 100%;\r
    height: 100%;\r
    display: flex;\r
    align-items: center;\r
    justify-content: center;\r
    cursor: pointer;\r
    z-index: 1;\r
}\r
\r
canvas {\r
    width: 100%;\r
    height: 100%;\r
    display: block;\r
    object-fit: contain;\r
}\r
\r
/* ===== Poster ===== */\r
:host([poster]) canvas.no-video {\r
    opacity: 0;\r
}\r
\r
.poster {\r
    position: absolute;\r
    top: 0;\r
    left: 0;\r
    width: 100%;\r
    height: 100%;\r
    object-fit: contain;\r
    display: none;\r
}\r
\r
:host([poster]) .poster {\r
    display: block;\r
}\r
\r
:host([poster]) canvas:not(.no-video) ~ .poster {\r
    display: none;\r
}\r
\r
/* ===== Loading / Buffering / Error States ===== */\r
:host(.loading) .vp-video-wrapper::after,\r
:host(.buffering) .vp-video-wrapper::after {\r
    content: '';\r
    position: absolute;\r
    top: 50%;\r
    left: 50%;\r
    width: 48px;\r
    height: 48px;\r
    margin: -24px 0 0 -24px;\r
    border: 3px solid rgba(255, 255, 255, 0.2);\r
    border-top-color: var(--vp-button-color);\r
    border-radius: 50%;\r
    animation: vp-spin 1s linear infinite;\r
    z-index: 50;\r
}\r
\r
:host(.error) .vp-video-wrapper::after {\r
    content: attr(data-error);\r
    position: absolute;\r
    top: 50%;\r
    left: 50%;\r
    transform: translate(-50%, -50%);\r
    color: #ff4444;\r
    font-size: 14px;\r
    max-width: 80%;\r
    text-align: center;\r
    z-index: 50;\r
}\r
\r
@keyframes vp-spin {\r
    to { transform: rotate(360deg); }\r
}\r
\r
/* ===== Fullscreen ===== */\r
:host(.fullscreen) {\r
    position: fixed;\r
    top: 0;\r
    left: 0;\r
    width: 100vw !important;\r
    height: 100vh !important;\r
    z-index: 9999;\r
}\r
\r
/* ===== 控制列 ===== */\r
.vp-controls {\r
    position: absolute;\r
    bottom: 0;\r
    left: 0;\r
    right: 0;\r
    height: var(--vp-controls-height);\r
    background: var(--vp-controls-bg);\r
    backdrop-filter: blur(25px);\r
    -webkit-backdrop-filter: blur(25px);\r
    border-top: 1px solid var(--vp-border-color);\r
    padding: 0 12px;\r
    transition: opacity 0.3s, transform 0.3s;\r
    z-index: 100;\r
    display: none;\r
}\r
\r
:host([controls]) .vp-controls {\r
    display: flex;\r
    align-items: center;\r
}\r
\r
:host(.controls-hidden) .vp-controls {\r
    opacity: 0;\r
    transform: translateY(10px);\r
    pointer-events: none;\r
}\r
\r
:host(.controls-none) .vp-controls {\r
    display: none !important;\r
}\r
\r
:host(.no-source) .vp-controls {\r
    display: none !important;\r
}\r
\r
.vp-controls-row {\r
    display: flex;\r
    align-items: center;\r
    width: 100%;\r
    gap: 12px;\r
}\r
\r
.vp-controls-group {\r
    display: flex;\r
    align-items: center;\r
    gap: 8px;\r
    flex-shrink: 0;\r
}\r
\r
/* ===== 按鈕 ===== */\r
.vp-btn {\r
    width: 24px;\r
    height: 24px;\r
    display: flex;\r
    align-items: center;\r
    justify-content: center;\r
    background: transparent;\r
    border: none;\r
    border-radius: 4px;\r
    cursor: pointer;\r
    color: var(--vp-button-color);\r
    transition: background 0.15s;\r
    padding: 0;\r
    flex-shrink: 0;\r
}\r
\r
.vp-btn:hover {\r
    background: var(--vp-button-hover-bg);\r
}\r
\r
.vp-btn [class^="vp-icon-"] {\r
    display: flex;\r
}\r
\r
.vp-btn svg {\r
    width: 24px;\r
    height: 24px;\r
}\r
\r
/* 播放/暫停按鈕 */\r
.vp-play-btn .vp-icon-pause {\r
    display: none;\r
}\r
\r
:host(.playing) .vp-play-btn .vp-icon-play {\r
    display: none;\r
}\r
\r
:host(.playing) .vp-play-btn .vp-icon-pause {\r
    display: flex;\r
}\r
\r
/* 音量按鈕 */\r
.vp-volume-btn .vp-icon-muted {\r
    display: none;\r
}\r
\r
:host(.muted) .vp-volume-btn .vp-icon-volume {\r
    display: none;\r
}\r
\r
:host(.muted) .vp-volume-btn .vp-icon-muted {\r
    display: flex;\r
}\r
\r
/* 全螢幕按鈕 */\r
.vp-fullscreen-btn .vp-icon-exit-fullscreen {\r
    display: none;\r
}\r
\r
:host(.fullscreen) .vp-fullscreen-btn .vp-icon-fullscreen {\r
    display: none;\r
}\r
\r
:host(.fullscreen) .vp-fullscreen-btn .vp-icon-exit-fullscreen {\r
    display: flex;\r
}\r
\r
/* 循環播放按鈕 */\r
.vp-loop-btn {\r
    transition: background 0.15s;\r
}\r
\r
.vp-loop-btn.active {\r
    background: var(--vp-button-active-bg);\r
}\r
\r
/* ===== 音量控制 ===== */\r
.vp-volume-container {\r
    display: flex;\r
    align-items: center;\r
    gap: 2px;\r
}\r
\r
.vp-volume-slider {\r
    width: 0;\r
    overflow: hidden;\r
    transition: width 0.2s, overflow 0s 0.2s;\r
}\r
\r
.vp-volume-container:hover .vp-volume-slider {\r
    width: 48px;\r
    overflow: visible;\r
    transition: width 0.2s, overflow 0s 0s;\r
}\r
\r
.vp-volume-track {\r
    width: 40px;\r
    height: 12px;\r
    background: transparent;\r
    cursor: pointer;\r
    position: relative;\r
}\r
\r
.vp-volume-track::before {\r
    content: '';\r
    position: absolute;\r
    left: 0;\r
    right: 0;\r
    top: 50%;\r
    height: 2px;\r
    background: var(--vp-progress-bg);\r
    border-radius: 1px;\r
    transform: translateY(-50%);\r
}\r
\r
.vp-volume-track-filled {\r
    position: absolute;\r
    left: 0;\r
    top: 50%;\r
    width: 50%;\r
    height: 2px;\r
    background: var(--vp-button-color);\r
    border-radius: 1px;\r
    transform: translateY(-50%);\r
    pointer-events: none;\r
}\r
\r
.vp-volume-handle {\r
    position: absolute;\r
    top: 50%;\r
    left: 50%;\r
    width: 10px;\r
    height: 10px;\r
    background: var(--vp-controls-bg);\r
    border: 1.5px solid var(--vp-button-color);\r
    border-radius: 50%;\r
    transform: translate(-50%, -50%);\r
    pointer-events: none;\r
    box-sizing: border-box;\r
}\r
\r
/* ===== 進度條區域 ===== */\r
.vp-progress-section {\r
    flex: 1;\r
    min-width: 0;\r
    display: flex;\r
    align-items: center;\r
    gap: 8px;\r
}\r
\r
.vp-time-display {\r
    display: flex;\r
    align-items: center;\r
    gap: 4px;\r
    padding-right: 10px;\r
    flex-shrink: 0;\r
    height: 24px;\r
}\r
\r
.vp-time-current,\r
.vp-time-separator,\r
.vp-time-duration {\r
    font-size: 13px;\r
    font-variant-numeric: tabular-nums;\r
    white-space: nowrap;\r
    line-height: 1;\r
    display: flex;\r
    align-items: center;\r
}\r
\r
.vp-time-current {\r
    color: var(--vp-text-color);\r
}\r
\r
.vp-time-separator,\r
.vp-time-duration {\r
    color: var(--vp-text-secondary-color);\r
}\r
\r
.vp-progress-container {\r
    position: relative;\r
    flex: 1;\r
    min-width: 0;\r
    height: 48px;\r
    display: flex;\r
    align-items: center;\r
    cursor: pointer;\r
}\r
\r
.vp-progress-bar {\r
    position: relative;\r
    width: 100%;\r
    height: 4px;\r
    display: flex;\r
    border-radius: 2px;\r
    background: var(--vp-progress-bg);\r
}\r
\r
.vp-progress-buffered {\r
    position: absolute;\r
    top: 0;\r
    left: 0;\r
    height: 100%;\r
    background: var(--vp-progress-buffered-color);\r
    width: 0;\r
}\r
\r
.vp-progress-playhead {\r
    position: absolute;\r
    top: 50%;\r
    left: 0;\r
    width: 1px;\r
    height: var(--vp-controls-height);\r
    background: var(--vp-progress-played-color);\r
    transform: translate(-50%, -50%);\r
    z-index: 5;\r
    pointer-events: none;\r
}\r
\r
/* Tooltip */\r
.vp-progress-tooltip {\r
    position: absolute;\r
    bottom: 100%;\r
    left: 0;\r
    transform: translateX(-50%);\r
    opacity: 0;\r
    pointer-events: none;\r
    transition: opacity 0.15s;\r
    margin-bottom: 8px;\r
}\r
\r
.vp-progress-tooltip.visible {\r
    opacity: 1;\r
}\r
\r
.vp-tooltip-thumbnail-wrapper {\r
    position: relative;\r
    border: 2px solid rgba(255, 255, 255, 0.9);\r
    border-radius: 8px;\r
    overflow: hidden;\r
    background: #000;\r
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);\r
    min-width: 120px;\r
    min-height: 68px;\r
}\r
\r
.vp-progress-tooltip::after {\r
    content: '';\r
    position: absolute;\r
    top: 100%;\r
    left: 50%;\r
    width: 1px;\r
    height: 64px;\r
    background: rgba(0, 0, 0, 0.2);\r
    transform: translateX(-50%);\r
}\r
\r
.vp-tooltip-thumbnail {\r
    display: none;\r
    vertical-align: bottom;\r
}\r
\r
.vp-tooltip-time {\r
    position: absolute;\r
    bottom: 0;\r
    left: 0;\r
    right: 0;\r
    text-align: center;\r
    color: #fff;\r
    font-size: 13px;\r
    font-weight: 500;\r
    font-variant-numeric: tabular-nums;\r
    padding: 4px 8px;\r
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.7));\r
}\r
\r
/* ===== 速度選單 ===== */\r
.vp-speed-container {\r
    position: relative;\r
}\r
\r
.vp-speed-btn {\r
    width: auto;\r
    height: var(--vp-button-size);\r
    padding: 0 6px;\r
    font-size: 13px;\r
    color: var(--vp-text-color);\r
    background: var(--vp-button-hover-bg);\r
    border-radius: 4px;\r
    line-height: 20px;\r
}\r
\r
.vp-speed-btn:hover {\r
    background: var(--vp-button-active-bg);\r
}\r
\r
.vp-menu {\r
    position: absolute;\r
    bottom: 100%;\r
    left: 50%;\r
    transform: translateX(-50%);\r
    margin-bottom: 8px;\r
    background: var(--vp-menu-bg);\r
    backdrop-filter: blur(25px);\r
    -webkit-backdrop-filter: blur(25px);\r
    border: 1px solid var(--vp-border-color);\r
    border-radius: 6px;\r
    padding: 6px;\r
    min-width: 78px;\r
    opacity: 0;\r
    visibility: hidden;\r
    transform: translateX(-50%) translateY(10px);\r
    transition: opacity 0.2s, transform 0.2s, visibility 0.2s;\r
    box-shadow: 0 3px 12px var(--vp-shadow-color);\r
}\r
\r
.vp-menu.show,\r
.vp-speed-container:hover .vp-speed-menu {\r
    opacity: 1;\r
    visibility: visible;\r
    transform: translateX(-50%) translateY(0);\r
}\r
\r
.vp-menu-item {\r
    display: flex;\r
    align-items: center;\r
    gap: 6px;\r
    padding: 4px 6px;\r
    height: 28px;\r
    color: var(--vp-text-secondary-color);\r
    font-size: 13px;\r
    cursor: pointer;\r
    border-radius: 4px;\r
    transition: background var(--vp-transition-duration);\r
    white-space: nowrap;\r
}\r
\r
.vp-menu-item:hover {\r
    background: var(--vp-button-hover-bg);\r
}\r
\r
.vp-menu-item.active {\r
    color: var(--vp-text-color);\r
    font-weight: 500;\r
}\r
\r
.vp-menu-item::before {\r
    content: '';\r
    width: 16px;\r
    height: 16px;\r
    flex-shrink: 0;\r
}\r
\r
.vp-menu-item.active::before {\r
    content: '\\2713';\r
    display: flex;\r
    align-items: center;\r
    justify-content: center;\r
    font-size: 12px;\r
    color: var(--vp-text-color);\r
}\r
\r
/* ===== 響應式 ===== */\r
@media (max-width: 600px) {\r
    .vp-controls {\r
        padding: 0 8px;\r
    }\r
\r
    .vp-controls-row {\r
        gap: 8px;\r
    }\r
\r
    .vp-controls-group {\r
        gap: 4px;\r
    }\r
\r
    .vp-time-display {\r
        display: none;\r
    }\r
\r
    .vp-volume-container:hover .vp-volume-slider {\r
        width: 30px;\r
    }\r
}\r
`;
const externalStyles = "/**\r\n * MpvVideoElement External Styles\r\n *\r\n * 用於外部創建的元素（如 drop zone）\r\n * 這些樣式會被添加到 document.head\r\n */\r\n\r\n/* Drop zone 樣式（可由外部控制） */\r\n.vp-drop-zone {\r\n    position: absolute;\r\n    top: 0;\r\n    left: 0;\r\n    width: 100%;\r\n    height: 100%;\r\n    display: none;\r\n    align-items: center;\r\n    justify-content: center;\r\n    background: rgba(0, 0, 0, 0.85);\r\n    border: 4px dashed #23ade5;\r\n    z-index: 200;\r\n    pointer-events: none;\r\n    box-sizing: border-box;\r\n}\r\n\r\n.vp-drop-zone.active {\r\n    display: flex;\r\n}\r\n\r\n.vp-drop-zone-text {\r\n    color: #fff;\r\n    font-size: 18px;\r\n    text-align: center;\r\n    padding: 20px;\r\n}\r\n";
let macOSCompatible = null;
function isMacOSCompatible() {
  if (macOSCompatible !== null)
    return macOSCompatible;
  if (typeof process === "undefined" || process.platform !== "darwin") {
    return macOSCompatible = true;
  }
  const darwinMajor = parseInt(require("os").release());
  macOSCompatible = darwinMajor >= 23;
  if (!macOSCompatible) {
    console.warn(`[MpvVideoElement] Darwin ${darwinMajor} detected. Requires macOS 14 (Sonoma) or later (Darwin >= 23).`);
  }
  return macOSCompatible;
}
const _MpvVideoElement = class _MpvVideoElement2 extends HTMLElement {
  constructor() {
    super();
    this.isConnected_ = false;
    this.lifecycleController = null;
    this.resizeObserver = null;
    this.resizeRafId = null;
    this.cachedElementWidth = 0;
    this.cachedElementHeight = 0;
    this.thumbnailController = null;
    this.thumbnailVideoPath = null;
    this.thumbnailRafId = null;
    this.thumbnailHovering = false;
    this.thumbnailWidth = 160;
    this.thumbnailHeight = 90;
    this.thumbnailLastRenderTime = 0;
    this.thumbnailIdleTimer = null;
    this.thumbnailFrameQueue = [];
    this._tooltipEnabled = true;
    this.instanceId = generateInstanceId();
    this.shadow = this.attachShadow({ mode: "open" });
    const style = document.createElement("style");
    style.textContent = shadowStyles;
    this.shadow.appendChild(style);
    if (!document.getElementById("mpv-video-external-styles")) {
      const externalStyle = document.createElement("style");
      externalStyle.id = "mpv-video-external-styles";
      externalStyle.textContent = externalStyles;
      document.head.appendChild(externalStyle);
    }
    this.videoWrapper = document.createElement("div");
    this.videoWrapper.className = "vp-video-wrapper";
    this.shadow.appendChild(this.videoWrapper);
    this.canvas = document.createElement("canvas");
    this.canvas.className = "no-video";
    this.videoWrapper.appendChild(this.canvas);
    this.controlsEl = document.createElement("div");
    this.controlsEl.className = "vp-controls";
    this.controlsEl.innerHTML = `
            <div class="vp-controls-row">
                <div class="vp-controls-group vp-controls-left"></div>
                <div class="vp-progress-section"></div>
                <div class="vp-controls-group vp-controls-right"></div>
            </div>
        `;
    this.shadow.appendChild(this.controlsEl);
    this.renderer = new webglRenderer.WebGLVideoRenderer(this.canvas);
    this.eventManager = new VideoEventManager(this);
    this.fpsController = new AdaptiveFpsController();
    this.setupFpsControllerCallbacks();
    this.instanceManager = new InstanceManager({
      instanceId: this.instanceId,
      instanceOptions: { hwdec: this._hwdec },
      onVideoEvent: (event) => this.handleVideoEvent(event)
    });
    this.mediaState = PluginManager.createMediaState();
    this.renderManager = new RenderManager({
      renderer: this.renderer,
      fpsController: this.fpsController,
      instanceManager: this.instanceManager,
      getElementSize: () => ({
        width: this.cachedElementWidth,
        height: this.cachedElementHeight
      }),
      getPlaybackState: () => this.mediaFacade.getPlaybackState(),
      onVideoSizeChange: (width, height) => {
        this.mediaFacade.updateVideoSize(width, height);
        this.eventManager.updateVideoSize(width, height);
      },
      onStalled: () => this.eventManager.stalled(),
      isConnected: () => this.isConnected_
    });
    this.mediaFacade = new MediaFacade({
      instanceId: this.instanceId,
      instanceManager: this.instanceManager,
      renderManager: this.renderManager,
      eventManager: this.eventManager,
      fpsController: this.fpsController,
      mediaState: this.mediaState,
      isConnected: () => this.isConnected_,
      getLoop: () => this.loop,
      getAutoplay: () => this.autoplay,
      getDefaultMuted: () => this.defaultMuted,
      getMuted: () => this.muted,
      setMuted: (v) => {
        this.muted = v;
      },
      canvas: this.canvas,
      renderer: this.renderer
    });
    this.controlsManager = new ControlsManager({
      host: this,
      shadowRoot: this.shadow,
      videoWrapper: this.videoWrapper,
      controlsEl: this.controlsEl,
      toggle: () => this.toggle(),
      toggleFullscreen: () => this.toggleFullscreen(),
      getVolume: () => this.volume
    });
    this.attributeObserver = new AttributeObserver(this, {
      onSrcChange: () => this.load(),
      onMutedChange: (muted) => {
        this.muted = muted;
      },
      onPlaybackRateChange: (rate) => {
        this.playbackRate = rate;
      },
      onControlsModeChange: (mode) => {
        this.controlsManager.mode = mode;
      },
      isConnected: () => this.isConnected_,
      isControlsInitialized: () => this.controlsManager.initialized
    });
    this.pluginManager = new PluginManager.PluginManager(this);
    this.themeManager = new ThemeManager(this.shadow);
    console.log(`[MpvVideoElement] Created with instanceId: ${this.instanceId} (Direct Mode)`);
  }
  /**
   * 檢查直連模式是否可用
   */
  static isAvailable() {
    return InstanceManager.isAvailable();
  }
  /**
   * 全局插件註冊
   */
  static use(plugin, options) {
    PluginManager.GlobalPluginRegistry.register(plugin, options);
    for (const instance of this.connectedInstances) {
      if (!instance.pluginManager.has(plugin.name)) {
        instance.pluginManager.register(plugin, options);
      }
    }
    return this;
  }
  static get observedAttributes() {
    return [...OBSERVED_ATTRIBUTES];
  }
  // ===== on* 事件處理器屬性 =====
  get onloadstart() {
    return this.eventManager.getEventHandler("loadstart");
  }
  set onloadstart(handler) {
    this.eventManager.setEventHandler("loadstart", handler);
  }
  get onprogress() {
    return this.eventManager.getEventHandler("progress");
  }
  set onprogress(handler) {
    this.eventManager.setEventHandler("progress", handler);
  }
  get onsuspend() {
    return this.eventManager.getEventHandler("suspend");
  }
  set onsuspend(handler) {
    this.eventManager.setEventHandler("suspend", handler);
  }
  get onabort() {
    return this.eventManager.getEventHandler("abort");
  }
  set onabort(handler) {
    this.eventManager.setEventHandler("abort", handler);
  }
  get onmediaerror() {
    return this.eventManager.getEventHandler("error");
  }
  set onmediaerror(handler) {
    this.eventManager.setEventHandler("error", handler);
  }
  get onemptied() {
    return this.eventManager.getEventHandler("emptied");
  }
  set onemptied(handler) {
    this.eventManager.setEventHandler("emptied", handler);
  }
  get onstalled() {
    return this.eventManager.getEventHandler("stalled");
  }
  set onstalled(handler) {
    this.eventManager.setEventHandler("stalled", handler);
  }
  get onloadedmetadata() {
    return this.eventManager.getEventHandler("loadedmetadata");
  }
  set onloadedmetadata(handler) {
    this.eventManager.setEventHandler("loadedmetadata", handler);
  }
  get onloadeddata() {
    return this.eventManager.getEventHandler("loadeddata");
  }
  set onloadeddata(handler) {
    this.eventManager.setEventHandler("loadeddata", handler);
  }
  get ondurationchange() {
    return this.eventManager.getEventHandler("durationchange");
  }
  set ondurationchange(handler) {
    this.eventManager.setEventHandler("durationchange", handler);
  }
  get oncanplay() {
    return this.eventManager.getEventHandler("canplay");
  }
  set oncanplay(handler) {
    this.eventManager.setEventHandler("canplay", handler);
  }
  get oncanplaythrough() {
    return this.eventManager.getEventHandler("canplaythrough");
  }
  set oncanplaythrough(handler) {
    this.eventManager.setEventHandler("canplaythrough", handler);
  }
  get onplay() {
    return this.eventManager.getEventHandler("play");
  }
  set onplay(handler) {
    this.eventManager.setEventHandler("play", handler);
  }
  get onplaying() {
    return this.eventManager.getEventHandler("playing");
  }
  set onplaying(handler) {
    this.eventManager.setEventHandler("playing", handler);
  }
  get onpause() {
    return this.eventManager.getEventHandler("pause");
  }
  set onpause(handler) {
    this.eventManager.setEventHandler("pause", handler);
  }
  get onended() {
    return this.eventManager.getEventHandler("ended");
  }
  set onended(handler) {
    this.eventManager.setEventHandler("ended", handler);
  }
  get onwaiting() {
    return this.eventManager.getEventHandler("waiting");
  }
  set onwaiting(handler) {
    this.eventManager.setEventHandler("waiting", handler);
  }
  get onseeking() {
    return this.eventManager.getEventHandler("seeking");
  }
  set onseeking(handler) {
    this.eventManager.setEventHandler("seeking", handler);
  }
  get onseeked() {
    return this.eventManager.getEventHandler("seeked");
  }
  set onseeked(handler) {
    this.eventManager.setEventHandler("seeked", handler);
  }
  get ontimeupdate() {
    return this.eventManager.getEventHandler("timeupdate");
  }
  set ontimeupdate(handler) {
    this.eventManager.setEventHandler("timeupdate", handler);
  }
  get onvolumechange() {
    return this.eventManager.getEventHandler("volumechange");
  }
  set onvolumechange(handler) {
    this.eventManager.setEventHandler("volumechange", handler);
  }
  get onratechange() {
    return this.eventManager.getEventHandler("ratechange");
  }
  set onratechange(handler) {
    this.eventManager.setEventHandler("ratechange", handler);
  }
  get onresize() {
    return this.eventManager.getEventHandler("resize");
  }
  set onresize(handler) {
    this.eventManager.setEventHandler("resize", handler);
  }
  // ===== 生命週期 =====
  connectedCallback() {
    this.isConnected_ = true;
    if (!this.hasAttribute("tabindex")) {
      this.tabIndex = 0;
    }
    if (!this.src) {
      this.classList.add("no-source");
    }
    _MpvVideoElement2.connectedInstances.add(this);
    const shouldResumePlayback = this.instanceManager.handleReconnect();
    this.lifecycleController?.abort();
    this.lifecycleController = new AbortController();
    this.resizeObserver = new ResizeObserver(() => this.updateCanvasSize());
    this.resizeObserver.observe(this);
    this.updateCanvasSize();
    PluginManager.GlobalPluginRegistry.installTo(this.pluginManager);
    this.controlsManager.initialize();
    if (this.instanceManager.isCreated() && this.readyState >= ReadyState.HAVE_CURRENT_DATA) {
      this.renderManager.startRenderLoop();
      if (shouldResumePlayback) {
        this.play().catch((err) => {
          console.warn("[MpvVideoElement] Failed to resume play on reconnect:", err);
        });
        console.log(`[MpvVideoElement] Connected (resuming playback): ${this.instanceId}`);
      } else {
        console.log(`[MpvVideoElement] Connected (resuming render): ${this.instanceId}`);
      }
    } else if (this.src && !this.instanceManager.isCreated()) {
      this.load();
      console.log(`[MpvVideoElement] Connected (triggering load): ${this.instanceId}`);
    } else {
      console.log(`[MpvVideoElement] Connected: ${this.instanceId}`);
    }
  }
  disconnectedCallback() {
    this.isConnected_ = false;
    _MpvVideoElement2.connectedInstances.delete(this);
    this.mediaFacade.incrementLoadId();
    if (this.lifecycleController) {
      this.lifecycleController.abort();
      this.lifecycleController = null;
    }
    this.renderManager.stopRenderLoop();
    this.renderManager.clearCache();
    if (this.resizeRafId) {
      cancelAnimationFrame(this.resizeRafId);
      this.resizeRafId = null;
    }
    if (this.resizeObserver) {
      this.resizeObserver.disconnect();
      this.resizeObserver = null;
    }
    this.instanceManager.handleDisconnectImmediate(this.paused);
    console.log(`[MpvVideoElement] Disconnected: ${this.instanceId}`);
  }
  attributeChangedCallback(name, oldValue, newValue) {
    this.attributeObserver.handleChange(name, oldValue, newValue);
  }
  handleVideoEvent(event) {
    if (event.type === "thumbnail" || event.type === "thumbnail-clear") {
      return;
    }
    if (event.error) {
      console.error("[MpvVideoElement] Video event error:", event);
    }
  }
  // ===== HTMLMediaElement 屬性 =====
  get src() {
    return this.getAttribute("src") || "";
  }
  set src(value) {
    if (value) {
      this.setAttribute("src", value);
    } else {
      this.removeAttribute("src");
    }
  }
  get currentTime() {
    return this.mediaFacade.currentTime;
  }
  set currentTime(value) {
    this.mediaFacade.currentTime = value;
  }
  get duration() {
    return this.mediaFacade.duration;
  }
  get paused() {
    return this.mediaFacade.paused;
  }
  get volume() {
    return this.mediaFacade.volume;
  }
  set volume(value) {
    this.mediaFacade.volume = value;
  }
  get muted() {
    return this.hasAttribute("muted");
  }
  set muted(value) {
    const wasMuted = this.muted;
    if (value) {
      this.mediaFacade.setLastVolume(this.mediaFacade.volume);
      this.setAttribute("muted", "");
      this.mediaFacade.setMuted(true);
    } else {
      this.removeAttribute("muted");
      this.mediaFacade.setMuted(false);
    }
    if (wasMuted !== value) {
      this.eventManager.updateVolume(this.mediaFacade.volume, value);
    }
  }
  get playbackRate() {
    return this.mediaFacade.playbackRate;
  }
  set playbackRate(value) {
    this.mediaFacade.playbackRate = value;
    this.setAttribute("playbackrate", String(value));
  }
  get hwdec() {
    return this._hwdec;
  }
  set hwdec(value) {
    this._hwdec = value;
  }
  get loop() {
    return this.hasAttribute("loop");
  }
  set loop(value) {
    const wasLoop = this.loop;
    if (value) {
      this.setAttribute("loop", "");
    } else {
      this.removeAttribute("loop");
    }
    if (wasLoop !== value) {
      this.dispatchEvent(new Event("loopchange"));
      if (value && this.ended) {
        this.play();
      }
    }
  }
  get autoplay() {
    return this.hasAttribute("autoplay");
  }
  set autoplay(value) {
    if (value) {
      this.setAttribute("autoplay", "");
    } else {
      this.removeAttribute("autoplay");
    }
  }
  get controls() {
    return this.hasAttribute("controls");
  }
  set controls(value) {
    if (value) {
      this.setAttribute("controls", "");
    } else {
      this.removeAttribute("controls");
    }
  }
  get poster() {
    return this.getAttribute("poster") || "";
  }
  set poster(value) {
    if (value) {
      this.setAttribute("poster", value);
    } else {
      this.removeAttribute("poster");
    }
  }
  get currentSrc() {
    return this.mediaFacade.currentSrc;
  }
  get defaultMuted() {
    return this.hasAttribute("muted");
  }
  set defaultMuted(value) {
    if (value) {
      this.setAttribute("muted", "");
    } else {
      this.removeAttribute("muted");
    }
  }
  get defaultPlaybackRate() {
    return this.mediaFacade.defaultPlaybackRate;
  }
  set defaultPlaybackRate(value) {
    this.mediaFacade.defaultPlaybackRate = value;
  }
  get controlsMode() {
    return this.controlsManager.mode;
  }
  set controlsMode(value) {
    this.controlsManager.mode = value;
    this.setAttribute("controls-mode", value);
  }
  get tooltipEnabled() {
    return this._tooltipEnabled;
  }
  set tooltipEnabled(value) {
    const changed = this._tooltipEnabled !== value;
    this._tooltipEnabled = value;
    if (changed && !value) {
      this.dispatchEvent(new CustomEvent("tooltipenabledchange", { detail: { enabled: false } }));
    }
  }
  get videoWidth() {
    return this.mediaFacade.videoWidth;
  }
  get videoHeight() {
    return this.mediaFacade.videoHeight;
  }
  get ended() {
    return this.mediaFacade.ended;
  }
  get readyState() {
    return this.mediaFacade.readyState;
  }
  get networkState() {
    return this.mediaFacade.networkState;
  }
  get seeking() {
    return this.mediaFacade.seeking;
  }
  get fps() {
    return this.mediaFacade.fps;
  }
  get isHdr() {
    return this.mediaFacade.isHdr;
  }
  get hdrPrimaries() {
    return this.mediaFacade.hdrPrimaries;
  }
  get hdrTransfer() {
    return this.mediaFacade.hdrTransfer;
  }
  get hdrMaxLuminance() {
    return this.mediaFacade.hdrMaxLuminance;
  }
  get sourceWidth() {
    return this.mediaFacade.sourceWidth;
  }
  get sourceHeight() {
    return this.mediaFacade.sourceHeight;
  }
  get videoCodec() {
    return this.mediaFacade.videoCodec;
  }
  get audioCodec() {
    return this.mediaFacade.audioCodec;
  }
  get containerFormat() {
    return this.mediaFacade.containerFormat;
  }
  get bitrate() {
    return this.mediaFacade.bitrate;
  }
  get rotation() {
    return this.mediaFacade.rotation;
  }
  get sampleRate() {
    return this.mediaFacade.sampleRate;
  }
  get channels() {
    return this.mediaFacade.channels;
  }
  get buffered() {
    return this.eventManager.buffered;
  }
  get error() {
    return this.eventManager.error;
  }
  // ReadyState 常量
  get HAVE_NOTHING() {
    return ReadyState.HAVE_NOTHING;
  }
  get HAVE_METADATA() {
    return ReadyState.HAVE_METADATA;
  }
  get HAVE_CURRENT_DATA() {
    return ReadyState.HAVE_CURRENT_DATA;
  }
  get HAVE_FUTURE_DATA() {
    return ReadyState.HAVE_FUTURE_DATA;
  }
  get HAVE_ENOUGH_DATA() {
    return ReadyState.HAVE_ENOUGH_DATA;
  }
  // NetworkState 常量
  get NETWORK_EMPTY() {
    return NetworkState.NETWORK_EMPTY;
  }
  get NETWORK_IDLE() {
    return NetworkState.NETWORK_IDLE;
  }
  get NETWORK_LOADING() {
    return NetworkState.NETWORK_LOADING;
  }
  get NETWORK_NO_SOURCE() {
    return NetworkState.NETWORK_NO_SOURCE;
  }
  // MediaError 常量
  get MEDIA_ERR_ABORTED() {
    return MediaErrorImpl.MEDIA_ERR_ABORTED;
  }
  get MEDIA_ERR_NETWORK() {
    return MediaErrorImpl.MEDIA_ERR_NETWORK;
  }
  get MEDIA_ERR_DECODE() {
    return MediaErrorImpl.MEDIA_ERR_DECODE;
  }
  get MEDIA_ERR_SRC_NOT_SUPPORTED() {
    return MediaErrorImpl.MEDIA_ERR_SRC_NOT_SUPPORTED;
  }
  // ===== 播放控制方法 =====
  async play() {
    return this.mediaFacade.play();
  }
  pause() {
    this.mediaFacade.pause();
  }
  toggle() {
    this.mediaFacade.toggle();
  }
  nextFrame(n = 1) {
    this.mediaFacade.nextFrame(n);
  }
  prevFrame(n = 1) {
    this.mediaFacade.prevFrame(n);
  }
  setScrubbing(enabled) {
    this.mediaFacade.setScrubbing(enabled);
  }
  async load() {
    const src = this.src;
    if (!src) {
      return;
    }
    if (!isMacOSCompatible()) {
      this.eventManager.emitError(
        MediaErrorImpl.MEDIA_ERR_SRC_NOT_SUPPORTED,
        "Unsupported macOS version. Requires macOS 14 (Sonoma) or later."
      );
      return;
    }
    if (this.instanceManager.needsRecreate(this._hwdec)) {
      console.log(`[MpvVideoElement] hwdec changed to '${this._hwdec}', recreating instance...`);
      await this.instanceManager.recreate({ hwdec: this._hwdec });
    }
    const instanceReady = await this.instanceManager.ensureInstance();
    if (!instanceReady) {
      this.eventManager.emitError(
        MediaErrorImpl.MEDIA_ERR_ABORTED,
        "Failed to create video instance"
      );
      return;
    }
    await this.mediaFacade.load(src, this.lifecycleController?.signal);
    console.log(`[MpvVideoElement] Loaded: ${src} (connected: ${this.isConnected_})`);
  }
  abort() {
    this.mediaFacade.abort();
  }
  toggleFullscreen() {
    if (!document.fullscreenElement) {
      this.requestFullscreen();
    } else {
      document.exitFullscreen();
    }
  }
  // ===== 公開方法 =====
  getCanvas() {
    return this.canvas;
  }
  getInstanceId() {
    return this.instanceId;
  }
  get plugins() {
    return this.pluginManager;
  }
  get state() {
    return this.mediaState;
  }
  get theme() {
    return this.themeManager;
  }
  // ===== Preview Transform API（預覽用旋轉/翻轉）=====
  /**
   * 設置預覽旋轉角度（只支持 0, 90, 180, 270，內部自動 %360）
   * 用於將直拍意外變橫的視頻旋轉成正確方向
   * @param angle 旋轉角度（0, 90, 180, 270, 360, 450, 540... 內部自動 %360）
   */
  rotate(angle) {
    this.renderer.setPreviewRotation(angle);
  }
  /**
   * 設置預覽水平翻轉
   * @param value 翻轉值 (1 = 正常, -1 = 水平翻轉)
   */
  flip(value) {
    this.renderer.setPreviewFlip(value);
  }
  /** 當前預覽旋轉角度（只讀） */
  get previewRotation() {
    return this.renderer.getPreviewRotation();
  }
  /** 當前預覽翻轉狀態（只讀） */
  get previewFlip() {
    return this.renderer.getPreviewFlip();
  }
  // ===== Scale Mode API =====
  /**
   * 縮放模式
   * - 'fit': 縮放至填滿窗口（放大或縮小）
   * - 'original': 以原始大小顯示（如果視頻超出窗口則縮小，但不放大）
   */
  get scaleMode() {
    return this.renderer.getScaleMode();
  }
  set scaleMode(mode) {
    this.renderer.setScaleMode(mode);
  }
  // ===== Adaptive FPS API =====
  getFpsStats() {
    return this.fpsController.getStats();
  }
  getFpsSummary() {
    return this.fpsController.getSummary();
  }
  configureAdaptiveFps(config) {
    this.fpsController.updateConfig(config);
  }
  forceFpsDegradation(level) {
    this.fpsController.forceDegradation(level);
  }
  setAdaptiveFpsEnabled(enabled) {
    this.fpsController.updateConfig({ enabled });
  }
  isAdaptiveFpsEnabled() {
    return this.fpsController.getStats().isDegraded !== void 0;
  }
  // ===== Metadata & HDR Support =====
  async getMetadata() {
    return this.mediaFacade.getMetadata();
  }
  async getHdrInfo() {
    return this.mediaFacade.getHdrInfo();
  }
  async setHdrMode(enabled) {
    return this.mediaFacade.setHdrMode(enabled);
  }
  // ===== Screenshot API =====
  /**
   * 擷取指定時間的視頻截圖
   *
   * @param time 時間點（秒，支援小數點）
   * @param options 截圖選項
   * @returns ImageData 或 null（失敗時）
   *
   * @example
   * ```typescript
   * const video = document.querySelector('mpv-video');
   *
   * // 基本用法：取得原始尺寸截圖
   * const img = await video.screenshot(20.12);
   *
   * // 限制最大寬度（保持比例）
   * const thumb = await video.screenshot(20.12, { maxWidth: 320 });
   *
   * // 限制最大尺寸（保持比例）
   * const small = await video.screenshot(20.12, { maxWidth: 320, maxHeight: 240 });
   *
   * // 繪製到 Canvas
   * const canvas = document.createElement('canvas');
   * canvas.width = img.width;
   * canvas.height = img.height;
   * canvas.getContext('2d').putImageData(img, 0, 0);
   * ```
   */
  async screenshot(time, options) {
    return this.mediaFacade.screenshot(time, options);
  }
  // ===== Thumbnail 系統（和主視頻 RenderManager 相同的 Queue 架構）=====
  //
  // Queue 架構（不用 setTimeout 生產者）：
  //   seekThumbnail() → seek + renderFrame + 推送到 Queue
  //   rAF 消費者 → 從 Queue 取出幀（最多 3 幀追趕）→ dispatch 事件
  //
  // Thumbnail 流程：
  //   mouseenter → startThumbnailHover()（啟動 rAF）
  //   mousemove  → seekThumbnail(x)（seek + 推送幀到 Queue）
  //   mouseleave → stopThumbnailHover()（停止 rAF）
  /**
   * 開始 thumbnail hover
   */
  startThumbnailHover() {
    const videoPath = this.mediaFacade.currentSrc;
    if (!videoPath)
      return;
    if (!this.ensureThumbnailController(videoPath)) {
      return;
    }
    this.thumbnailHovering = true;
    this.thumbnailLastRenderTime = 0;
    this.thumbnailFrameQueue.length = 0;
    this.resetThumbnailIdleTimer();
    if (this.thumbnailRafId === null) {
      this.startThumbnailRenderLoop();
    }
  }
  /**
   * 結束 thumbnail hover
   */
  stopThumbnailHover() {
    this.thumbnailHovering = false;
    if (this.thumbnailRafId !== null) {
      cancelAnimationFrame(this.thumbnailRafId);
      this.thumbnailRafId = null;
    }
    this.thumbnailFrameQueue.length = 0;
  }
  /**
   * Seek thumbnail 到指定時間
   *
   * Queue 架構：
   * 1. 執行 seek
   * 2. 獲取幀並推送到 Queue
   * 3. rAF 會從 Queue 消費並 dispatch 事件
   */
  seekThumbnail(time) {
    if (!this.thumbnailController || !this.thumbnailHovering)
      return;
    this.resetThumbnailIdleTimer();
    this.thumbnailController.seek(time, "absolute");
    const frame = this.thumbnailController.renderFrame(
      this.thumbnailWidth,
      this.thumbnailHeight
    );
    if (frame && frame.width > 0 && frame.height > 0) {
      this.thumbnailFrameQueue.push({
        buffer: frame.buffer,
        width: frame.width,
        height: frame.height,
        format: frame.format,
        rotation: frame.rotation
      });
      while (this.thumbnailFrameQueue.length > _MpvVideoElement2.THUMBNAIL_MAX_QUEUE_SIZE) {
        this.thumbnailFrameQueue.shift();
      }
    }
  }
  /**
   * 設置 thumbnail 渲染尺寸
   */
  setThumbnailSize(width, height) {
    this.thumbnailWidth = width;
    this.thumbnailHeight = height;
  }
  /**
   * rAF 渲染循環（消費者，和主視頻 RenderManager 一樣）
   */
  startThumbnailRenderLoop() {
    const renderLoop = (timestamp) => {
      if (!this.thumbnailHovering || !this.thumbnailController) {
        this.thumbnailRafId = null;
        return;
      }
      this.thumbnailRafId = requestAnimationFrame(renderLoop);
      this.consumeThumbnailQueue(timestamp);
    };
    this.thumbnailRafId = requestAnimationFrame(renderLoop);
  }
  /**
   * 從 Queue 消費幀並 dispatch 事件（和主視頻 RenderManager 一樣）
   */
  consumeThumbnailQueue(timestamp) {
    const timeSinceLastRender = timestamp - this.thumbnailLastRenderTime;
    if (timeSinceLastRender < 16)
      return;
    if (this.thumbnailFrameQueue.length === 0)
      return;
    const maxFrames = Math.min(3, this.thumbnailFrameQueue.length);
    let lastFrame = null;
    for (let i = 0; i < maxFrames; i++) {
      lastFrame = this.thumbnailFrameQueue.shift();
    }
    if (lastFrame) {
      this.thumbnailLastRenderTime = timestamp;
      this.dispatchEvent(new CustomEvent("thumbnail", {
        detail: {
          buffer: lastFrame.buffer,
          width: lastFrame.width,
          height: lastFrame.height,
          format: lastFrame.format,
          rotation: lastFrame.rotation
        }
      }));
    }
  }
  /**
   * 確保縮圖控制器存在
   */
  ensureThumbnailController(videoPath) {
    try {
      if (this.thumbnailController && this.thumbnailVideoPath === videoPath) {
        return true;
      }
      this.destroyThumbnailController();
      this.thumbnailController = new NativeController({
        hwdec: this._hwdec,
        thumbnailMode: true
      });
      if (!this.thumbnailController.loadFile(videoPath)) {
        console.error("[MpvVideoElement] Thumbnail: failed to load video");
        this.destroyThumbnailController();
        return false;
      }
      this.thumbnailController.pause();
      this.thumbnailVideoPath = videoPath;
      console.log(`[MpvVideoElement] Thumbnail controller created (thumbnailMode: 320px@24fps, precise seek)`);
      return true;
    } catch (error) {
      console.error("[MpvVideoElement] Thumbnail: failed to create controller:", error);
      this.destroyThumbnailController();
      return false;
    }
  }
  /**
   * 銷毀縮圖控制器
   */
  destroyThumbnailController() {
    if (this.thumbnailRafId !== null) {
      cancelAnimationFrame(this.thumbnailRafId);
      this.thumbnailRafId = null;
    }
    if (this.thumbnailIdleTimer) {
      clearTimeout(this.thumbnailIdleTimer);
      this.thumbnailIdleTimer = null;
    }
    this.thumbnailFrameQueue.length = 0;
    if (this.thumbnailController) {
      try {
        this.thumbnailController.destroy();
      } catch (error) {
        console.error("[MpvVideoElement] Thumbnail: destroy error:", error);
      }
      this.thumbnailController = null;
    }
    this.thumbnailVideoPath = null;
    this.thumbnailHovering = false;
  }
  /**
   * 重置閒置計時器（超時後銷毀控制器釋放資源）
   */
  resetThumbnailIdleTimer() {
    if (this.thumbnailIdleTimer) {
      clearTimeout(this.thumbnailIdleTimer);
    }
    this.thumbnailIdleTimer = setTimeout(() => {
      console.log("[MpvVideoElement] Thumbnail: idle timeout, destroying");
      this.destroyThumbnailController();
    }, _MpvVideoElement2.THUMBNAIL_IDLE_TIMEOUT_MS);
  }
  // ===== 內部方法 =====
  setupFpsControllerCallbacks() {
    this.fpsController.onDegradationChange((stats, reason) => {
      console.log(`[MpvVideoElement] FPS degraded: ${stats.nativeFps}fps → ${stats.effectiveFps.toFixed(1)}fps (level ${stats.degradationLevel})`);
      this.dispatchEvent(new CustomEvent("fpsdegraded", {
        detail: { ...stats, reason }
      }));
    });
    this.fpsController.onRecoveryChange((stats, reason) => {
      console.log(`[MpvVideoElement] FPS recovered: effective FPS now ${stats.effectiveFps.toFixed(1)}fps (level ${stats.degradationLevel})`);
      this.dispatchEvent(new CustomEvent("fpsrecovered", {
        detail: { ...stats, reason }
      }));
    });
  }
  updateCanvasSize() {
    const rect = this.getBoundingClientRect();
    if (rect.width <= 0 || rect.height <= 0)
      return;
    this.cachedElementWidth = rect.width;
    this.cachedElementHeight = rect.height;
    const targetWidth = Math.round(rect.width * window.devicePixelRatio);
    const targetHeight = Math.round(rect.height * window.devicePixelRatio);
    if (this.canvas.width === targetWidth && this.canvas.height === targetHeight) {
      return;
    }
    if (this.resizeRafId) {
      return;
    }
    this.resizeRafId = requestAnimationFrame(() => {
      this.resizeRafId = null;
      const latestRect = this.getBoundingClientRect();
      if (latestRect.width <= 0 || latestRect.height <= 0)
        return;
      const finalWidth = Math.round(latestRect.width * window.devicePixelRatio);
      const finalHeight = Math.round(latestRect.height * window.devicePixelRatio);
      this.cachedElementWidth = latestRect.width;
      this.cachedElementHeight = latestRect.height;
      this.canvas.width = finalWidth;
      this.canvas.height = finalHeight;
      this.renderer.updateViewport();
      this.renderer.redraw();
    });
  }
  /**
   * 完全銷毀元素和所有資源
   */
  destroy() {
    this.pluginManager.destroyAll();
    this.renderManager.stopRenderLoop();
    this.mediaFacade.stopStateSync();
    this.controlsManager.destroy();
    this.fpsController.destroy();
    this.destroyThumbnailController();
    if (this.resizeObserver) {
      this.resizeObserver.disconnect();
      this.resizeObserver = null;
    }
    this.renderManager.clearCache();
    this.eventManager.reset();
    this.renderer.destroy();
    this.instanceManager.destroy();
    console.log(`[MpvVideoElement] Destroyed: ${this.instanceId}`);
  }
};
_MpvVideoElement.THUMBNAIL_IDLE_TIMEOUT_MS = 5e3;
_MpvVideoElement.THUMBNAIL_MAX_QUEUE_SIZE = 8;
_MpvVideoElement.connectedInstances = /* @__PURE__ */ new Set();
let MpvVideoElement = _MpvVideoElement;
applyMediaEventHandlers(MpvVideoElement.prototype);
customElements.define("mpv-video", MpvVideoElement);
window.MpvVideoElement = MpvVideoElement;
exports.EventBus = PluginManager.EventBus;
exports.GlobalPluginRegistry = PluginManager.GlobalPluginRegistry;
exports.MediaEventBus = PluginManager.MediaEventBus;
exports.PluginManager = PluginManager.PluginManager;
exports.Signal = PluginManager.Signal;
exports.batchUpdate = PluginManager.batchUpdate;
exports.createMediaState = PluginManager.createMediaState;
exports.createSimplePlugin = PluginManager.createSimplePlugin;
exports.definePlugin = PluginManager.definePlugin;
exports.Button = components_index.Button;
exports.Component = components_index.Component;
exports.Slider = components_index.Slider;
exports.ToggleButton = components_index.ToggleButton;
exports.allPlugins = plugins_index.allPlugins;
exports.defaultControlPlugins = plugins_index.defaultControlPlugins;
exports.defaultExtensionPlugins = plugins_index.defaultExtensionPlugins;
exports.defaultPlugins = plugins_index.defaultPlugins;
exports.ControlPlugin = ControlPlugin.ControlPlugin;
exports.PlayButtonPlugin = plugins_playButton_index.PlayButtonPlugin;
exports.FrameStepPlugin = plugins_frameStep_index.FrameStepPlugin;
exports.VolumePlugin = plugins_volume_index.VolumePlugin;
exports.TimeDisplayPlugin = plugins_timeDisplay_index.TimeDisplayPlugin;
exports.ProgressPlugin = plugins_progress_index.ProgressPlugin;
exports.SpeedPlugin = plugins_speed_index.SpeedPlugin;
exports.LoopButtonPlugin = plugins_loopButton_index.LoopButtonPlugin;
exports.FullscreenPlugin = plugins_fullscreen_index.FullscreenPlugin;
exports.KeyboardPlugin = plugins_keyboard_index.KeyboardPlugin;
exports.ABLoopPlugin = plugins_abLoop_index.ABLoopPlugin;
exports.ThumbnailPlugin = plugins_thumbnail_index.ThumbnailPlugin;
exports.HdrPlugin = plugins_hdr_index.HdrPlugin;
exports.DebugPlugin = plugins_debug_index.DebugPlugin;
exports.NoteManager = plugins_notes_index.NoteManager;
exports.NotePlugin = plugins_notes_index.NotePlugin;
exports.MpvVideoElement = MpvVideoElement;
exports.isNativeModuleAvailable = isNativeModuleAvailable;
//# sourceMappingURL=index.cjs.map
