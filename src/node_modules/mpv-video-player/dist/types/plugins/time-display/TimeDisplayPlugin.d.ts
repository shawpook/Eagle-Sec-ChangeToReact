import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface TimeDisplayPluginOptions extends ControlPluginOptions {
    /** 是否顯示總時長 */
    showDuration?: boolean;
}
export declare const TimeDisplayPlugin: MpvPlugin<TimeDisplayPluginOptions>;
export declare class TimeDisplayManager extends ControlPlugin<Required<TimeDisplayPluginOptions & {
    position: 'progress';
    order: number;
}>> {
    readonly name = "timeDisplay";
    private timeCurrent;
    private timeDuration;
    protected createEl(): HTMLElement;
    /**
     * 覆寫插入方法，將時間顯示插入到 progress-section 的開頭
     */
    protected insertElement(): void;
    private updateTime;
}
//# sourceMappingURL=TimeDisplayPlugin.d.ts.map