# Eagle React 化改造 — 任务交接（2026-09-01，9a-1 进行中）

> 交接锚点：`bd6df79`（9a 深勘察已提交）。续作依据：`src/app/react/PROGRESS.md`（唯一权威）+ 本文档。
> 本次会话已落盘 **3 个新文件（未提交）**，9a-1 尚未完成、尚未验证。下一会话从「修 shell.tsx → 补 entry/静态壳/接线 → tsc → 回归」继续。

---

## 1. 当前状态总览

| 项 | 状态 |
| --- | --- |
| 阶段 | 9a-1（preview-window 迁移，第一片：接线 + controller/shell 骨架 + image/svg/gif/pdf/video/custom/plugin 分支 + driver 改写） |
| 已提交锚点 | bd6df79（9a 深勘察：驱动契约/init 序列/指令清单/分片计划，全部仍有效） |
| 本会话新增（未提交） | `src/app/react/preview-window/controller.ts`（完整）、`src/app/react/preview-window/detailHooks.ts`（完整）、`src/app/react/preview-window/shell.tsx`（**初稿，含已知待修项**） |
| 尚未写 | `entry.tsx`、`preview-window.html` 静态壳改造、`vite.preview.config.mjs` 分支、`shims.js` 缓冲、`electron/main.cjs` driver 替换 |
| 验证 | **tsc 尚未运行**；回归门 `tests/preview-delivery-closed-loop.mjs`（当前 Angular 态全绿）尚未跑 |
| 工作树其他未提交 | 非任务文件：`tests/probe-7d2.mjs`（M）、`.zcode/plans/plan-sess_*.md`（D）、`docs/plan.md`（D）——**禁止提交** |

---

## 2. 本会话已完成的工作

### 2.1 前置勘察（全部结论已固化，可直接使用）

- **驱动契约**（main.cjs `--smoke-preview-delivery`，2474-2880）读毕：全程走 `window.$bodyScope`（current/images/selectNext()/selectPrev()/$evalAsync()/zoom()/getRawUrl()/getRawPath()/openWithDefault()/openWithFinder()/copyAsPath()/copyImage()/startDrag()/useMpvPlayer）+ DOM（#detail-image/#gif-viewer/#pdf-viewer/video）。**迁移 = 3 处 `window.$bodyScope` 机械替换为 `window.__eaglePreviewController`，其余 driver 代码零改动**（2505/2533/2821 行附近，`waitForPreview` 与 reopened 两段）。
- **preview-window.js 全文（2565 行）与 preview-window.html（383 行）已通读**，控制器函数族/指令/分支逐字掌握（见 PROGRESS 9a 深勘察行号）。
- **shims 桥接**：`preview:init`（main 在 onDidFinishLoad 发）→ preload `onInit` → shims.js **1734-1736** `desktopApi.preview.onInit(payload => mockEmit('init', payload))`。**竞态**：React 入口冷启动 vite transform 可能慢于该 emit → 'init' 丢失（8e-2 同款）。**对策：该桥改为缓冲**（见 §4.3）。
- **阶段5 hooks 复用性**：`useMediaElement`/`useMpvMediaElement`（detailHooks）明确支持 preview-window（`$('#preview-window')` 检测），但**依赖 window.$bodyScope（含 $on/$watch/$evalAsync/$apply）与 window.preferences** → controllerScope 必须提供这些门面（已实现）。
- **smoothZoom 插件依赖**（jquery.smoothZoom.min.js）：5 处 `angular.element("body").scope()` 兜底 + 全局 `$bodyScope.startDrag` 引用 + 读写 `$scope.imageSize.zoomRatioExp/getRatioNonExp/updateDetailSlider/$evalAsync`。**预览窗移除 Angular 后必须提供 `window.angular` 微型 stub**（见 §4.1），否则滚轮/拖拽炸。
- **gif-viewer iframe**（gif-viewer/index.html:201）读 `window.parent.$bodyScope` → 保持 `window.$bodyScope = controllerScope` 即 9a-2 的 gif 播放器 iframe 链路不断。
- **`zoomRatioExp` 由 smoothZoom 插件赋值**（非控制器），模板 `zoom-in` class 依赖它 → React 渲染时用 `controllerScope.imageSize.zoomRatioExp || 100`。
- **image 分支用缩略图作为 zoomer 代理源**（`getThumbnailUrl(current) || 1px base64`，margin-left:-2px）——与主窗口详情模式不同，逐字保留。
- **electron-settings 获取**：`req(String(req('app-root-path')) + '/my_modules/electron-settings')`（preferences/controller.ts:925 同款，real-electron 走真实模块、浏览器走 shims resolver）。

### 2.2 已写文件

#### `src/app/react/preview-window/controller.ts`（完整，~900 行）
- 模块级：EagleConfig 类型表（VIDEO/AUDIO/MODEL/FONT/URL_TYPES）、fontFolder、MPV 注册（try/catch）、videojs SeekBar 覆写、dragover/drop、gup、`window.preferences = preferences`。
- `controllerScope` 普通对象：全部字段 + 方法（构造期静态字段、getRawUrl/getThumbnailUrl/getRawPath/getPDFPath/getGIFPath/getTxtPath/getFontPath/getModelPath/getURLSrc/各 viewer path、缩放体系 updateZoomRatio/zoomActual/zoomFit/zoomFitEdge/smartZoom/isMobileResolution/toggleZoom/zoomIn/Out、导航 selectNext/selectPrev、窗口控制 close/minimize/maximize/restore/toggleFullScreen/toggleAlwaysOnTop、拖拽 startDrag/onMiddleClick、动作 openWithDefault/openWithFinder/copyAsPath/copyAsLink/copyImage、右键 openContextMenu/openRatioContextMenu/openGifContextMenu、视频 videoScreenShot/saveVideoFrame/copeVideoFrame/rotateVideo/rotateImage/flipImage/flipVideo/rotateHandler/flipHandler/toggleVideoPlay/spaceHandler/mHandler、gif gifViewer 对象全量 + toggleGifPlay/nextGifFrame/prevGifFrame/toggleGifPlayerMode、字体 isFontActivate/activateFont/deactivateFont、getMetas/currentIndex/getRatioExp/getRatioNonExp）。
- **门面**：`$root`（自引用）、`$$phase:false`、`$watch(expr,fn)`（支持 'theme'/'current.id'，notify 时差量触发）、`$on`（no-op 返回解绑）、`$evalAsync(fn?)`/`$apply(fn?)` = fn+notify。
- **订阅**：subscribeController/getControllerVersion/applyController/notifyController，notify 带重入折叠。
- **init 序列** `runInitSequence(params)`：581-658 逐字（pluginModule 四方法赋值、imagesDir darwin/win32 encodeURI、current=images[0]、metas、**notifyController() 后 initContainer()**——保持「先 $evalAsync 后 initContainer」的 Angular 同步顺序）。
- **initShellBehaviors**：Shift 拖拽 overlay、#min/max/restore、resize 节流、光标 500ms 巡检、blur/mouseenter/mousemove hide-toolbar、gif 工具列 5 组委托（mousedown/mouseup/mouseleave/mousemove×2）。
- **buildMousetrapBindings**：2265-2363 逐字（preferences keybinds + 硬编码合并）。
- **initContainer**：821-850 逐字（smoothZoom 选项 + on_IMAGE_LOAD：updateNavigator → orientationchange → zoom → opacity → showDetailImage=true+notify）。
- **契约**：`window.__eaglePreviewController = scope; window.$bodyScope = scope;`。
- 已知保留项：`notify()` 为占位（cgNotify 等价层 9a-2 补，当前仅 font 通知路径会用到）；`$('#min-btn')` 等死绑定逐字保留。

#### `src/app/react/preview-window/detailHooks.ts`（完整）
- `usePreviewMouseGesture(ref)`：preview-window.js 128-411 **逐字**（与主窗口 useMouseGesture **不同**：linear-800 缩放公式、333ms 换页阈值、右键 `openContextMenu(event,current)`）。
- `usePreviewTgaImage(ref, rawUrl)`：2367-2440 逐字。
- `usePreviewMousetrap()`：wMousetrap 语义（Mousetrap.bind + throttle(50) + $evalAsync，卸载解绑）。

#### `src/app/react/preview-window/shell.tsx`（**初稿，含待修项，见 §3**）
- 架构：静态壳 + 4 锚点 portal（toolbar/content/#detail-container/footbar），body class 差量维护（插值串变化才重写 base，ng-class 每次 notify 应用——原版 digest 语义）。
- 已含：ToolbarSwitch（7 分支）、PreviewNavigator、toolbar-btn* 系列、RatioButton、DetailContainerInterior（14 分支）、GifFootbar、ImageBranch/SvgBranch/GifBranch/NativeVideoBranchPreview/MpvVideoBranchPreview/TgaBranch/SpecialBranch/CustomBranch。

---

## 3. shell.tsx 待修项（下一会话第一步，按此清单修完再 tsc）

1. **ToolbarBtnPin / ToolbarBtnUnpin**：删除非法 `style2={undefined}` 与 `data-hidden`；**改为双渲染**（ng-show 语义：两按钮都在 DOM，`style={ngShowStyle(isAlwaysOnTop)}` / `ngShowStyle(!isAlwaysOnTop)`），PinCloseGroup 与 gif 工具列组同步改双渲染（原 DOM 两个 ic-btn 并存）。
2. **gif 速度菜单**：`call('gifViewer.setSpeed', value)` 错误（call 只解析顶层方法）→ 改为 `onClick={() => applyController(s => s.gifViewer.setSpeed(value))}`。
3. **plugin 分支**：`<plugin-view>` 应接一个 `PreviewPluginView` 组件（port 阶段5 DetailViewer PluginView：创建 webview + preload + dom-ready 时按 allowZoom 设 style + executeJavaScript 注入 + plugin-create/plugin-run），删除 `ng-src=""` 写法。
4. **CommentsLayerSvg / CommentItemBoxSvg**：改用真实 `useCommentsContainer(current?.id, hasComments)` 与 `useCommentItem(elRef, index, {enabled:true})`（import 自 `../components/detail/commentHooks`），ratio 读 `controllerScope.ratio || 1`，删除自写 no-op stub。
5. **notSupportRef 时序**：把 not-support-preview 抽成独立 `NotSupportPreview` 组件（内部 ref + usePreviewMouseGesture），避免「ng-if 挂载晚于 hook effect」丢监听（原指令随 ng-if 实例化）。
6. **alway-show-toolbar**：改用 `scope.FONT_TYPES[ext] || scope.URL_TYPES[ext] || ext==='txt' || ext==='pdf' || scope.MODEL_TYPES[ext] || pluginModule?.previewExtension?.viewerPluginMap?.[ext]`，删除硬编码 FONT_TYPES_SET 等。
7. **ToolbarBtnZoomActual tippy-content**：`{{ '<key>Command</key><key>0</key>' | shortcuts }}` 用 `shortcuts('<key>Command</key><key>0</key>')`（import 自 `../app/filters`）求值，删除字面量。
8. **清理未用导入**：`useMemo`、`notifyController`（controller 导出但 shell 未用）等，避免 tsc noUnusedLocals 报错。
9. **WebviewToolbar adapter**：`{ theme } as any` 可保留（其内部只用 snapshot.theme）；url 分支 9a-2 补全 web-view 宿主。

---

## 4. 关键决策（不可丢失的移植要点）

### 4.1 preview-window.html 静态壳（下一步要写，含 5 行 angular stub）
- head 保留：i18n 内联 script、tippy.js（vendor）、jquery-1.8.0.min.js、jquery-ui.min.js、jquery.smoothZoom.min.js、mousetrap.min.js、tif-img.js、videojs.js + 4 语言包、shortcut-manager.js、global.js、devices.js；css：base/preview-window-new/preview-window/jquery-ui.min.css/video-js.css/angular-notify.min.css。
- head **删除**：angular.min.js、angular-notify.min.js、modules/tippy.js、wMousetrap.js、media-element.js、mpv-media-element.js、plugin-view.js、preview-window.js、webview.js、webview-toolbar.js、filters.js。
- **inline stub（必须在 react entry 之前）**：
  ```html
  <script>
    window.angular = { element: function () { return { scope: function () { return window.$bodyScope; } }; } };
  </script>
  ```
  （smoothZoom 5 处 `angular.element("body").scope()` 兜底；`window.$bodyScope` 由 controller 模块置。）
- body 静态壳（顺序与原版一致）：
  ```html
  <body id="preview-window" class=" hide-toolbar">
    <div id="drag-mode-overlay"></div>
    <div class="drag-area"></div>
    <div id="eagle-preview-toolbar-anchor"></div>
    <div class="container" style="position: fixed; z-index: 9999;">
      <div id="eagle-preview-content-anchor"></div>
      <div id="detail-container" mouse-gesture="" image="current" rect="commentRect" class="box-container"></div>
    </div>
    <div id="eagle-preview-footbar-anchor"></div>
    <div id="eagle-preview-react-host"></div>
  </body>
  ```
  `#detail-container` 元素身份永不重建（smoothZoom wrap 关系）；React 只 portal 其 interior（同阶段5 先例）。

### 4.2 controller 订阅/门面（已实现，别改坏）
- notify 重入折叠（`notifying` 标志）——等价 $phase 语义；`applyController`=ng-click $apply 等价；`notifyController`=$evalAsync 等价。
- `$watch` 只实现 'theme'/'current.id'（useMpvMediaElement 恰好只 watch 这两个）；`$on` 返回 no-op 解绑（useMediaElement `$on('REFRESH_VIDEO_COMMENTS')` 需要）。
- `$root = scope` 自引用 + `$$phase:false`——scopeBridge.scopeApply 的 `scope.$root.$$phase` 检查不炸。
- `window.preferences` 必须由 controller 构造期设置（useMediaElement initShortcuts 读 `(window as any).preferences.shortcuts.keybinds`）。

### 4.3 shims.js 两处修改（下一步）
1. **1734-1736 桥改缓冲**：`desktopApi.preview.onInit(payload => { window.__eaglePendingPreviewInit = payload; if (window.__eaglePreviewEntryReady) mockEmit('init', payload); })`（real-electron 路径，防 vite 冷启动丢 init）。
2. **preview-window 分支（3036-3052）改 marker 等待**（浏览器 mock 路径，仿 preferences 8e-2）：轮询 `window.__eaglePreviewEntryReady`（25ms），就绪后 emit 缓冲载荷（优先 `__eaglePendingPreviewInit`）否则 mock 载荷（images/imagesDir/rootDir/machineID/Registration/pluginModule，image = items 按 `?id=` 或 items[0]）；保留 `if (desktopApi && desktopApi.preview) return;`。

### 4.4 entry.tsx（下一步写，8e-2 教训）
- `createRoot(host).render(<PreviewShell/>)`；ipc 'init' → `applyController(s => s.runInitSequence(params))`。
- **就绪标记 `window.__eaglePreviewEntryReady = true` 必须在注册 'init' 监听器的 effect 内（commit 后）**，不得在模块尾部（probe 定位过的竞态）。
- host = `#eagle-preview-react-host`（静态壳尾部空 div）。

### 4.5 main.cjs driver 替换（下一步）
- 仅 3 处字符串机械替换 `window.$bodyScope` → `window.__eaglePreviewController`（约 2514/2533/2802 行，另 2821 行 reopened 段内）。其余 driver 零改动。

### 4.6 vite 分支（下一步）
- `readPreviewWindow()`：读 preview-window.html → injectPreviewScripts（已有）→ 尾插 REACT_REFRESH_PREAMBLE + `<script type="module" src="/src/app/react/preview-window/entry.tsx">`；middleware 加 `/src/app/preview-window.html` 分支（仿 readPreviewPreferences）。

---

## 5. 完成 9a-1 的步骤清单（下一会话按序执行）

1. 修 shell.tsx（§3 清单 1-9）。
2. 写 `src/app/react/preview-window/entry.tsx`（§4.4）。
3. 改造 `src/app/preview-window.html` 静态壳（§4.1）。
4. `frontend/vite.preview.config.mjs` 加 readPreviewWindow + 分支（§4.6）。
5. `frontend/public/shims.js` 两处修改（§4.3）。
6. `electron/main.cjs` driver 替换（§4.5）。
7. `npx tsc --noEmit` 零错 → 有错即修再跑。
8. 回归门：`node tests/preview-delivery-closed-loop.mjs`（**9a-1 验收门**，断言见下）。
9. 全量回归：`node tests/run-react-suite.mjs`；`node tests/run-api-smoke-isolated.mjs`。
10. PROGRESS.md 更新（9a-1 完成块 + 状态表行）+ git commit（**只提交任务文件**：preview-window/ 三件 + entry + html + vite + shims + main.cjs + PROGRESS；`tests/probe-7d2.mjs`/`.zcode/plans/`/`docs/plan.md` 禁止提交）。

### 回归门断言面（closed-loop assertRound，9a-1 必须全绿）
imageLoaded(#detail-image naturalWidth>0)、svg.width、gif.mode(image 或 gif-viewer canvas)、pdf.page(.pdfViewer .page)、jpg.width、video.readyState>=1、videoInteraction(volume=0.5,currentTime>0.05)、badVideo.unsupported(mp4 坏文件)、fakeOpen/Copy/Reveal/Drag 拒绝、trashRejected/missingRejected(round2)、renamedResult(name/rawPath)、openDefault/reveal/copyPath/copyImage/drag ok。
→ 故 9a-1 必须覆盖：image/svg/gif(image 态)/pdf/video(含 bad→useMpvPlayer 回退)/custom/plugin 分支 + 全部动作 + smoothZoom 初始化。

### 已知 flake（重跑即绿，勿误判为回归）
main-ui-workflow markdown 缩略图计数、react-stage-smoke、multi-inspector-persistence；9a 新冒烟若冷启动首跑红，先重跑一次再查。

---

## 6. 后续阶段（9a-1 之后）

- **9a-2**：txt/font/url/model/raw/tga/特殊格式 分支交互补全 + gif iframe 播放器链路（gif-viewer iframe 经 window.parent.$bodyScope 已通）+ cgNotify 等价层（controller.notify 占位）+ webview-toolbar 宿主。
- **9a-3**：工具列 tippy 实际挂载（当前 tippy 属性惰性）、右键菜单细节（openContextMenu 已转写）、拖拽模式 overlay（已转写）、grayscale、window 控制按钮实测。
- **9b** collect-window（独立 Angular app，浏览器扩展采集流）。
- **阶段11**：index.html 移除 angular/app.bundle（届时 smoothZoom 的 angular stub 需全局化或 hook 化——9a 已在 preview-window.html 局部解决）。
- 旧文件 `src/app/js/preview-window.js` 在 9a 各片完成（含验证）后按「只改写不删除」节奏删除，preview-window.html 的 ng-* 残留随阶段11 清理。

---

## 7. 铁律速查（续作时同样适用）

- 原版界面像素级一致：DOM 结构与 class 逐字，复用原 CSS。
- 功能不改变：每按钮/快捷键/右键/拖拽行为一致；gifViewer 等 typo（gifUpadteInterval）保留。
- 数据面零改动：ipc 通道名/localStorage 键/electron-settings 键逐字（'get.viewer.image'/'ondragstart'/'open-with-default'/'show-item-in-folder'/'copy-images'/'screencapture-from-extension'/'reveal-in-eagle'/'regenerate-thumbnail'/'regenerate-gif-thumbnail'、eagle.viewer.lastZoomMode/isHideNavigator_Viewer/eagle.videoPlayer.volume）。
- 不在 `src/app` 之外改文件（例外：preferences.html 类接线、vite.preview.config.mjs、shims.js、electron/main.cjs 测试驱动、tests/、mock-library 清理）。
- 旧实现未验证不删除；「React 验证过 + 旧未删」不提交。
