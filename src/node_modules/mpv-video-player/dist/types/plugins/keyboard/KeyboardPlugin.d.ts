import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * 快捷鍵動作類型
 */
export type KeyboardAction = (player: MpvVideoElement, event: KeyboardEvent) => void;
/**
 * 快捷鍵映射
 */
export interface KeyboardShortcut {
    /** 按鍵（KeyboardEvent.key） */
    key: string;
    /** 是否需要 Ctrl/Cmd */
    ctrl?: boolean;
    /** 是否需要 Shift */
    shift?: boolean;
    /** 是否需要 Alt */
    alt?: boolean;
    /** 動作函數 */
    action: KeyboardAction;
    /** 描述（用於幫助文檔） */
    description?: string;
}
/**
 * 快捷鍵插件配置
 */
export interface KeyboardPluginOptions {
    /** 自定義快捷鍵（會與預設合併） */
    shortcuts?: KeyboardShortcut[];
    /** 是否啟用全局快捷鍵（即使播放器未聚焦也生效） */
    global?: boolean;
    /** 是否在輸入框中禁用快捷鍵 */
    disableInInputs?: boolean;
    /** 是否顯示快捷鍵提示 */
    showHints?: boolean;
    /** seek 步進（秒） */
    seekStep?: number;
    /** 音量步進（0-1） */
    volumeStep?: number;
}
/**
 * 快捷鍵插件定義
 */
export declare const KeyboardPlugin: MpvPlugin<KeyboardPluginOptions>;
/**
 * 快捷鍵管理器
 */
export declare class KeyboardManager implements PluginInstance {
    readonly name = "keyboard";
    private player;
    private options;
    private shortcuts;
    private _enabled;
    private keydownHandler;
    constructor(player: MpvVideoElement, options: Required<KeyboardPluginOptions>);
    /**
     * 標準化按鍵名稱
     */
    private normalizeKey;
    /**
     * 處理按鍵事件
     */
    private handleKeydown;
    /**
     * 添加快捷鍵
     */
    addShortcut(shortcut: KeyboardShortcut): void;
    /**
     * 移除快捷鍵
     */
    removeShortcut(key: string, action?: KeyboardAction): void;
    /**
     * 獲取所有快捷鍵
     */
    getShortcuts(): KeyboardShortcut[];
    /**
     * 啟用快捷鍵
     */
    enable(): void;
    /**
     * 禁用快捷鍵
     */
    disable(): void;
    /**
     * 是否啟用
     */
    get enabled(): boolean;
    /**
     * 銷毀插件
     */
    destroy(): void;
}
//# sourceMappingURL=KeyboardPlugin.d.ts.map