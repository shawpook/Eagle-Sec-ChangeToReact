/**
 * MPV Video Player - 共享類型定義
 *
 * 這些類型定義供參考使用
 */
/**
 * 視頻播放狀態
 */
export interface VideoState {
    /** 當前播放位置（秒） */
    position: number;
    /** 視頻總時長（秒） */
    duration: number;
    /** 是否暫停 */
    paused: boolean;
    /** 音量（0-100） */
    volume: number;
    /** 是否正在播放 */
    playing: boolean;
}
/**
 * 視頻幀數據
 */
export interface FrameData {
    /** 幀數據緩衝區 */
    buffer: ArrayBuffer;
    /** 幀寬度 */
    width: number;
    /** 幀高度 */
    height: number;
    /** 旋轉角度 */
    rotation: number;
    /** 像素格式 */
    format: 'rgba' | 'yuv420';
}
/**
 * 幀元數據
 */
export interface FrameMetadata {
    /** 幀寬度 */
    width: number;
    /** 幀高度 */
    height: number;
    /** 旋轉角度 */
    rotation: number;
    /** 像素格式 */
    format: 'rgba' | 'yuv420';
}
/**
 * 視頻事件
 */
export interface VideoEvent {
    /** 事件類型 */
    type: string;
    /** 實例 ID */
    instanceId?: string;
    /** 是否錯誤 */
    error?: boolean;
    /** 錯誤碼 */
    errorCode?: number;
    /** 錯誤訊息 */
    errorMessage?: string;
    /** 其他屬性 */
    [key: string]: unknown;
}
/**
 * 視頻元數據
 */
export interface VideoMetadata {
    /** 視頻時長（秒） */
    duration: number;
    /** 原始寬度 */
    width: number;
    /** 原始高度 */
    height: number;
    /** 旋轉角度 */
    rotation: number;
    /** 容器格式 */
    format: string;
    /** 視頻編碼 */
    videoCodec: string;
    /** 音頻編碼 */
    audioCodec: string;
    /** 碼率（bps） */
    bitrate: number;
    /** 幀率（fps） */
    fps: number;
    /** 是否 HDR 內容 */
    isHdr: boolean;
    /** HDR 色彩原色 */
    hdrPrimaries: string;
    /** HDR 傳輸特性 */
    hdrTransfer: string;
    /** HDR 最大亮度 */
    hdrMaxLuminance: number;
    /** 音頻採樣率（Hz） */
    sampleRate: number;
    /** 音頻聲道數 */
    channels: number;
}
/**
 * HDR 資訊
 */
export interface HdrInfo {
    /** 是否 HDR 內容 */
    isHdr: boolean;
    /** 色彩原色（如 'bt.709', 'bt.2020'） */
    primaries: string;
    /** 傳輸函數（如 'bt.1886', 'pq', 'hlg'） */
    transfer: string;
    /** 峰值亮度（nits） */
    maxLuminance: number;
    /** HDR 輸出模式是否啟用 */
    hdrModeEnabled: boolean;
}
/**
 * 檔案支援檢查結果
 */
export interface FileSupportResult {
    /** 是否支援 */
    supported: boolean;
    /** 是否有視頻軌 */
    hasVideo: boolean;
    /** 是否有音頻軌 */
    hasAudio: boolean;
    /** 錯誤訊息 */
    error: string | null;
    /** 容器格式 */
    format?: string;
    /** 視頻編碼 */
    videoCodec?: string;
    /** 音頻編碼 */
    audioCodec?: string;
}
/**
 * 目標解析度（用於幀縮放）
 */
export interface TargetResolution {
    /** 目標寬度（像素） */
    width: number;
    /** 目標高度（像素） */
    height: number;
}
/**
 * 縮圖數據
 */
export interface ThumbnailData {
    /** 縮圖數據緩衝區 */
    buffer: ArrayBuffer;
    /** 縮圖寬度 */
    width: number;
    /** 縮圖高度 */
    height: number;
    /** 對應的視頻時間（秒） */
    time: number;
}
//# sourceMappingURL=types.d.ts.map