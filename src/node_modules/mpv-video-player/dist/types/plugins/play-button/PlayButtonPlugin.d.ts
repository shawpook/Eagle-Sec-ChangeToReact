import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface PlayButtonPluginOptions extends ControlPluginOptions {
    /** 按鈕位置：left 或 right */
    position?: 'left' | 'right';
    /** 插入順序（數字越小越前面） */
    order?: number;
}
export declare const PlayButtonPlugin: MpvPlugin<PlayButtonPluginOptions>;
export declare class PlayButtonManager extends ControlPlugin<Required<PlayButtonPluginOptions>> {
    readonly name = "playButton";
    protected createEl(): HTMLElement;
    private updateState;
}
//# sourceMappingURL=PlayButtonPlugin.d.ts.map