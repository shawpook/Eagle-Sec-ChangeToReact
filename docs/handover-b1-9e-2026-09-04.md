# Eagle React 化 · 阶段 11 交接（2026-09-04 v6 / b1-9e 收口后）

写给**零上下文的新会话**。分支 `react-in-place`，仓库 `H:\dev\Eagle-Sec-development - 副本`
（Windows 11 + Git Bash）。HEAD = `17b1cf2`。

**本文取代 `docs/handover-b1-9d-2026-09-04.md`（v5）**。v5 的战线（m1 同 id 双发）已结案，
其「三假说」调查结论见本文 §一.3 与 §三.5；v5 只作历史查阅，勿再按其 To-Do 执行。

> **⚠️ 项目级约束 —— markdown 组件隔离、完全不处理**（用户裁定；权威表述在
> `src/app/react/PROGRESS.md:14` 同名节）。已隔离点：
> `tests/main-ui-workflow-closed-loop.mjs` 不建 markdown 样例、不传
> `EAGLE_WORKFLOW_MARKDOWN_SOURCE`；`electron/main.cjs` 的 `markdownSource` 恒空、
> `markdown meta shows file size` 闸门恒跳过，且**终局 `ok` 合取里的三项 markdown 断言
> 已于 b1-9e 摘除**（否则全门通过也只会打印 `MAIN_WORKFLOW_SMOKE_FAIL`）。
> **任何会话不得为满足 markdown 断言改业务代码，也不得把它算作回归。**

---

## 一、核心板块

### 1. 项目核心目标

把 Eagle 桌面端主窗口的 Angular 1 应用**原地替换**为 React：`src/app/app.bundle.js` 里的
`EagleController`（bundle 20197-54236，约 34,000 行「页面内后端」）与 `eagle` 对象族逐字端口
到 `src/app/react/**`，主窗口 `src/app/index.html` 已**不再加载** `angular*.js` 与
`app.bundle.js`（阶段 11 的 b1 已实质完成，见 §一.2）。

当前阶段判据 = `npm run test:main-ui-workflow`（下称 **m1**）从头跑到
`MAIN_WORKFLOW_SMOKE_OK` + `MAIN_UI_RESTART_OK`。

### 2. 已完成进度（截至 `17b1cf2`）

- **m1 全绿**（连续两轮）：`MAIN_WORKFLOW_SMOKE_OK` + `MAIN_UI_RESTART_OK`，EXIT:0。
  `detailDelivery` = `mode:"canvas"` / `tileCount:10` / `canvas 818x752` / `visible:true`。
- `npm run test:isolated` → `FULL_REGRESSION_ISOLATED_OK`，EXIT:0。
- `npx tsc --noEmit -p tsconfig.json` → EXIT:0。
- 阶段 11 的 **11-pre a1→a9 全部已验证并接管**（PROGRESS.md:1764 起逐片记录），
  **b1（index.html → 静态壳）已实质完成**：`grep "ng-"` 现仅剩两处**误命中**
  （`index.html:116` 的 `id="eagle-auto-tagging-host"`、`:211` 的 `jquery-long-click.js`），
  无 `ng-app` / `ng-controller` / `ng-cloak`，无 `angular*.js`，无 `app.bundle.js`。
- b1-9e 本轮修完四类「bundle 摘除后的全局词法绑定缺口」+ 两处非 bundle 缺口（见 §二）。

### 3. 当前状态与卡点

**工作树状态**：`17b1cf2` 已提交（12 文件 / +4926 / -227）。`git status` 剩余项**全部是禁提交
类**：`docs/handover-*`、`tests/probe-*`、`tests-tmp/`、`.workbuddy/`，以及三处**与本战线无关的
既有删除**（` D .claude/launch.json`、` D .zcode/plans/plan-sess_*.md`、` D docs/plan.md`）
和 ` M tests/probe-7d2.mjs`。**不要**把这些删除当作本轮产物去提交或恢复——来源未知，标「待确认」。

**push 状态**：**用户已裁定跳过 push**。`react-in-place` 无 upstream；`git push -u origin
react-in-place` 曾被远端拒绝（`! [rejected] … (fetch first)`，远端已有本地没有的提交）。
**新会话不要自行 push / pull / merge**——需要时先问用户。全局 CLAUDE.md 里的
`git push origin main` 规则**不适用于本分支**。

**唯一已知缺陷（非阻塞）**：m1 的 `multi inspector persistence` 闸门约 **1/6 轮**超时
（`electron/main.cjs:1790-1815`）。已知事实与两条候选机制见 §三.6，**这是下一步的主战线**。

**待确认 / 未知**（不要当成事实）：
- b1-9e 之前的 `sendTrace` / `selectionTrace` / `scopeStars` 富诊断**已从 main.cjs 清除**，
  现在闸门只报 `droppedId / clipboardId / targets / selectedIds`（`main.cjs:1801`）。
  历史证据只在 `tests-tmp/m1-b43-3.log`、`m1-b44-{1,2,3}.log`、`m1-b45-3.log` 里。
- `src/app/app.bundle.js`（4.4MB）与 `src/app/js/vendors/angular.min.js` **仍在磁盘上**。
  bundle 是**逐字端口的规范来源**，阶段 11 的 b3 清算完成前**禁止删除**。
- 阶段 11 余项 b2/b3/b4 未动（见 §一.4 的第三项）。
- React 侧 45 处 `w.angular.copy/isNumber/extend/equals` 与 `angular.element(...).injector()`
  仍是**死调用**（bundle 在世时靠真 angular）。其中 `itemDomain.ts:361/389` 的
  `w.angular.extend(img, newImage)` 会抛 TypeError，导致其后的 `img.star = newImage.star`
  永不执行——这既是隐患，也可能正是 `multi inspector` 缺陷「没有回滚载体」的原因（见 §三.6）。
  **若要供给 `window.angular`，必须同时给「bundle 在世」探测点
  （`global/scopeBridge.ts:14-22`、`core/itemDomain.ts:654`、shims 内 8 处）加 `!__eagleShim` 门**，
  否则 shim scope 根本不会建立 —— 这是 b1-9e 明确记录的雷区。

### 4. 下一步行动（To-Do，按序；新会话接手后紧接着做这 3 件）

0. **（前置，可与 1 并行）补跑 React 全量门**：`node tests/run-react-suite.mjs`
   （后台，重定向 `tests-tmp/react-suite-b49.log`，30-45 分钟）。b1-9e 收口时**漏跑了这道门**，
   而它覆盖 40 个阶段冒烟 —— 先确认 b1-9e 没打破既有面，再叠加新改动。详见 §四 首节。
   遇 `react-stage7a-smoke` / `main-ui-workflow` 失败先单跑复验（既有偶发家族）。

1. **给 `multi inspector persistence` 取一次对象身份证据**（≤3 次工具调用，不动业务代码）。
   在 `electron/main.cjs:1801` 的 reject 载荷里临时加三个布尔 + 三份字段快照：
   ```js
   const rawEntry = scope.raw.find((i) => i.id === droppedId);
   const mapEntry = scope.itemMappings && scope.itemMappings[droppedId];
   identity: {
     mapIsRaw: mapEntry === rawEntry,
     selIsMap: scope.selected[0] === mapEntry,
     rawVals: rawEntry && [rawEntry.star, rawEntry.annotation, (rawEntry.tags||[]).join('/')],
     mapVals: mapEntry && [mapEntry.star, mapEntry.annotation, (mapEntry.tags||[]).join('/')],
     selVals: scope.selected.map((it) => it && [it.id.slice(-4), it.star, it.annotation]),
   }
   ```
   然后**循环跑 m1 直到复现**（约 1/6，日志重定向 `tests-tmp/m1-b<N>-<i>.log`，逐轮读 `EXIT:` 行）。
   `mapIsRaw === false` ⇒ 机制 A（同 id 双实例分叉）；三者同一且值皆旧 ⇒ 机制 B（晚到快照回写）。
   **⚠️ 禁用 `Object.defineProperty` 监视条目属性**——b1-9e 实测会让整个冒烟脚本
   抛 "Script failed to execute"。
2. **按证据修真源头**：
   - 机制 A ⇒ 收口 `src/app/react/core/itemDomain.ts:435` 的
     `s.itemMappings[image.id] = image`（无条件覆写映射，而 unshift 进 `s.raw` 是**有条件**的
     → 同 id 两个实例）。可疑的第二发来源是 `itemDomain.ts:575` 的
     `ipc.send("file-uploaded-end", generated)` 回补路径。
   - 机制 B ⇒ 在 mock 总线 `emit` 上挂钩，记录「哪个 channel 的处理器前后改变了该条目的
     star/annotation/tags」（`frontend/public/shims.js` 的 `mockEmit` / `ipcRenderer.emit`
     拦截段，约 905-935 行）。已排除的载体：`domainUpdateItemListView`
     （`itemDomain.ts:69`，只拷 width/height/size/ext/fontMetas）。
   修完连跑 m1 **≥6 轮**确认 0 失败，再更新 `PROGRESS.md:42` 基线行与 b1-9e 记录的「遗留」段。
3. **继续阶段 11 余项**（与 2 并行或其后，用户未指定优先级，**接手时可先问一句**）：
   - **b4 双轨 CSS 清点**：`src/app/index.html:9` 仍引 `css/modules/angular-notify/angular-notify.min.css`，
     消费方（angular-notify）已随 bundle 摘除 → 核实无 React 消费后删。
   - **b2 独立窗口旧文件删除** / **b3 主窗口 `src/app/js` 源码树清算**：清单与前置条件见
     `PROGRESS.md:1742-1762`；**b3 必须逐项核对 b1 后 index.html 的实际 script 清单
     （`index.html:202-236`）+ React import 面**，vendors 与共享脚本保留。
   - 其余分节残留「待办」行分布（`PROGRESS.md` 内 grep `待办`）：
     §7 弹窗/右键/标签/面板 24 项、过滤器清单 21 项、§3 工具栏与筛选 20 项、
     其他独立 Angular 模块 15 项、§2 侧栏 10 项、补充清单 8 项、§9 其余窗口 5 项、
     §1 壳与全局状态 5 项、§4 内容网格 4 项、§10 快捷键 2 项、§11 本节 2 项、§5 详情 1 项。

### 5. 关键规则与 SOP（雷区）

**编辑纪律**：小改只走 Edit（先 Read）；批量机械替换写成 `tests-tmp/*.py` 再执行；
python 改过的文件当轮不再 Edit；绝不同批混用同一文件的 Edit + Bash 写入。

**取证纪律**（多轮实证）：
- 跨总线 / 跨世界的行为假设**必须先探针证实再动刀**。b1-9d 曾据「main.cjs 独家发布」推断改
  shims，直接把 m1 打回更早失败点。
- 取证上限 ~3 次工具调用；超出就把结论标「存疑待验」落盘，不要滚雪球。
- `electronLog` 吞错、不进 console —— 探查必须钩 `window.electronLog.error`。
- 后台跑测试**必须**重定向到 `tests-tmp/` 并确认 `EXIT:` 行属于本轮（旧日志会骗人）。

**项目铁律**：
- **数据面零改动**：ipc 通道名 / localStorage 键 / electron-settings 键逐字；bundle 的 typo
  原样保留（`gifUpadteInterval` / `destoryMousetrap` / `supportFoamts` / `findDupclipate`…）。
- **bundle 怪癖逐字保留**：双写调用、矛盾守卫、死变量、裸 `event` → `w.event`、
  forEach 用非箭头函数以保 `this`。
- `@ts-nocheck` 文件（`controllerFns.ts` / `tagManagerDomain.ts`）里 TS 不报裸标识符 ——
  改后必须 grep 自查（`$scope|$rootScope|裸 i18n/fs/path/swal`）。
- **禁提交**：`tests/probe-*`、`tests-tmp/`、`mock-library/`、`.workbuddy/`、`docs/handover-*`、
  backend 的 cpSync 临时补丁。
- **【环境缺陷】** 宿主的 node `fs.cpSync(recursive)` 被静默击杀（exit 127）。凡跑
  `npm run test:isolated`，`backend/src/{library-migration,importer,library-backup-service}.js`
  + `tests/roadmap-panels.mjs` 的 `copyRecursiveManual` 临时补丁**必须在树**，
  跑完 `git checkout` 回退，**永不提交**。
- **【m1 回归判据】** 退回比当前更早的失败点（如 `original main scope timeout`、
  `text file drop import timeout`）= 新回归，必须优先查；markdown 段恒跳过。
- **不在监听侧盲目加去重/兜底来掩盖症状**——修真源头。

---

## 二、最近修改内容（= 提交 `17b1cf2` 的全部 12 个文件）

主题：**app.bundle.js 从 index.html 摘除后，凡「bundle 内联但不是 window 属性」的东西一并消失，
而消费侧多包在 `try/catch` 里 → 症状是「静默空值」而不是报错。**

| 文件 | 改了什么（函数 / 数据结构 / 配置） | 目的 |
|---|---|---|
| `frontend/public/shims.js`（+103） | ① 新增模块级计数 `pendingLocalImports` / `settledLocalImports` + `trackLocalImport(promise)`（紧跟 `let itemUpdateQueue = Promise.resolve();`），包住两处导入派发（`desktopApi.import.*` 与 `desktopApi.clipboard.import`）② `startCapturePolling` 的 `tick`（:2989）在 `await desktopApi.library.current()` **前**记 `const importGeneration = settledLocalImports`，await 后 `if (pendingLocalImports > 0 \|\| settledLocalImports !== importGeneration) return;` ③ 同段 `rawIds` 防线由 `angular.element(document.body).scope()` 改 `window.angular ? … : (window.$bodyScope \|\| null)` ④ `refreshImportedThumbnails` 先 `await itemUpdateQueue.catch(()=>undefined)`，且 `thumbnail-generated` 的载荷**收窄为纯缩略图字段**（id/name/ext/width/height/noThumbnail/processingThumbnail/modificationTime） | ①②修同 id 双发**真源头**：capture 轮询把「库里已有、cache/raw 还没有」的本地在飞导入误判成外部捕获并补发 `file-uploaded`，与导入 `.then` 抢跑（实测间隔 50ms）③ angular 缺席时原表达式恒 null，这道「已在列表里」的防线整条失效 ④ 阻断「晚到的库快照整条覆盖内存条目」（把 multi inspector 失败率从 ~3/3 压到 ~1/6） |
| `frontend/public/vendor/eagle-smooth-zoom.js`（**新增 3989 行**） | `src/app/js/vendors/jquery.smoothZoom.min.js` 的提取版，**仅 6 处机械替换**：`angular.element("body").scope()`→`window.$bodyScope`（×5）、`angular.element("html").injector()`→ 只认 `$rootScope` 的最小 injector 桩（×1）。生成脚本 `tests-tmp/extract-smooth-zoom.py`（未提交） | `$.fn.smoothZoom` + `BitmapViewer` 原被 bundle 内联，摘除后详情原图管线整条死。**刻意不注入 `window.angular`**（见 §一.3 末条） |
| `src/app/index.html`（+14） | `:231` 加 `<script src="/vendor/eagle-smooth-zoom.js">`（放回 app.bundle.js 原位，紧随 `js/services/lazy-load-manager.js`）；`:217` 加 `<script src="js/vendors/jquery-audio.js">`（mousetrap 之后）。两处都带注释说明「因 bundle 摘除而丢失」 | 补回两个内联 vendor（同步加载，顺序敏感） |
| `src/app/react/core/fileUrlHelper.ts`（+16） | 模块顶部新增惰性 `URL_MODULE` 解析：`require(appRoot.path + '/my_modules/url')` → `require('url')` → `window.URL_MODULE` | **根因级修复**：`URL_MODULE` 是 bundle 顶层 `const`（classic script 的全局词法绑定，不是 window 属性）。`getRawUrl`/`getThumbnailUrl` 裸引用它 → 抛 ReferenceError 被 try/catch 吞 → **所有 raw/thumbnail URL 恒空** |
| `src/app/react/core/controllerFns.ts`（±406） | ① 新增 `fns["addImagesToFolder"]`（bundle 43132-43198 逐字，插在 `addToRecentFolders` 前）；`angular.copy` → `Array.isArray(x)?x.slice():x` ② `$filter` shim 退到 `machineryGetFilter()` ③ **149 处 `$${` → `${`**（94 行） | ①155-fns 抽取漏项，`scope.addImagesToFolder is not a function` ② `$root.$filter` 在 shim 世界不存在（死依赖） ③ 提取器转义污染：污染面含 **localStorage 键**（`eagle.list.orderBy.$<rootDir>`）、**filterCounts 的 year/month 键**、字体安装路径、jQuery 选择器、各查看器 URL，以及 `getRawUrl` 的 `?v=$<ver>`（**详情原图 404 的直接原因**） |
| `src/app/react/global/scopeShim.ts`（+21） | 新增模块级 `selfProxy` 与 `evalPath(expr)`；`$eval` 与**字符串型 `$watch`** 都改为经 proxy 取值（`return proxy` 前 `selfProxy = proxy`） | **影响面最大的一处**：数据面字段只存在于 `coreState`、仅经 Proxy 可达。`$eval` 原对着裸 `shim` 取值 → `startScopeSync` 的 12 个域快照**自 b1 起永久停在初值**（body 的 `is-detail-mode` 等类名不跟随 → 详情面板无尺寸 → 原图闸门超时） |
| `src/app/react/core/dataMachinery.ts`（±257） | ① 导出 `getFilter` ② `machineryEnterDetailMode` 在 `s.smoothZoomDone = false;` 后加 `s.$evalAsync();` ③ `machinerySeedControllerState` 补种 bundle 20238-20254 的 `removeSound`/`duplicateSound`/`errorSound`（各 `{play(){ w.$.playSound('sounds/*.wav') }}`） | ② bundle 靠 Angular digest 在 100ms smoothZoom 初始化**前**把 body 类落到 DOM，shim 世界必须显式 flush ③ `machineryRemoveSelected` 原在 `s.removeSound.play()` 处 TypeError |
| `src/app/react/core/itemDomain.ts`（±172）、`core/bundleGlobals.ts`（±37） | b1-9d 遗留的未提交本体一并入提交（file-uploaded 等监听域逐字端口 / 裸标识符供给） | 供给闭环 |
| `electron/main.cjs`（±39） | ① 终局 `ok` 合取**摘除**三项 markdown 断言（带【项目决策·IGNORED】注释）② `detail original delivery` 闸门加富诊断 `.catch`（delivery state / canvas 尺寸 / 4 个选择器的 rect / rawUrl 尾段 / `rawStatus` 实测 fetch）③ 清除本轮全部临时打点（selectionTrace/backendTrace/sendTrace/总线 monkey-patch），并**恢复**被误删的 `require('electron').ipcRenderer.on('thumbnail-generated', thumbnailGeneratedListener)` | ①`markdownSource` 恒空 → 不摘除则全门通过也只打印 SMOKE_FAIL ②留给下一轮的诊断资产 ③收口 |
| `tests/main-ui-workflow-closed-loop.mjs`（±7） | 摘 markdown 样例与环境变量 | markdown 隔离 |
| `src/app/react/PROGRESS.md`（±92） | 基线表 m1 行翻绿（原写 `original inspector timeout`，已过时）；新增长篇「b1-9e」记录（四类缺口 + 两处额外修复 + 教训 + 两项遗留） | 基线登记（**唯一权威基线载体**） |

**没有改动的面**：数据库 / Schema（本项目无 DB，落盘经 backend HTTP API）；后端 API 形状；
electron-settings / localStorage 键（铁律：零改动）；UI 视觉（本轮无新组件，只补回丢失的 vendor
与声音资源）。

---

## 三、当前代码状态（具体到文件 / 函数）

### 1. 启动与装载链

`frontend/vite.preview.config.mjs` 服务 `src/app/index.html`（本仓 `/src/app/*.html` 走自定义
中间件直出、绕过 `transformIndexHtml`，所以 react-refresh preamble 必须手补，否则 `.tsx` 抛
"can't detect preamble"）。head 注入 `mock-data.js` + `frontend/public/shims.js`，`</body>` 前
注入 `src/app/react/main.tsx`。

`window.angular` **缺席** → `react/global/scopeBridge.ts:14-22` 的「bundle 在世」探测判为
「不在世」→ `getBodyScope()` 新建 **shim scope** 并回填 `window.$bodyScope` →
`attachCoreFnsToShim()` 挂 155 个 `controllerFns` → `applyDataMachineryScope()` → 五个域 takeover
（itemDomain / libraryDomain / selectionViewDomain / tagManagerDomain / apiServerDomain）。

### 2. 双总线（**动手前必须先确认监听挂在哪条**）

- **mock 总线**：`frontend/public/shims.js:907` 的 `const ipcRenderer = new EventEmitter()`，
  同时暴露为 `window.$electronIpc` / `window.__eagleIpc`，也是 shims 版 `require('electron')`
  的返回值。`mockEmit(...)` 全部走这条。
- **真 IPC**：preload 的 `desktopApi`。**`preload.cjs` 没有暴露 `ipc` 键**，所以
  `react/global/eagleGlobals.ts:41` 的 `ipcRenderer()` 回落到 mock 总线。
- 结论（b1-9d 的 b25 轮实证）：**`itemDomain` 的 `file-uploaded` 消费链唯一由 shims renderer
  侧 emit 驱动**；`electron/main.cjs:1180` 那句逐项 `event.sender.send('file-uploaded', item)`
  到达真 IPC 总线，**无人消费**（有效但空转，不要据它推断行为）。

### 3. 状态管理

- 唯一后端 = `react/core/appCore.ts` 的 `coreState`；`react/global/scopeShim.ts` 返回其 **Proxy**。
  **所有 `$eval` / 字符串 `$watch` 必须经 proxy 解析**（b1-9e 修复点，见 §二）。
- `startScopeSync({watch, build, apply})` 驱动 12 个 zustand store 快照
  （`store/appState.ts` 等）；`machinerySeedControllerState`（`dataMachinery.ts`）补种
  controller-init 默认值，**仅在 `s.__eagleShim` 时调用**。
- 测试契约（只增不删）：`window.__eagleReactStore`、`window.__eagleUploadState`、
  `window.__eagleInspectorActions`（`components/inspector/inspectorActions.ts:921`
  暴露 `{updateSelection, imagesChange, annotationChange}`）、`window.$bodyScope`。

### 4. drop 导入数据流（m1 的主断言面）

m1 内嵌脚本（`electron/main.cjs`）构造 dataTransfer → `scope.onDropContainer`
（`dataMachinery.ts`，bundle 52717 端口）→ `ipc.send('upload-local-files' | 'import-folders')`
→ **shims 的 send 拦截段（约 1600-1665）**：有 `desktopApi.import` 时走真 invoke
→ `main.cjs` 的 `item:import-files` / `item:import-folders` handle（:1168 / :1192，轮询 backend job）
→ `.then` → `emitImportedItems(items, channel)`（shims:~960，**已被 `trackLocalImport` 计入在飞**）
→ mock `ipcRenderer.emit('file-uploaded', item)` → `react/core/itemDomain.ts:429` 监听体
（bundle 30427 逐字：`lastestAddItem` → `itemMappings[id] = image` → `isDuplicateImage` /
`duplicateQueue` 分支 → `s.raw.unshift(image)` → `addToDuplicateMapping` → filterExtensions）
→ m1 轮询 `scope.raw.find(name === 'Dropped Text')` 通过。

**并行的另一条腿**：`shims.js` 的 `refreshImportedThumbnails` 在导入落地后补发
`thumbnail-generated`（载荷已收窄），被 `itemDomain.ts:533`（#1 详情图刷新）与 `:564`
（#2 封面更新 + 未收录时回补 `ipc.send("file-uploaded-end", generated)`）双监听消费。

### 5. 详情原图交付链（b1-9e 修通，勿回退）

`machineryEnterDetailMode`（`dataMachinery.ts`）置 `isDetailMode` + `smoothZoomDone=false`
→ `s.$evalAsync()` 让 `startScopeSync` 把 body 的 `is-detail-mode` 落到 DOM（**顺序敏感**：
必须早于 100ms 后的 smoothZoom 初始化，否则容器 0 高）→ `#detail-container` 由 `react/components/shell/BodyBindings.tsx:198` 渲染（**不是 index.html
静态节点**；元素身份不可重建，smoothZoom 包裹它）
→ `$.fn.smoothZoom`（现由 `/vendor/eagle-smooth-zoom.js` 提供）建 BitmapViewer canvas
→ URL 来自 `react/core/fileUrlHelper.ts` 的 `getRawUrl`（依赖 §二 修好的 `URL_MODULE`
与去污染后的 `?v=${…}`）。闸门断言 `deliveryState.mode === 'canvas'` 且 `tileCount > 0`。

### 6. `multi inspector persistence` 缺陷（**下一步主战线**）

**闸门位置**：`electron/main.cjs:1785-1815`。动作序列：
`selectInspectorItems([droppedId, clipboardId])`（:1726 —— 内部
`scope.selected = ids.map(currentItem)`，而 `currentItem`（:1566）**优先取
`scope.itemMappings[id]`**，回退 `scope.raw.find(...)`）→ `inspector.newAnnotation='多选备注持久化'`
→ `inspectorActions.annotationChange()` → `TagManager.addTag('batch-ui')`
→ `changeStar(3, false, true)` → 轮询 backend `library.current()` 要求两条都满足
`annotation==='多选备注持久化' && star===3 && tags.includes('batch-ui')`，15s 超时。

**已确证事实**（来自失败轮 `tests-tmp/m1-b45-3.log`，该轮 main.cjs 尚带富打点）：
- 三步操作后读 `s.selected`：**两条都是新值**（`KQ5V:3:多选备注:…batch-ui`）。
- 同轮 `images-change` 载荷与 backend 落盘值：**clipboard 条目新值、dropped 条目恒旧值**
  （`4:原版检查:main-ui/persisted`）。`item:operation-result` **无错误**。
- ⇒ 「写内存成功、持久化拿到旧值」，即**同一 id 在内存里存在两份不同实例**，或**有晚到的
  快照把 dropped 条目字段写回旧值**。

**两条候选机制**（都未取证，按 §一.4 第 1 步的探针二选一）：
- **A 双实例分叉（较强）**：`itemDomain.ts:435` 每次 `file-uploaded` 都**无条件**
  `s.itemMappings[image.id] = image`，而 `s.raw.unshift(image)` 是**有条件**的（:456/461/469）。
  一旦有第二发（含 `:575` 的 `file-uploaded-end` 回补路径），`itemMappings` 换成新实例、`raw`
  仍留旧实例 —— 且 `raw` 里 id 不重复，所以 `assertUniqueItems` 照样通过。
  `selected` 取自 `itemMappings`，落盘链若读 `raw` 就会拿旧实例。
- **B 晚到快照回写**：某个异步处理器把库快照覆盖到存量条目。**已排除** `domainUpdateItemListView`
  （`itemDomain.ts:69`，只拷 width/height/size/ext/fontMetas）。`image.changed` / `image.changed.mute`
  （`:369` / `:345`）里的 `w.angular.extend(img, newImage)` 在 shim 世界会**抛异常**
  （`w.angular` 不存在）——所以它们既不是载体，也说明 B 目前**缺少可信载体**。

**已知发送侧行为**：`ayncsImagesChange`（`react/core/bundleGlobals.ts:427`，bundle 49667 逐字）
`setTimeout(0)` + `requestAnimationFrame` 分批发 `images-change`，**发送的是传入数组的原始引用**
（不按 id 重新解析），发完还会 `delete image.oldName/newName`。

### 7. 双引擎与循环依赖（**勿"修复"**）

`react/core/controllerFns.ts`（155 fns，`@ts-nocheck`）与 `react/core/dataMachinery.ts`
存在 ESM 循环 import —— 运行时安全，是有意为之。`callScope`（`hooks.ts`）优先命中
React 移植表，bundle 同名函数退为后备（bundle 现已不在世，后备恒空）。

### 8. m1 闸门顺序（读日志时用来判断「进展 or 回归」）

`original main scope` → `text file drop import` → *(markdown meta shows file size：恒跳过)* →
`folder drop import` → `clipboard path import` → `clipboard image import` → `original inspector`
→ `inspector annotation persistence` → `inspector tags folder and star persistence`
→ **`multi inspector persistence`（间歇失败点）** → `tag history persistence`
→ `detail original delivery` → `trash persistence` → `restore persistence`
→ 重启段：`original main scope` → `viewer rendered surface` → `main scope` → `native detail video`
→ `detail image` → `video ready` → `renamed image` → 终局 `ok` 合取 → `MAIN_WORKFLOW_SMOKE_OK`
→ harness 打印 `MAIN_UI_RESTART_OK`。

---

## 四、重要代码 / 命令

### 回归门全景（**别只跑 m1 + isolated，这两条不覆盖 React 侧**）

| 门 | 命令 | 实际覆盖 | 成功标志 |
|---|---|---|---|
| **React 全量门** | `node tests/run-react-suite.mjs` | **45 项**：40 个 `tests/react-stage*-smoke.mjs` + m1 + source-mode-ui / library-switch-ui / drag-start / preview-delivery 四个闭环。顺序隔离执行，单项超时 300s | `REACT SUITE ALL GREEN` |
| m1（单项） | `npm run test:main-ui-workflow` | 主界面闭环冒烟 + 重启段 | `MAIN_WORKFLOW_SMOKE_OK` + `MAIN_UI_RESTART_OK` |
| 隔离全量 | `npm run test:isolated` | = `node tests/full-regression-isolated.mjs`：起隔离端口 + 临时库根 + `electron --regression-host`，然后**只跑 `npm test`（44 个 backend/API 套件）**。**不含 m1，不含任何 react 冒烟** | `FULL_REGRESSION_ISOLATED_OK` |
| UI/electron 链 | `npm run test:full` | 14 条串联（source-mode-ui / electron-* / preview-delivery / drag-start / export-progress / video-detail / browser-capture / workbench / screenshots …） | 各套件自报 |
| 类型门 | `npx tsc --noEmit -p tsconfig.json` | 全仓 TS | `EXIT:0`（**必须读退出码**） |

> ⚠️ **b1-9e 收口时只跑了 m1 + `test:isolated` + tsc，`run-react-suite` 没跑。**
> 而 b1-9e 动的面很广（`scopeShim.$eval` 影响 12 个 store 同步、`index.html` 新增两个 vendor
> script、`controllerFns` 149 处替换），**理论上可能打破其它阶段冒烟**。
> `run-react-suite.mjs` 也不在 `package.json` 的 scripts 里（只能 `node` 直跑），
> 文件头注释写的「43+3 项」已过时（实为 45）。**下一轮开工第一件事之一就是补跑它。**
> 一轮约 30-45 分钟（每项都要冷启 electron），务必后台跑 + 重定向。

### 命令

```bash
# m1（约 60-90s/轮）—— 后台跑，日志必须重定向，读 EXIT 行确认属于本轮
npm run test:main-ui-workflow > tests-tmp/m1-b49-1.log 2>&1; echo "EXIT:$?"

# 全量隔离回归（前置：backend 三个 cpSync 临时补丁必须在树；跑完 git checkout 回退）
npm run test:isolated > tests-tmp/iso-b49.log 2>&1; echo "EXIT:$?"   # 末行须为 FULL_REGRESSION_ISOLATED_OK

# 类型门 —— 必须读真实退出码，0 才算过
npx tsc --noEmit -p tsconfig.json > tests-tmp/tsc-b49.log 2>&1; echo "EXIT:$?"

# 任何 shims 改动后的语法自查
node --check frontend/public/shims.js

# React 全量回归门（30-45 分钟，后台跑）
node tests/run-react-suite.mjs > tests-tmp/react-suite-b49.log 2>&1; echo "EXIT:$?"

# CDP 探针（走 tests/react-cdp-harness.mjs 的 bootStack/stop/waitFor/delay）
node tests/probe-delivery.mjs
```

**宿主 cpSync 缺陷的临时补丁**（跑 `test:isolated` 前放回，跑完必须回退，**永不提交**）：
```bash
# 回退（终审 / 提交前）
git checkout -- backend/src/library-migration.js backend/src/importer.js \
  backend/src/library-backup-service.js tests/roadmap-panels.mjs
```

**`URL_MODULE` 类缺口的标准修法**（bundle 顶层 `const` 的通用模板，已在
`fileUrlHelper.ts` / `apiServerDomain.ts:704` / `detailHooks.ts:455` / `Sidebar.tsx:693` 落地）：
```ts
const URL_MODULE = (() => {
  const req = (window as any).require;
  if (typeof req !== 'function') return (window as any).URL_MODULE;
  const appRoot = (window as any).appRoot;
  const rootPath = appRoot && (appRoot.path || appRoot);
  if (rootPath) { try { return req(`${rootPath}/my_modules/url`); } catch (err) {} }
  try { return req('url'); } catch (err) { return (window as any).URL_MODULE; }
})();
```

**权威上下文载体（按优先级）**：
1. `src/app/react/PROGRESS.md` —— 唯一基线登记处。基线表在 `:35-54`；b1-9e 记录 `:56-101`；
   阶段 11 清单 `:1707-1762`；b1 前置勘察结论 `:1925-1943`。
2. 本文（`docs/handover-b1-9e-2026-09-04.md`）。
3. `src/app/app.bundle.js` —— 逐字端口的**规范来源**，禁删。
4. 历史失败证据：`tests-tmp/m1-b4*.log`（`m1-b45-3.log` 含 multi inspector 富打点）。

**注意**：`PROGRESS.md` 的「当前盘上状态」类描述经常落后于磁盘。**动手前先跑一遍 m1 确认真实
失败点**，不要凭文档复述基线。

### 环境与工程惯例（零上下文会话最容易踩的一层）

- **平台**：Windows 11 + Git Bash。仓库路径**含空格与中文**
  （`H:\dev\Eagle-Sec-development - 副本`）→ 所有 bash 命令必须引号包路径
  （`cd "H:/dev/Eagle-Sec-development - 副本"`）；electron 栈里会出现 URL 编码形态
  `file:///H:/dev/Eagle-Sec-development%20-%20%E5%89%AF%E6%9C%AC/…`，那是同一个仓库，不是别的目录。
- **测试自带编排**：各 harness 自己 spawn backend + vite + electron 并分配隔离端口，
  **不需要手动先起 dev server**。跑前确认没有上一轮残留的 electron / vite 进程占端口
  （失败信息会是启动等待超时而不是断言失败）。
- **m1 冒烟脚本的物理形态**：它是**内联在 `electron/main.cjs` 里的字符串**，由
  `win.webContents.executeJavaScript` 执行。因此脚本里的语法错误或早期异常会表现为整体
  `Script failed to execute`（**没有行号**）。改脚本要小步、一次只加一处打点，加完先跑一轮。
- **编号惯例**：测试日志 `tests-tmp/m1-b<N>[-<i>].log` 单调递增，**当前已用到 b48，下一轮从
  b49 起**；提交信息 `feat(react): b1-9<x> — <中文摘要>` + 正文分条列根因；`PROGRESS.md`
  收口块统一写成 `> **<编号> 已验证并接管（日期）**：…` 并附「教训」段。
- **既有偶发失败家族**（`PROGRESS.md` 有据，**别误判成回归**）：`react-stage7a-smoke` 与
  `main-ui-workflow` 属「既有偶发家族，单跑复跑转绿」。遇到先单跑复验再判定。
- **仓库内另有一份项目规则** `src/CLAUDE.md`（繁体中文）：禁在根目录建文件、禁擅自创建 `.md`
  文档（**本 handover 属用户明确要求，例外**）、优先用工具而非 `find/grep/cat` shell 命令、
  先搜索再新建（禁 `*_v2` / `enhanced_*` 命名）。它会被自动加载，按其执行。
- **禁删（区别于禁提交）**：`src/app/app.bundle.js`（逐字端口的规范来源）、
  `src/app/js/vendors/angular.min.js`（阶段 11 b3 清算前）、`mock-library/`（测试基底）。

---

## 五、未知 / 待确认清单（**不要当成已知事实使用**）

1. `multi inspector persistence` 的**真因未定** —— §三.6 的 A / B 两条机制都只有间接证据，
   必须先取证再动刀。
2. **`node tests/run-react-suite.mjs`（React 全量门 45 项）在 b1-9e 收口时未跑** ——
   40 个阶段冒烟对 b1-9e 改动的反应**未知**。这是当前最大的验证缺口，见 §四 首节。
2. 工作树里三处**与本战线无关的删除**（`.claude/launch.json`、`.zcode/plans/plan-sess_*.md`、
   `docs/plan.md`）与 ` M tests/probe-7d2.mjs` 的来源**未查明**，不要提交也不要擅自恢复。
3. `electron/main.cjs:1180` 逐项 `event.sender.send('file-uploaded', item)` 到达真 IPC 后
   **是否真的完全无消费者**，只有 b1-9d 的 b25 轮反证（关掉 shims emit → 一条都不进 raw），
   **未逐点确认**。
4. 阶段 11 的 b2/b3 删除面**未逐项核对**；`src/app/js` 下哪些文件在 b1 后彻底失去引用需实测。
5. `PROGRESS.md` 顶部「当前盘上状态」类描述可能落后于磁盘 —— 以实跑结果为准。
6. 阶段 11 之后的独立阶段（`PROGRESS.md:1756-1758` 的 C 项：native `Menu.popup` 自动化、
   8 个 iframe viewer 窗口接管）**未排期**。

---

## 六、新窗口启动指令

```
读取 docs/handover-b1-9e-2026-09-04.md（v6）并按其第一部分第 5 节 SOP 工作。分支 react-in-place，HEAD=17b1cf2，m1（npm run test:main-ui-workflow）与 npm run test:isolated 当前均为绿、tsc EXIT:0；不要 push / pull / merge（用户已裁定跳过，且远端有本地没有的提交）。立即执行：⓿ 后台补跑 React 全量门 node tests/run-react-suite.mjs > tests-tmp/react-suite-b49.log 2>&1（45 项、30-45 分钟，末行应为 REACT SUITE ALL GREEN）——b1-9e 收口漏跑了这道门，它覆盖 40 个阶段冒烟，而 b1-9e 动了 scopeShim.$eval（波及 12 个 store 同步）、index.html 的 vendor script 与 controllerFns 149 处替换；遇 react-stage7a-smoke / main-ui-workflow 失败先单跑复验（既有偶发家族）。与之并行推进主战线：消除 m1 的 multi inspector persistence 间歇失败（约 1/6 轮，闸门在 electron/main.cjs:1785-1815）。已确证：三步操作后 s.selected 两条都是新值，但 images-change 载荷与 backend 落盘对 dropped 条目恒为旧值、item:operation-result 无错误（证据在 tests-tmp/m1-b45-3.log，该轮的富打点现已从 main.cjs 清除）。① 在 main.cjs:1801 的 reject 载荷里加对象身份取证（mapIsRaw = scope.itemMappings[droppedId] === scope.raw.find(i=>i.id===droppedId)、selIsMap = scope.selected[0] === itemMappings[droppedId]，外加三处的 star/annotation/tags 快照），循环跑 m1 直到复现（日志重定向 tests-tmp/m1-b49-<i>.log，逐轮读 EXIT 行）；严禁用 Object.defineProperty 监视条目属性——实测会让整个冒烟脚本抛 "Script failed to execute"（脚本是 main.cjs 内联字符串，报错无行号，改动务必小步）。② 按证据修真源头：mapIsRaw===false ⇒ 收口 itemDomain.ts:435 的无条件 s.itemMappings[image.id]=image（unshift 进 raw 是有条件的 → 同 id 两实例；可疑第二发来源是 :575 的 file-uploaded-end 回补）；三者同一且值皆旧 ⇒ 在 shims mock 总线 emit 上挂钩定位「哪个 channel 改了该条目的 star/annotation/tags」（已排除 itemDomain.ts:69 的 domainUpdateItemListView）。不要在监听侧盲目加去重或兜底掩盖症状。③ 修完连跑 m1 至少 6 轮确认 0 失败，跑 npx tsc --noEmit（读 EXIT 行）、node tests/run-react-suite.mjs 与 npm run test:isolated（backend 三个 cpSync 临时补丁必须在树，跑完 git checkout 回退 backend/src/{library-migration,importer,library-backup-service}.js 与 tests/roadmap-panels.mjs），然后更新 PROGRESS.md:42 基线行与 b1-9e 记录的「遗留」段并提交（提交信息格式 feat(react): b1-9f — <中文摘要>）。markdown 段恒跳过，不得为其改业务代码；禁提交 tests/probe-*、tests-tmp/、docs/handover-*、.workbuddy/、mock-library、backend 临时补丁；禁删 src/app/app.bundle.js（逐字端口规范来源）。若主战线修完仍有余力，接阶段 11 余项（b4 双轨 CSS：index.html:9 的 angular-notify.min.css；b2/b3 见 PROGRESS.md:1742-1762），动手前先问用户优先级。
```





