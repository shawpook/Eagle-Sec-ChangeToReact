import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * A-B Loop 插件配置
 */
export interface ABLoopPluginOptions {
    /** 邊緣吸附閾值（百分比），默認 0.02 */
    snapThreshold?: number;
    /** A-B 點最小間隔（秒），默認 0.1 */
    minRangeDuration?: number;
    /** 是否自動啟用循環模式 */
    autoEnableLoop?: boolean;
}
/**
 * A-B Loop 插件定義
 */
export declare const ABLoopPlugin: MpvPlugin<ABLoopPluginOptions>;
/**
 * A-B Loop 管理器
 */
export declare class ABLoopManager implements PluginInstance {
    readonly name = "abLoop";
    private player;
    private options;
    private _loopStart;
    private _loopEnd;
    private _enabled;
    private _isDragging;
    private containerEl;
    private rangeBar;
    private handleA;
    private handleB;
    private timeupdateHandler;
    constructor(player: MpvVideoElement, options: Required<ABLoopPluginOptions>);
    private initialize;
    private createUI;
    private bindDragEvents;
    /** 重置 A 點到起始位置 */
    resetA(): void;
    /** 重置 B 點到結束位置 */
    resetB(): void;
    private updateUI;
    /** A 點時間（秒） */
    get loopStart(): number;
    set loopStart(value: number);
    /** B 點時間（秒），0 表示視頻結尾 */
    get loopEnd(): number;
    set loopEnd(value: number);
    /** 實際的結束時間 */
    get effectiveLoopEnd(): number;
    /** 重置範圍 */
    reset(): void;
    /** 設置 A 點為當前時間 */
    setAToCurrentTime(): void;
    /** 設置 B 點為當前時間 */
    setBToCurrentTime(): void;
    enable(): void;
    disable(): void;
    get enabled(): boolean;
    destroy(): void;
}
//# sourceMappingURL=ABLoopPlugin.d.ts.map