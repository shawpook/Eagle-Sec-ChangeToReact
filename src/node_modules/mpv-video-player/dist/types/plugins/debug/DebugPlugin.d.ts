import { MpvPlugin, PluginInstance } from '../../core/types/plugin';
import { MpvVideoElement } from '../../renderer/ui/MpvVideoElement';

/**
 * 除錯面板位置
 */
export type DebugPanelPosition = 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right';
/**
 * 除錯插件配置
 */
export interface DebugPluginOptions {
    /** 是否顯示效能指標 */
    showPerformance?: boolean;
    /** 是否顯示狀態資訊 */
    showState?: boolean;
    /** 是否記錄事件 */
    logEvents?: boolean;
    /** 面板位置 */
    position?: DebugPanelPosition;
    /** 最大事件記錄數 */
    maxEventLogs?: number;
    /** 自訂要監控的事件 */
    watchEvents?: string[];
}
/**
 * 效能指標
 */
interface PerformanceMetrics {
    actualFps: number;
    nativeFps: number;
    targetFps: number;
    effectiveFps: number;
    degradationLevel: number;
    lastUpdate: number;
}
/**
 * 除錯插件定義
 */
export declare const DebugPlugin: MpvPlugin<DebugPluginOptions>;
/**
 * 除錯管理器
 */
export declare class DebugManager implements PluginInstance {
    readonly name = "debug";
    private player;
    private options;
    private panelEl;
    private toggleBtn;
    private performanceEl;
    private stateEl;
    private eventsEl;
    /** 緩存內容元素，避免每次 updateUI 時重複 querySelector */
    private performanceContent;
    private stateContent;
    private eventsContent;
    /** 緩存值元素引用，避免 innerHTML 重建 DOM 造成內存洩漏 */
    private perfValueEls;
    private stateValueEls;
    /** 緩存上一次的事件日誌長度，只在變化時更新 */
    private lastEventLogsLength;
    private _enabled;
    private _visible;
    private eventLogs;
    private metrics;
    private updateInterval;
    private eventHandlers;
    constructor(player: MpvVideoElement, options: Required<DebugPluginOptions>);
    private initialize;
    private createUI;
    private createToggleButton;
    private insertByOrder;
    private injectStyles;
    private bindEvents;
    private startMetricsCollection;
    private updateMetrics;
    private updateUI;
    private logEvent;
    private formatTime;
    private formatEventTime;
    /** 顯示除錯面板 */
    show(): void;
    /** 隱藏除錯面板 */
    hide(): void;
    /** 切換除錯面板顯示 */
    toggle(): void;
    /** 是否可見 */
    get visible(): boolean;
    /** 獲取效能指標 */
    getMetrics(): PerformanceMetrics;
    /** 獲取事件日誌 */
    getEventLogs(): Array<{
        time: number;
        event: string;
    }>;
    /** 清空事件日誌 */
    clearEventLogs(): void;
    enable(): void;
    disable(): void;
    get enabled(): boolean;
    destroy(): void;
}
export {};
//# sourceMappingURL=DebugPlugin.d.ts.map