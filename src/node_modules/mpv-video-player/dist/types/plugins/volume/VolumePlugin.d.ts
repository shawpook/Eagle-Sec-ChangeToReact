import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface VolumePluginOptions extends ControlPluginOptions {
    position?: 'left' | 'right';
    order?: number;
}
export declare const VolumePlugin: MpvPlugin<VolumePluginOptions>;
export declare class VolumeManager extends ControlPlugin<Required<VolumePluginOptions>> {
    readonly name = "volume";
    private volumeBtn;
    private volumeSlider;
    private volumeTrackFilled;
    private volumeHandle;
    private isDragging;
    protected createEl(): HTMLElement;
    private bindEvents;
    private updateVolumeFromMouse;
    private toggleMute;
    private updateUI;
}
//# sourceMappingURL=VolumePlugin.d.ts.map