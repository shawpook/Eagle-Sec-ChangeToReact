/**
 * 插件配置持久化
 *
 * 提供插件配置的存儲和讀取功能
 * 支援 localStorage 和自定義存儲後端
 */
/**
 * 存儲後端接口
 */
export interface StorageBackend {
    getItem(key: string): string | null;
    setItem(key: string, value: string): void;
    removeItem(key: string): void;
}
/**
 * 插件配置存儲接口
 */
export interface IPluginConfigStorage {
    /**
     * 獲取插件配置值
     * @param pluginName - 插件名稱
     * @param key - 配置鍵
     * @param defaultValue - 默認值
     */
    get<T>(pluginName: string, key: string, defaultValue?: T): T | undefined;
    /**
     * 設置插件配置值
     * @param pluginName - 插件名稱
     * @param key - 配置鍵
     * @param value - 配置值
     */
    set<T>(pluginName: string, key: string, value: T): void;
    /**
     * 刪除插件配置值
     * @param pluginName - 插件名稱
     * @param key - 配置鍵
     */
    remove(pluginName: string, key: string): void;
    /**
     * 獲取插件所有配置
     * @param pluginName - 插件名稱
     */
    getAll(pluginName: string): Record<string, unknown>;
    /**
     * 清除插件所有配置
     * @param pluginName - 插件名稱
     */
    clear(pluginName: string): void;
}
/**
 * 插件配置存儲實現
 */
export declare class PluginConfigStorage implements IPluginConfigStorage {
    private storage;
    private cache;
    constructor(storage?: StorageBackend);
    /**
     * 獲取存儲鍵
     */
    private getStorageKey;
    /**
     * 從存儲加載插件配置
     */
    private loadPluginConfig;
    /**
     * 保存插件配置到存儲
     */
    private savePluginConfig;
    get<T>(pluginName: string, key: string, defaultValue?: T): T | undefined;
    set<T>(pluginName: string, key: string, value: T): void;
    remove(pluginName: string, key: string): void;
    getAll(pluginName: string): Record<string, unknown>;
    clear(pluginName: string): void;
    /**
     * 清除所有插件配置
     */
    clearAll(): void;
}
/**
 * 創建插件配置存儲實例
 */
export declare function createPluginConfigStorage(storage?: StorageBackend): IPluginConfigStorage;
//# sourceMappingURL=PluginConfig.d.ts.map