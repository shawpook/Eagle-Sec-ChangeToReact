import { MpvPlugin } from '../../core/types/plugin';
import { ControlPlugin, ControlPluginOptions } from '../base/ControlPlugin';

export interface SpeedPluginOptions extends ControlPluginOptions {
    position?: 'left' | 'right';
    order?: number;
    /** 可選速度列表 */
    speeds?: number[];
}
export declare const SpeedPlugin: MpvPlugin<SpeedPluginOptions>;
export declare class SpeedManager extends ControlPlugin<Required<SpeedPluginOptions>> {
    readonly name = "speed";
    private speedBtn;
    private speedMenu;
    /** 緩存菜單項元素，避免重複 querySelectorAll */
    private menuItems;
    protected createEl(): HTMLElement;
    private bindEvents;
    private updateUI;
}
//# sourceMappingURL=SpeedPlugin.d.ts.map