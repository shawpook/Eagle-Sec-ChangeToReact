"use strict";
Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: "Module" } });
const ControlPlugin = require("../../../ControlPlugin-320fa16e.cjs");
const index = require("../../../index-bf534bb9.cjs");
const styles = "/**\r\n * Loop Button Plugin Styles\r\n */\r\n\r\n.vp-loop-btn {\r\n    width: var(--vp-button-size, 24px);\r\n    height: var(--vp-button-size, 24px);\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    background: transparent;\r\n    border: none;\r\n    border-radius: 4px;\r\n    cursor: pointer;\r\n    transition: background var(--vp-transition-duration, 0.15s);\r\n    padding: 0;\r\n    flex-shrink: 0;\r\n}\r\n\r\n.vp-loop-btn:hover {\r\n    background: var(--vp-button-hover-bg, rgba(44, 47, 50, 0.08));\r\n}\r\n\r\n.vp-loop-btn svg {\r\n    width: var(--vp-icon-size, 24px);\r\n    height: var(--vp-icon-size, 24px);\r\n}\r\n\r\n.vp-loop-btn.active {\r\n    background: var(--vp-button-active-bg, rgba(44, 47, 50, 0.1));\r\n}\r\n";
const LoopButtonPlugin = {
  name: "loopButton",
  version: "1.0.0",
  styles,
  install(player, options = {}) {
    return new LoopButtonManager(player, {
      position: options.position ?? "right",
      order: options.order ?? 10
    });
  }
};
class LoopButtonManager extends ControlPlugin.ControlPlugin {
  constructor() {
    super(...arguments);
    this.name = "loopButton";
  }
  createEl() {
    const btn = document.createElement("button");
    btn.className = "vp-btn vp-loop-btn";
    btn.title = "循環播放 (L)";
    btn.innerHTML = `<span class="vp-icon-loop">${index.loopIcon}</span>`;
    this.on(btn, "click", (e) => {
      e.stopPropagation();
      this.player.loop = !this.player.loop;
      this.updateState();
    });
    this.onPlayer("loopchange", () => this.updateState());
    this.onPlayer("play", () => this.updateState());
    this.onPlayer("loadedmetadata", () => this.updateState());
    this.updateState();
    return btn;
  }
  updateState() {
    if (this.el) {
      this.el.classList.toggle("active", this.player.loop);
    }
  }
}
exports.LoopButtonManager = LoopButtonManager;
exports.LoopButtonPlugin = LoopButtonPlugin;
exports.default = LoopButtonPlugin;
//# sourceMappingURL=index.cjs.map
