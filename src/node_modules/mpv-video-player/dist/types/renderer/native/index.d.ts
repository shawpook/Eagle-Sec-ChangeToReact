/**
 * Native Module 直接調用支持
 *
 * 允許在 Renderer 進程中直接使用 native MpvController，
 * 消除 IPC 帶寬瓶頸。
 */
export { NativeController, isNativeModuleAvailable, } from './NativeController';
export type { VideoEvent, VideoState, FrameData, FrameMetadata, TargetResolution, VideoMetadata, HdrInfo, FileSupportResult, } from './NativeController';
//# sourceMappingURL=index.d.ts.map